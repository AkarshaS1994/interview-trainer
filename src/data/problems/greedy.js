export const GREEDY_PROBLEMS = [
  {
    id: "gy1", title: "Jump Game", difficulty: "Medium", pattern: "Greedy",
    companies: ["Amazon","Meta","Google"], minLevel: 3,
    description: "Given array where nums[i] is the max jump length from index i, return true if you can reach the last index starting from index 0.",
    example: { input: "nums=[2,3,1,1,4]", output: "true" },
    hints: ["Track the farthest index reachable so far.", "At each index: if index > farthest, you're stuck — return false.", "If farthest >= last index, return true."],
    clarify: { question: "What would you clarify?", options: ["Can I jump exactly the number shown or up to it?","Are elements non-negative?","What if nums has only one element?","Must I reach the LAST index, not beyond?","Can I jump backwards?"], mustAsk: [0,1,4], explanation: "Can jump up to nums[i] steps (not exactly). Elements ≥ 0. Single element: already at last index → true. Reach last index (index n-1). No backwards jumps." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What does it mean to be 'stuck'? When is an index unreachable?", why: { question: "When can you not reach index i?", options: ["When nums[i]==0","When no previously reachable index j<i has j+nums[j]>=i. The farthest you could reach from all j<i is less than i.","When the array has zeros"], correct: 1, explanation: "Index i is reachable only if some index j<i with j+nums[j]>=i was itself reachable. If farthest reachable < i, then i and everything beyond is unreachable." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you BFS/DFS from index 0 trying all jump sizes? What's the complexity?", why: { question: "Why can BFS/DFS be O(n²) here?", options: ["It's O(n)","At each index, try all jump sizes 1..nums[i]. Total edges = sum of nums[i] values which can be O(n²) in worst case (all values = n).","Always O(n log n)"], correct: 1, explanation: "BFS/DFS: each reachable node tries all jump sizes. Worst case (e.g. nums=[n,n,n,...]): O(n²) edges explored. Greedy reduces this to O(n)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "What single value tracks whether each index is reachable? What's the greedy insight?", why: { question: "What does 'farthest' represent?", options: ["The index with the highest value","The maximum index reachable from any index 0..current. If current > farthest: stuck.","The last index jumped from"], correct: 1, explanation: "farthest = max(j + nums[j]) for all reachable j ≤ current. Update at every step. If current index > farthest: this index was never reachable — can't continue." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through the greedy algorithm. When do you update farthest vs return false?", why: { question: "Why can you stop early when farthest >= n-1?", options: ["Must always process all elements","Once farthest >= last index: last index is already confirmed reachable. No need to continue. Return true immediately.","Farthest can decrease later"], correct: 1, explanation: "farthest is monotonically non-decreasing (max function). Once farthest >= n-1: last index is reachable. Safe to return true immediately — farthest can't decrease." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "DP approach O(n²) vs greedy O(n). Describe the DP and why greedy is better.", why: { question: "What does the DP state dp[i] represent?", options: ["dp[i] = max jump from i","dp[i] = true if index i is reachable. For each i: check all j<i where dp[j]=true and j+nums[j]>=i. O(n²).","dp[i] = number of ways to reach i"], correct: 1, explanation: "DP: O(n²) — for each i, scan all previous reachable positions. Greedy equivalent: track only the maximum reachable frontier. One pass, O(n). Greedy subsumes the DP insight." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space?", why: { question: "Why O(1) space for greedy?", options: ["O(n) for tracking reachable positions","Single variable 'farthest' — constant space regardless of n","O(n) stack frames"], correct: 1, explanation: "One variable farthest, one loop variable i. O(1) extra space. O(n) time — single pass through array." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "nums=[0,1]: can you reach index 1?", options: ["Yes — jump from 0","No — nums[0]=0, can only stay at 0. farthest=0, i=1>farthest=0. Return false.","Depends on direction"], correct: 1, explanation: "nums[0]=0: can't jump anywhere. farthest=max(0,0+0)=0. At i=1: 1>farthest=0 → stuck, return false. Correct." } },
    ],
    dryrun: { question: "nums=[2,3,1,1,4]. farthest=0. i=0: farthest=max(0,0+2)=2. i=1: farthest=max(2,1+3)=4. i=2: farthest=max(4,2+1)=4. farthest=4>=4(last index). Return?", options: ["true — last index reachable","false — not enough","Continue to i=3"], correct: 0, explanation: "After i=1: farthest=4=last index. Return true immediately (can early-exit here). Last index confirmed reachable via index 1 (jump 3 steps: 1+3=4)." },
    pushback: { challenge: "What is the MINIMUM number of jumps to reach the last index?", options: ["Greedy with two pointers: track current range [l,r] and next farthest. Each time r is reached: increment jumps, set l=r+1, r=nextFarthest. O(n).","Same single-variable greedy","BFS only approach"], correct: 0, explanation: "Jump Game II: greedy with 'jump boundary'. Track current max reach and next max reach. When you exhaust current reach: must jump (increment count). Set new reach = next max. O(n) O(1)." },
    followup: { question: "What if jumps are bidirectional (can jump forward or backward)?", options: ["BFS from index 0: enqueue all reachable indices, mark visited. O(n) time O(n) space. Greedy doesn't apply (can revisit).","Same greedy","Always reachable if any element > 0"], correct: 0, explanation: "Bidirectional: BFS/DFS needed to track visited nodes (can revisit). Greedy fails: might go backward to reach unreachable forward area. BFS guarantees reachability check correctly." },
    solution: { approach: "Greedy — Track Farthest Reach", steps: ["farthest=0","For i in 0..n-1: if i > farthest: return false","farthest=max(farthest, i+nums[i])","If farthest>=n-1: return true","Return true"], time: "O(n)", space: "O(1)" },
    keywords: {
      understand: ["reachable","farthest","stuck","unreachable","index","jump","frontier"],
      brute: ["O(n²)","BFS","DFS","all jump sizes","edges","try","explore"],
      pattern: ["farthest","max","greedy","frontier","current","reachable","track"],
      algorithm: ["farthest","update","i > farthest","return false","O(1)","single pass"],
      tradeoff: ["O(n²)","DP","O(n)","greedy","track","frontier","subsumes"],
      complexity: ["O(n)","time","O(1)","space","single variable","farthest","constant"],
      edge: ["nums[0]=0","stuck","single element","true","all zeros","false"],
    },
  },
  {
    id: "gy2", title: "Task Scheduler", difficulty: "Medium", pattern: "Greedy",
    companies: ["Amazon","Meta","Google"], minLevel: 3,
    description: "Given tasks (letters A-Z) and a cooldown n, find the minimum number of intervals to finish all tasks. Between two same tasks, must wait at least n intervals (can insert idle intervals).",
    example: { input: 'tasks=["A","A","A","B","B","B"], n=2', output: "8 (A→B→idle→A→B→idle→A→B)" },
    hints: ["Most frequent task determines the minimum total time.", "Formula: max(len(tasks), (maxFreq-1)*(n+1) + countOfMaxFreqTasks).", "Slots = (maxFreq-1) slots of size (n+1), plus final cycle."],
    clarify: { question: "What would you clarify?", options: ["Can idle intervals be inserted anywhere?","Are all task types from A-Z?","Is n the cooldown or window size?","What if n=0?","Can we reorder tasks freely?"], mustAsk: [0,3,4], explanation: "Yes, insert idle anywhere. n=cooldown between same tasks (n=0: no cooldown, answer=len(tasks)). Can reorder freely — just must respect cooldown between same letter." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why does the most frequent task determine the minimum total time?", why: { question: "Why does maxFreq (highest frequency task) drive the answer?", options: ["We execute all tasks once","The most frequent task creates 'mandatory gaps'. Must execute it maxFreq times with at least n gaps between. All other tasks fill these gaps — reducing idle time.","Alphabetically first task"], correct: 1, explanation: "Task A with maxFreq=3, n=2: must do A_?_A_?_A. Each ? is n=2 slots. Two gaps of size n+1 each, plus final A. This skeleton = (maxFreq-1)*(n+1)+1. Other tasks fill the ?s." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would a priority queue simulate the scheduler? What's the cost?", why: { question: "Priority queue simulation — what's its time complexity?", options: ["O(n) tasks × O(n) cooldown = O(n²)","O(n log k) where k=distinct tasks (≤26). Always O(n log 26)=O(n). But more complex code than formula.","O(n!)"], correct: 1, explanation: "PQ simulation: O(n) total tasks, each requiring O(log k) heap operations. O(n log k) = O(n) since k≤26. Correct but verbose — the formula is O(n) and simpler." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Derive the formula: why (maxFreq-1)*(n+1) + countOfMaxFreqTasks?", why: { question: "What does (maxFreq-1)*(n+1) represent?", options: ["Total tasks","(maxFreq-1) full cycles of size (n+1). Each cycle: one execution of the busiest task + n slots for others/idle. Last partial cycle = only the maxFreq tasks.","Total idle time"], correct: 1, explanation: "Cycles: A_??_A_??_A (n=2). First two A's each anchor a cycle of length 3 (n+1). Last A is alone. = (3-1)×3 + 1 = 7. If multiple tasks tie for max: + each tied task at end. Formula total = (maxFreq-1)*(n+1)+countMax." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Why does the final answer use max(len(tasks), formula)? When is len(tasks) the answer?", why: { question: "When does len(tasks) exceed the formula?", options: ["Never — formula is always correct","When tasks are diverse enough to fill all slots with no idle. e.g. n=0: no cooldown needed. Or enough different tasks to use all slots. Formula may undercount actual task count.","When n=0 only"], correct: 1, explanation: "If diverse tasks fill all slots: no idle needed. Formula might give < len(tasks) because it models idle. Actual answer: max(len(tasks), formula). len(tasks) is a lower bound — can't finish faster than executing all tasks." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Formula O(n) vs PQ simulation O(n log k). Both correct — when would you use simulation?", why: { question: "When is PQ simulation preferable over the formula?", options: ["Always use formula","When problem changes: e.g. tasks have different durations, or cooldown varies by task type. Formula is brittle to variations; simulation generalizes easily.","Simulation is always slower"], correct: 1, explanation: "Formula: elegant for this exact problem. Simulation: adaptable. If task A takes 3 units and task B takes 2 units: formula breaks, simulation extends naturally. Real-world schedulers use simulation." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity of formula approach?", why: { question: "Why O(n) time and O(1) space?", options: ["O(n log n) for sort","Count frequencies: O(n) to build freq array of size 26 (O(1)). Find max: O(26)=O(1). Formula: O(1). Total: O(n) time, O(1) space (26-element array is constant).","O(n) space for freq array"], correct: 1, explanation: "O(n): one pass to build frequency counts. O(1): 26-element array is constant regardless of n. Max find: O(26)=O(1). Formula: arithmetic. Space: O(1)." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "n=0 — what is the answer?", options: ["0","len(tasks) — no cooldown means tasks can run back-to-back with no idle. Formula: (maxFreq-1)×1+count = len(tasks) at minimum. max(len,formula)=len(tasks).","maxFreq×(n+1)"], correct: 1, explanation: "n=0: no cooldown needed. Execute all tasks consecutively. Answer = len(tasks). Formula: (maxFreq-1)×1+count. max(len(tasks), formula) = len(tasks). Correct." } },
    ],
    dryrun: { question: 'tasks=["A","A","A","B","B","B"], n=2. Freq: A=3, B=3. maxFreq=3. countMax=2 (A and B both have freq 3). Formula=(3-1)*(2+1)+2=2*3+2=8. len(tasks)=6. Answer=max(6,8)=?', options: ["8","6","7"], correct: 0, explanation: "max(6,8)=8. Schedule: A→B→idle→A→B→idle→A→B. 8 intervals. The two tasks of max frequency each anchor the end of the last cycle. Formula works." },
    pushback: { challenge: "What if tasks also have priorities and higher-priority tasks must run before lower-priority ones?", options: ["Hybrid: greedy schedule respecting priorities using PQ sorted by (priority desc, frequency desc). Complex but O(n log k).","Priority makes scheduling NP-hard","Same formula still works"], correct: 0, explanation: "Priority constraint: PQ simulation with multi-key sort (priority, then frequency). At each slot: pick highest-priority available task not in cooldown. O(n log k). Formula breaks." },
    followup: { question: "What is the MINIMUM idle time specifically (not total intervals)?", options: ["idle_time = max(0, (maxFreq-1)*(n+1) + countMax - len(tasks)). Same formula, subtract actual tasks from total.","Count idle intervals by simulation","Always maxFreq*n idle slots"], correct: 0, explanation: "Idle time = total intervals - actual tasks = max(len(tasks), formula) - len(tasks) = max(0, formula - len(tasks)). Simple subtraction from the same formula." },
    solution: { approach: "Greedy Formula", steps: ["Count task frequencies","maxFreq = max frequency, countMax = count of tasks with maxFreq","formula = (maxFreq-1)*(n+1) + countMax","Return max(len(tasks), formula)"], time: "O(n)", space: "O(1)" },
    keywords: {
      understand: ["maxFreq","mandatory","gaps","cooldown","skeleton","cycles","frequent","intervals"],
      brute: ["priority queue","O(n log k)","simulate","verbose","O(n)","heap"],
      pattern: ["formula","(maxFreq-1)","n+1","countMax","cycles","slots","idle"],
      algorithm: ["max","len(tasks)","formula","lower bound","diverse","no idle"],
      tradeoff: ["formula","simulation","generalize","brittle","variations","duration"],
      complexity: ["O(n)","time","O(1)","space","26","freq","constant"],
      edge: ["n=0","len(tasks)","back-to-back","no cooldown","single task"],
    },
  },
  {
    id: "gy3", title: "Assign Cookies", difficulty: "Easy", pattern: "Greedy",
    companies: ["Amazon","Google"], minLevel: 1,
    description: "Assign cookies to children. Each child i has a greed factor g[i]. Each cookie j has size s[j]. Assign cookie j to child i if s[j] >= g[i]. Maximize the number of content children.",
    example: { input: "g=[1,2,3], s=[1,1]", output: "1 (give cookie 1 to child 1, child 2 needs 2 but only have 1s)" },
    hints: ["Sort both greed factors and cookie sizes.", "Two pointers: try to satisfy the least greedy child with the smallest cookie.", "If cookie satisfies child: advance both. Else: advance cookie only."],
    clarify: { question: "What would you clarify?", options: ["One cookie per child?","One assignment per cookie?","Does every child need to be assigned?","Can a child receive multiple cookies?","Return count or assignment?"], mustAsk: [0,1,4], explanation: "One cookie per child. Each cookie used at most once. Not every child needs a cookie — maximize satisfied count. Return the count." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What's the greedy strategy? Why match the smallest adequate cookie to the least greedy child?", why: { question: "Why not give the biggest cookie to the greediest child?", options: ["That works too","'Smallest adequate cookie to least greedy child': wastes no cookies. A big cookie on a greedy child might satisfy them but wastes capacity. Smallest adequate = exactly sufficient.","Order doesn't matter"], correct: 1, explanation: "Key insight: each cookie can satisfy at most one child. Use smallest cookie that satisfies each child, in order of increasing greed. Avoids 'wasting' big cookies on small-greed children." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you try all assignments? Why is it exponential?", why: { question: "Why brute force is O(n×m) at minimum?", options: ["O(1)","Try each child-cookie pair: n children × m cookies = O(n×m) naive. With matching: O(n! × m!) to try all assignments. Sorting + greedy: O((n+m) log(n+m)).","O(n²)"], correct: 1, explanation: "Brute force all assignments: exponential. Sort + greedy two-pointer: O(n+m) after sorting. Sorting dominates at O((n+m) log(n+m))." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does sorting both arrays and using two pointers enable the O(n+m) scan?", why: { question: "Why sort both arrays?", options: ["Arbitrary","Sort greed: try least-demanding first. Sort sizes: use smallest cookie first. Two pointers process both in one scan without backtracking.","Only sort one array"], correct: 1, explanation: "Both sorted: can scan both left-to-right. If s[j] >= g[i]: satisfy child, advance both. Else: cookie too small, try next cookie. No backtracking needed — monotone structure." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: i=0, j=0. If s[j]>=g[i]: satisfy, i++, j++. Else: j++ only. Count how many i advanced.", why: { question: "Why advance j alone when s[j]<g[i]?", options: ["Bug — should advance i","Cookie is too small for LEAST greedy child. It won't satisfy any remaining child (all more greedy). Move to bigger cookie.","Advance i alone"], correct: 1, explanation: "g is sorted: if s[j] < g[i], it's also < g[i+1], g[i+2], etc. Cookie j can't satisfy anyone. Discard cookie j, try j+1. Never need to 'try' discarded cookie again." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "This greedy is optimal. How would you prove it can't be improved?", why: { question: "Why is the greedy strategy provably optimal?", options: ["It might not be","Exchange argument: any other assignment either leaves a child unsatisfied who could be satisfied, or uses a bigger cookie when a smaller one works. Greedy is non-wasteful.","Only works for sorted arrays"], correct: 1, explanation: "Exchange argument: suppose optimal assigns cookie c2>c1 to child g1 and c1 to some other. We can swap: give c1 to g1 (still satisfied), free c2 for others. Greedy with smallest adequate is never worse." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O((n+m) log(n+m)) not O(n+m)?", options: ["Sorting dominates","Two-pointer scan: O(n+m). Sort: O(n log n + m log m). Total: O((n+m) log(n+m)). Sorting dominates the linear scan.","O(n²)"], correct: 1, explanation: "Sort g: O(n log n). Sort s: O(m log m). Two-pointer: O(n+m). Total dominated by sort: O((n+m) log(n+m)). Space: O(1) extra (sort in-place)." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "All children greedier than all cookies — answer?", options: ["Error","0 — no cookie can satisfy any child. j exhausted before any i advances.","1"], correct: 1, explanation: "All g[i] > max(s): cookie j always < g[i]. j pointer exhausted first. i never advances. Return 0." } },
    ],
    dryrun: { question: "g=[1,2,3], s=[1,1]. Sorted: same. i=0,j=0. s[0]=1>=g[0]=1: satisfy! i=1,j=1. s[1]=1<g[1]=2: j++. j=2, loop ends. Return?", options: ["1","2","0"], correct: 0, explanation: "Only 1 child satisfied (child with g=1). Two cookies both size 1 — second child needs 2. Return 1." },
    pushback: { challenge: "What if a child can receive multiple cookies and greed is satisfied when total ≥ g[i]?", options: ["Greedy with min-heap: always give smallest cookie to most-greedable unmet need. Or DP. O(n×m) DP.","Same two-pointer","Sort by total needed"], correct: 0, explanation: "Multiple cookies per child: different problem. DP[i][j] = max children satisfied using first i children, first j cookies. Or greedy heuristic. Two-pointer no longer works." },
    followup: { question: "What if cookies expire (can only be used within a time window) and you must schedule assignments?", options: ["Earliest deadline first: sort children by arrival, sort cookies by expiry. Use greedy matching respecting time windows. O((n+m) log(n+m)).","Same two-pointer","Cookie expiry doesn't matter"], correct: 0, explanation: "Deadline-constrained: sort cookies by expiry. For each expiry deadline: greedily assign to a waiting child who needs the smallest cookie. Earliest deadline first scheduling." },
    solution: { approach: "Sort + Two Pointers", steps: ["Sort g (greed factors), sort s (cookie sizes)","i=0 (child pointer), j=0 (cookie pointer)","While i < len(g) and j < len(s): if s[j]>=g[i]: i++ (satisfied); j++ (always)","Return i (satisfied count)"], time: "O((n+m) log(n+m))", space: "O(1)" },
    keywords: {
      understand: ["smallest","adequate","greed","cookie","satisfy","maximize","waste"],
      brute: ["O(n×m)","all","assignments","exponential","try","matching"],
      pattern: ["sort","both","two pointers","scan","least greedy","smallest","one pass"],
      algorithm: ["i++","j++","s[j]>=g[i]","satisfy","advance","cookie","child"],
      tradeoff: ["exchange","optimal","non-wasteful","never worse","provably"],
      complexity: ["O((n+m) log(n+m))","sort","dominates","O(1)","space","O(n+m)"],
      edge: ["all greedy","0","no match","exhausted","cookies smaller"],
    },
  },
  {
    id: "gy4", title: "Gas Station", difficulty: "Medium", pattern: "Greedy",
    companies: ["Amazon","Meta","Google"], minLevel: 3,
    description: "There are n gas stations in a circle. gas[i] is the fuel at station i. cost[i] is the fuel to travel from i to i+1. Starting with an empty tank, find the starting station index to complete the circuit, or -1 if impossible.",
    example: { input: "gas=[1,2,3,4,5], cost=[3,4,5,1,2]", output: "3" },
    hints: ["If total gas >= total cost, a solution exists and is unique.", "Start at any point where running total goes negative — skip it and restart.", "If current tank < 0 at station i: start from i+1."],
    clarify: { question: "What would you clarify?", options: ["Circle means go around back to start?","Is the answer guaranteed unique if it exists?","Tank must never go negative mid-circuit?","Can gas or cost be 0?","Does starting tank matter (guaranteed empty)?"], mustAsk: [0,1,2], explanation: "Circular route. If solution exists: exactly one solution (unique). Tank must be non-negative at every station. Start with empty tank." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What two conditions determine if a starting station is valid?", why: { question: "What are the two necessary conditions for a valid start?", options: ["max(gas)>max(cost)","(1) Total gas >= total cost (global feasibility). (2) Start at a station where the cumulative surplus never goes negative from start back to start.","gas[i]>cost[i] for start"], correct: 1, explanation: "Necessary: sum(gas) >= sum(cost). Sufficient: starting at the right station, cumulative surplus (gas[i]-cost[i]) stays non-negative throughout the circuit." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you try every starting station? What's the complexity?", why: { question: "Why O(n²) for brute force?", options: ["O(n) possible starts × O(n) circuit check each = O(n²)","O(n)","Always O(n³)"], correct: 0, explanation: "n starting stations. For each: simulate entire n-station circuit checking if tank stays non-negative. O(n) per start × n starts = O(n²)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "The greedy key insight: why can you rule out stations 0..i-1 if tank goes negative at station i?", why: { question: "If starting at 0 and tank goes negative at station i, why can't any station 0..i be the answer?", options: ["Can't conclude that","Starting at any station 0..i: reach i with same or less gas (since i was draining). If starting at 0 failed to reach i+1, starting anywhere from 0 to i-1 also fails to reach i+1.","Only station 0 fails"], correct: 1, explanation: "Key lemma: if starting from station s and tank goes negative at station f, no station between s and f can be a valid start. All of them have less fuel at f than starting from s." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: track current tank and total surplus. Reset start when tank goes negative.", why: { question: "Why does the algorithm need only ONE pass?", options: ["Can't do in one pass","Track total (to check feasibility) and current (to find start). When current<0: reset to next station. Final: if total>=0, return start; else -1.","Must restart from candidate"], correct: 1, explanation: "One pass: total tracks global feasibility. current tracks local tank from candidate start. When current<0: proven start can't work, try next. Total at end: if<0, no solution." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "O(n) greedy vs O(n²) brute force. What makes the greedy provably correct?", why: { question: "How do you know exactly ONE valid start exists when total>=0?", options: ["Multiple starts possible","Mathematical proof: sum(gas-cost)>=0 implies exactly one station where cumulative sum from that point never goes negative. Circular property guarantees uniqueness.","Just an observation"], correct: 1, explanation: "Existence: sum>=0 guarantees at least one valid start. Uniqueness: if two valid starts existed, the first would accumulate surplus before reaching the second — contradiction with the second being a 'reset' point." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space?", why: { question: "Why O(n) and O(1)?", options: ["O(n log n) sort needed","Single pass through n stations: O(n). Three variables (total, current, start): O(1) space.","O(n) space for simulation"], correct: 1, explanation: "One pass: O(n). Variables: total surplus, current tank, start index. O(1) space. No extra arrays or data structures needed." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "n=1, gas=[5], cost=[5] — answer?", options: ["-1","0 — station 0: pick up 5, cost 5, return to 0. Tank never negative. Single station = valid.","1"], correct: 1, explanation: "n=1: only station 0. gas[0]=5, cost[0]=5. Tank: start 0+5=5, drive to next (which is 0): -5=0. Complete circuit. Return 0." } },
    ],
    dryrun: { question: "gas=[1,2,3,4,5], cost=[3,4,5,1,2]. net=[−2,−2,−2,3,3]. Run: total=-2-2-2+3+3=0>=0. current: -2→reset start=1; from 1: -2→reset start=2; from 2: -2→reset start=3; from 3: 3→4→5≥0. Return start=?", options: ["3","4","0"], correct: 0, explanation: "Start=3: net[3]=3, net[4]=3, net[0]=-2, net[1]=-2, net[2]=-2. Running sum from 3: 3,6,4,2,0. Never negative. Return 3." },
    pushback: { challenge: "What if you start with a non-empty tank (initial fuel given)?", options: ["Add initial fuel to the starting tank. Check if total+initial>=0. Otherwise same algorithm. O(n).","Must resimulate all stations","Different algorithm needed"], correct: 0, explanation: "Initial fuel: add to current at start. Check feasibility: sum(gas)+initial >= sum(cost). Finding start: same greedy (can subtract initial from net[start] or handle at start node). O(n)." },
    followup: { question: "What if some roads are one-way (can't go back) and you need the MINIMUM starting fuel to complete a specific path?", options: ["Binary search on starting fuel: can complete with F fuel? O(n) check. O(n log(maxFuel)) total.","Same gas station algorithm","DP on stations"], correct: 0, explanation: "Minimum starting fuel for directed path: binary search on answer. For given fuel F: simulate trip, check if tank stays ≥ 0. Binary search on F in [0, sum(cost)]. O(n log(sum)) total." },
    solution: { approach: "Greedy — One Pass", steps: ["total=0, current=0, start=0","For i in 0..n-1: total+=gas[i]-cost[i]; current+=gas[i]-cost[i]","If current<0: start=i+1, current=0","Return start if total>=0 else -1"], time: "O(n)", space: "O(1)" },
    keywords: {
      understand: ["total","surplus","non-negative","tank","circuit","start","global","local"],
      brute: ["O(n²)","every","starting","simulate","check","n starts","n steps"],
      pattern: ["reset","negative","candidate","next","lemma","between","any"],
      algorithm: ["total","current","start","reset","i+1","one pass","tank"],
      tradeoff: ["unique","total>=0","circular","existence","uniqueness","proof"],
      complexity: ["O(n)","time","O(1)","space","three variables","single pass"],
      edge: ["n=1","equal","gas=cost","0","complete circuit","return 0"],
    },
  },
];
