export const STACK_QUEUE_PROBLEMS = [
  {
    id: "sq1", title: "Valid Parentheses", difficulty: "Easy", pattern: "Stack / Queue",
    companies: ["Meta","Amazon","Google"], minLevel: 1,
    description: "Given a string of brackets '(', ')', '{', '}', '[', ']', determine if the input string is valid. Every open bracket must be closed by the same type in correct order.",
    example: { input: 's="()[]{}"', output: "true" },
    hints: ["Use a stack: push on open bracket, pop and match on close bracket.", "If stack has leftovers at end, string is invalid.", "On close bracket: stack must be non-empty and top must match."],
    clarify: { question: "What would you clarify?", options: ["Only bracket characters or other characters too?","Empty string — valid or invalid?","Is case sensitivity relevant?","Can string have whitespace?","What about nested brackets?"], mustAsk: [0,1], explanation: "Only bracket characters. Empty string is valid (vacuously). Case not relevant — no letter brackets. No whitespace. Nested brackets must be properly ordered and closed." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What makes this problem 'last in, first out'? Why is a stack the natural choice?", why: { question: "Why does this problem have LIFO structure?", options: ["Random order","The most recently opened bracket must be closed first. Nesting forces LIFO: ( [ { } ] ) — innermost closed before outermost.","Queues work better","Arrays are sufficient"], correct: 1, explanation: "LIFO: in '([{', the last opened '{' must be closed first. Stack naturally models this — push on open, peek/pop on close, last pushed = expected close." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you repeatedly replace matched pairs until empty or no matches? What's the cost?", why: { question: "Why is replacement approach O(n²)?", options: ["It's O(n)","Scan for innermost pair, remove (O(n)), repeat. Worst case n/2 passes × O(n) each = O(n²).","It's correct but O(n log n)","O(n) with the right data structure"], correct: 1, explanation: "Repeatedly scan and remove matched pairs: '()' → '', '{}' → '', etc. Each scan O(n), up to n/2 replacements. O(n²). Stack approach is O(n)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does the stack encode the 'expected close bracket' at every point in the string?", why: { question: "What does the stack contain at any point?", options: ["All open brackets seen","Stack top = most recently opened bracket = the NEXT expected close bracket. Stack encodes all pending 'IOUs' in order.","Closed brackets","Count of each bracket type"], correct: 1, explanation: "Every unmatched open bracket is an 'IOU'. Stack holds them in order, top = most recent IOU = next expected close. When close arrives: pop and verify match." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through the algorithm for each character type.", why: { question: "When do you return false immediately (not wait for end)?", options: ["Never — always check at end","When close bracket arrives but: (a) stack is empty (no matching open) or (b) top doesn't match the close. Fail immediately.","Only at end","After scanning all characters"], correct: 1, explanation: "Fail fast: on ')' with empty stack → false. On ')' when top is '[' → false. No point continuing — the string is already invalid." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Stack approach O(n) space — can you do O(1) space? What's the catch?", why: { question: "Why can't you do O(1) space for general valid parentheses?", options: ["You can — use counters","Multiple bracket types: ')' requires knowing type of open bracket. Counter for '(' can't distinguish between '(' and '['. O(1) only works for single bracket type.","Always O(n) space required","Use three counters instead"], correct: 1, explanation: "Single type (e.g. only ()): O(1) counter. Multi-type: need to know WHICH bracket was opened. Stack holds bracket identity. No way to retrieve that in O(1) space." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space of stack solution?", why: { question: "Why O(n) space worst case?", options: ["Always O(1)","If all characters are open brackets '((((': push all n → stack size n. O(n) space worst case.","O(log n)","O(n²) space"], correct: 1, explanation: "String of all open brackets: push all n → O(n) stack. Average case for balanced strings: O(n/2). O(n) time (single pass), O(n) space worst." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "List all edge cases.", why: { question: "String ']' — what happens?", options: ["Returns true","Close bracket with empty stack → return false immediately","Stack error","Returns null"], correct: 1, explanation: "']' arrives, stack empty: no matching open. Return false. Also: '([)]' — passes naive count but stack catches mismatch: '(' then '[' then ')' doesn't match '['. Return false." } },
    ],
    dryrun: { input: 's="({[]})"', steps: [{ label: "push '('", state: "stack=['(']" }, { label: "push '{'", state: "stack=['(', '{']" }, { label: "push '['", state: "stack=['(', '{', '[']" }, { label: "']': pop '[', matches ']'", state: "stack=['(', '{']" }, { label: "'}': pop '{', matches '}'", state: "stack=['(']" }, { label: "')': pop '(', matches ')'", state: "stack=[]" }], question: 's="({[]})". Process: push (, push {, push [, then ]: pop [ matches ]. Then }: pop { matches }. Then ): pop ( matches ). Stack empty at end?', options: ["Yes — return true","No — one item left","Error on ]","Stack has '{' remaining"], correct: 0, explanation: "Stack operations: push (, push {, push [. ']' arrives: pop [, matches. '}' arrives: pop {, matches. ')' arrives: pop (, matches. Stack empty → return true." },
    pushback: { question: "What if you also need to return the index of the first invalid bracket?", answer: "Track current index alongside stack operations. Return current index when mismatch or stack underflow detected." },
    followup: { question: "What is the minimum number of bracket reversals to make a string valid?", answer: "Count unmatched opens (a) and closes (b) after stack processing. Answer: ceil(a/2) + ceil(b/2). O(n)." },
    solution: { approach: "Stack with Bracket Matching", steps: ["stack = [], pairs = {')':'(', '}':'{', ']':'['}", "For each char: if open bracket: push", "If close: if stack empty or stack.top != pairs[char]: return false; pop", "Return stack is empty"], time: "O(n)", space: "O(n)" },
    keywords: {
      understand: ["LIFO","stack","last","first","nesting","open","close","innermost"],
      brute: ["O(n²)","replace","remove","innermost","pair","scan","repeated"],
      pattern: ["IOU","pending","top","expected","close","open","match","pop"],
      algorithm: ["push","pop","empty","mismatch","fail fast","pairs","top","match"],
      tradeoff: ["O(1)","counter","single type","multi-type","distinguish","bracket type"],
      complexity: ["O(n)","space","time","worst","all open","O(n)","stack size"],
      edge: ["empty","single close","([)]","mismatch","balanced","unmatched"],
    },
  },
  {
    id: "sq2", title: "Min Stack", difficulty: "Medium", pattern: "Stack / Queue",
    companies: ["Amazon","Meta","Google"], minLevel: 3,
    description: "Design a stack that supports push, pop, top, and getMin — all in O(1) time. getMin returns the minimum element in the current stack.",
    example: { input: 'push(-2), push(0), push(-3), getMin()→-3, pop(), getMin()→-2', output: "All operations O(1)" },
    hints: ["Track minimum alongside each element, or use a second auxiliary stack.", "When pushing: also push the current minimum (including new element) to min stack.", "When popping: pop from both stacks."],
    clarify: { question: "What would you clarify?", options: ["Must getMin be O(1) — not O(n)?","What if pop or top called on empty stack?","Can elements be negative?","Must it handle duplicates correctly?","Is there a space limit?"], mustAsk: [0,1,3], explanation: "O(1) required for all operations — key constraint. Empty stack behavior: undefined/guaranteed not called per problem. Negatives allowed. Duplicates: getMin after popping a duplicate min must return previous min." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why is getMin O(n) with a regular stack? What information do you need to precompute?", why: { question: "Why can't a regular stack do getMin in O(1)?", options: ["It can — just check top","Must scan all elements to find minimum — O(n). No way to jump to minimum without tracking it.","Sort the stack","Use a priority queue"], correct: 1, explanation: "Regular stack: elements added in arbitrary order. To find min, scan all: O(n). Need to track minimum incrementally as elements are pushed/popped." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you track min with a separate sorted structure? Why is it overkill?", why: { question: "Why is a sorted set overkill for tracking min?", options: ["It's not — sorted set is efficient","Sorted set: O(log n) insert/delete. But we only need the minimum — O(log n) is worse than needed. A second stack achieves O(1) for min.","Can't use sorted set with stack","Both are O(1)"], correct: 1, explanation: "Only need min, not full sorted order. Sorted set wastes work. Min stack (second stack tracking running minimums) is O(1) insert/delete/query — simpler and faster." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does an auxiliary 'min stack' track the minimum at every point in history?", why: { question: "What does the min stack contain at any point?", options: ["All minimums ever seen","Current minimum at each depth. Top of min stack = current minimum. When main stack pops, min stack syncs to reveal previous minimum.","Just the global minimum","The last 5 minimums"], correct: 1, explanation: "Min stack mirrors main stack depth. Each push to main stack: push min(newVal, minStack.top) to min stack. Pop syncs. At any depth, minStack.top = minimum of elements up to that depth." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through push, pop, top, and getMin implementations.", why: { question: "Why push to min stack on EVERY push, not just when new element is smaller?", options: ["Only push when new minimum","Must maintain same depth as main stack. If pop() always pops both, they must always be in sync. Selective push breaks sync.","Doesn't matter","Save space by not always pushing"], correct: 1, explanation: "Min stack must mirror main stack depth so pop() can be coordinated. Always push min(new, currentMin) keeps them in sync and O(1) per operation." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Auxiliary min stack (2x space) vs pair encoding (same space) — describe both.", why: { question: "What's the pair encoding approach?", options: ["Store value and index","Store (value, currentMin) as pair in single stack. Same O(1) operations, avoids second stack object but same space asymptotically.","Can't do with single stack","Pair encoding uses less space"], correct: 1, explanation: "Pair approach: push (val, currentMin) as tuple. getMin: return stack.top[1]. Same O(1) all ops, same O(n) space, no second stack object. Either is accepted." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity for all operations?", why: { question: "Why O(n) space for min stack approach?", options: ["O(1) — we only store minimums","Both main and min stacks grow proportionally to pushes. n pushes → n entries in each stack → O(n) total.","O(n²)","O(log n)"], correct: 1, explanation: "Worst case: n pushes, each grows both stacks. 2n total entries = O(n) space. Each operation O(1). Constant-size overhead beyond the elements themselves." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases involve duplicates?", why: { question: "push(3), push(3), pop(), getMin() — what is the min after pop?", options: ["Undefined","3 — the duplicate 3 remains. Min stack correctly returns 3 as min for remaining stack.","Error","0"], correct: 1, explanation: "push(3): minStack→[3]. push(3): minStack→[3,3] (min stays 3). pop(): both stacks pop, minStack top=3. getMin()=3. Duplicates work correctly because min stack always syncs." } },
    ],
    dryrun: { input: "push(-2), push(0), push(-3)", steps: [{ label: "push(-2)", state: "main=[-2], min=[-2]" }, { label: "push(0)", state: "main=[-2,0], min=[-2,-2]" }, { label: "push(-3)", state: "main=[-2,0,-3], min=[-2,-2,-3]" }, { label: "getMin()", state: "top of min = -3" }, { label: "pop()", state: "main=[-2,0], min=[-2,-2]" }, { label: "getMin()", state: "top of min = -2" }], question: "push(-2), push(0), push(-3). getMin()=?. pop(). getMin()=?", options: ["getMin()=-3, then after pop, getMin()=-2","getMin()=0, then -2","getMin()=-3, then 0","getMin()=-2, then -3"], correct: 0, explanation: "Top of min stack = -3. getMin()=-3. pop() removes both stacks' tops: main=[-2,0], min=[-2,-2]. New min top=-2. getMin()=-2. Correct." },
    pushback: { question: "What if you need getMax as well as getMin?", answer: "Add a second auxiliary max stack alongside min stack. Same approach: push max(val, maxStack.top). getMax: return maxStack.top. Pop: sync maxStack. All O(1), O(n) each." },
    followup: { question: "Design a queue using two stacks (enqueue/dequeue in amortized O(1))?", answer: "Two stacks: inbox and outbox. Enqueue: push to inbox. Dequeue: if outbox empty, pour inbox→outbox (reverse). Then pop from outbox. Amortized O(1)." },
    solution: { approach: "Auxiliary Min Stack", steps: ["Two stacks: main and minStack", "push(val): main.push(val); minStack.push(min(val, minStack.top or val))", "pop(): main.pop(); minStack.pop()", "top(): return main.top()", "getMin(): return minStack.top()"], time: "O(1) all ops", space: "O(n)" },
    keywords: {
      understand: ["getMin","O(1)","track","minimum","incremental","precompute","scan","history"],
      brute: ["sorted set","O(log n)","overkill","minimum","only need","O(1)"],
      pattern: ["auxiliary","min stack","depth","sync","current minimum","pop","mirror"],
      algorithm: ["min(val","top)","push","pop","sync","depth","O(1)","every"],
      tradeoff: ["pair","tuple","currentMin","single stack","2x space","asymptotically"],
      complexity: ["O(1)","O(n)","space","2n","proportional","all operations"],
      edge: ["duplicate","sync","pop","getMin","negative","pair"],
    },
  },
  {
    id: "sq3", title: "Daily Temperatures", difficulty: "Medium", pattern: "Stack / Queue",
    companies: ["Amazon","Google","Meta"], minLevel: 3,
    description: "Given daily temperatures, return an array where answer[i] is the number of days until a warmer temperature. If no warmer day exists, answer[i] = 0.",
    example: { input: "temps=[73,74,75,71,69,72,76,73]", output: "[1,1,4,2,1,1,0,0]" },
    hints: ["Use a monotone decreasing stack of indices.", "When current temp > temp at stack top, we found the 'next warmer' day.", "Store indices (not values) so you can compute the day gap."],
    clarify: { question: "What would you clarify?", options: ["Return number of days or the index?","What if no future warmer temperature exists?","Are temperatures positive integers?","Can there be equal temperatures?","Guaranteed at least one temperature?"], mustAsk: [0,1,3], explanation: "Return day count (gap). No warmer future: answer[i]=0. Temperatures are positive. Equal temperatures don't count as 'warmer'. At least one temperature." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "This is a 'next greater element' problem. What are you computing for each index?", why: { question: "What is the 'next greater element' here?", options: ["Global maximum temperature","For each day i: the first day j>i where temps[j]>temps[i]. Answer is j-i (0 if none exists).","Average temperature","The sum of future temperatures"], correct: 1, explanation: "Next Greater Element: for each i, find smallest j>i with nums[j]>nums[i]. Here: answer[i]=j-i. Classic pattern solved optimally with monotone stack." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you check every future day for each current day? What's the complexity?", why: { question: "Why O(n²) for brute force?", options: ["n days × up to n future days per check = O(n²) total comparisons","O(n log n) from sorting","Single pass works","O(n) with two passes"], correct: 0, explanation: "For each i: scan j=i+1 to n-1 until temps[j]>temps[i]. Worst case (decreasing temps): n-1 + n-2 + ... + 1 = O(n²) comparisons." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a monotone decreasing stack efficiently find the next greater element?", why: { question: "Why does a monotone decreasing stack work for 'next greater'?", options: ["Stacks are always efficient","Stack maintains candidates waiting for their 'next greater'. When new element arrives and is larger than top: top found its next greater. Pop and record answer.","Sorted order needed","Hash maps are faster here"], correct: 1, explanation: "Monotone decreasing stack: each element waits on stack until a warmer day arrives. When temps[i]>temps[stack.top]: top's next warmer is day i. Pop, compute gap." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: push indices, pop when warmer found. Store indices not values — why?", why: { question: "Why store indices in the stack (not temperatures)?", options: ["Indices are smaller","Need the DAY number to compute answer[stackTop] = i - stackTop. Temperature value is retrieved as temps[stackTop].","Convention only","Indices are sorted"], correct: 1, explanation: "answer[j] = i - j where j is the index of the day and i is the current day. Storing index j allows both: lookup temp (temps[j]) and compute gap (i - j)." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Monotone stack O(n) vs brute force O(n²). When is brute force acceptable?", why: { question: "For what array sizes does brute force become impractical?", options: ["Always impractical","n>10^4: O(n²)=10^8 ops, approaching timeout at ~1s. n>10^5: O(n²)=10^10, definitely timeout. Monotone stack O(n) handles n=10^5 easily.","Always acceptable","Only impractical for n>10^6"], correct: 1, explanation: "Modern CPU: ~10^8-10^9 simple ops/sec. n=10^4 brute: 10^8 ops — borderline. n=10^5: 10^10 — too slow. Monotone stack: 10^5 ops — instant." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Why is the monotone stack O(n) even though it has a nested-looking structure?", why: { question: "Why O(n) total and not O(n²) for the pop operations?", options: ["Each pop is O(1)","Each element pushed exactly once and popped at most once. Total pushes + pops ≤ 2n. Amortized O(1) per element.","Pushes are rare","The while loop is O(log n)"], correct: 1, explanation: "Amortized analysis: n elements, each enters stack once and leaves once. 2n total operations regardless of how many pops happen per iteration. O(n) total." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What happens to indices still in the stack when the loop ends?", why: { question: "Remaining stack indices after processing all days — what is their answer?", options: ["Error — should be empty","answer[i]=0 for all remaining. No future warmer day found. Initialize answer array to 0 to handle this automatically.","Must pop and set 0","Continue looking forward"], correct: 1, explanation: "Initialize answer=[0]*n. Remaining stack indices never had a warmer day found — they stay 0 (default). No need to explicitly set after loop." } },
    ],
    dryrun: { input: "temps=[73,74,75,71]", steps: [{ label: "i=0(73): push 0", state: "stack=[0]" }, { label: "i=1(74>73): pop 0, answer[0]=1", state: "push 1, stack=[1]" }, { label: "i=2(75>74): pop 1, answer[1]=1", state: "push 2, stack=[2]" }, { label: "i=3(71<75): push 3", state: "stack=[2,3]" }, { label: "End: stack=[2,3] → answer[2]=0, answer[3]=0", state: "" }], question: "temps=[73,74,75,71]. After processing all 4 days, stack=[2,3], answer=[1,1,0,0]. What remains in the stack and what are their answers?", options: ["stack=[2,3], answer[2]=0, answer[3]=0","answer=[1,1,0,0] — correct","Stack cleared automatically","answer=[1,1,4,0]"], correct: 1, explanation: "Final answer: [1,1,0,0]. answer[0]=1, answer[1]=1, set during loop. answer[2]=0, answer[3]=0 (defaults). Stack=[2,3] — no warmer days found for them. Correct." },
    pushback: { question: "What if you need the previous greater element instead of the next?", answer: "Process array left-to-right. Maintain monotone stack. For each element i: pop elements smaller than nums[i] — those found their previous greater. O(n)." },
    followup: { question: "What if temperatures can repeat and you need next STRICTLY warmer — not next greater or equal?", answer: "Same algorithm — the condition is already strict: temps[i] > temps[stack.top], not >=. Equal temps do not trigger a pop. No change needed." },
    solution: { approach: "Monotone Decreasing Stack", steps: ["answer=[0]*n, stack=[]", "For i in 0..n-1: while stack and temps[i]>temps[stack.top]: j=stack.pop(); answer[j]=i-j", "stack.push(i)", "Return answer"], time: "O(n)", space: "O(n)" },
    keywords: {
      understand: ["next greater","warmer","gap","day","j-i","first","future","temperature"],
      brute: ["O(n²)","every","future","scan","nested","compare","decreasing"],
      pattern: ["monotone","decreasing","stack","candidates","waiting","pop","next greater"],
      algorithm: ["indices","temps[j]","i-j","gap","push","pop","amortized"],
      tradeoff: ["O(n)","O(n²)","10^4","10^5","timeout","stack","brute force"],
      complexity: ["O(n)","amortized","2n","push once","pop once","total"],
      edge: ["remaining","zero default","initialize","no warmer","last days"],
    },
  },
  {
    id: "sq4", title: "Implement Queue using Stacks", difficulty: "Easy", pattern: "Stack / Queue",
    companies: ["Amazon","Meta","Google"], minLevel: 1,
    description: "Implement a queue using only two stacks. Queue supports push (to back), pop (from front), peek (front element), and empty operations.",
    example: { input: "push(1), push(2), peek()→1, pop()→1, empty()→false", output: "FIFO order using two LIFO stacks" },
    hints: ["Use two stacks: inbox and outbox.", "Pour inbox into outbox (reversing order) to expose the front.", "Only pour when outbox is empty."],
    clarify: { question: "What would you clarify?", options: ["Operations must be O(1) amortized?","Should push be O(1) and pop be amortized O(1)?","Can we use more than 2 stacks?","Guaranteed valid calls (no pop on empty)?","Order of elements in push?"], mustAsk: [0,1,3], explanation: "Pop amortized O(1). Push O(1). Only 2 stacks. Pop/peek guaranteed valid. Standard FIFO — first pushed = first popped." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why is building a queue from stacks non-trivial? What fundamental ordering conflict exists?", why: { question: "What ordering conflict makes this non-trivial?", options: ["No conflict — trivial","Stack is LIFO, queue is FIFO. First element pushed needs to be first popped (FIFO) but would be on bottom of stack (LIFO). Need reversal.","Queues are always harder","Stacks and queues are equivalent"], correct: 1, explanation: "LIFO vs FIFO: stack pops newest first, queue pops oldest first. To expose the oldest element from a stack, you must reverse the order — that's what the second stack achieves." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you use a single stack and scan from bottom? Why is it O(n) per pop?", why: { question: "Why is single-stack approach O(n) per pop?", options: ["Pop is always O(1)","Single stack: to pop front, move all to temp, pop bottom, move back. O(n) per pop. Two-stack amortizes this.","O(n) per push","O(log n) per pop"], correct: 1, explanation: "Single stack: pop requires reversing all n elements to access front. Two stacks: pour once, then pop many times from outbox. Amortizes the reversal cost." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does the 'lazy transfer' trick amortize the reversal cost?", why: { question: "When do you transfer from inbox to outbox?", options: ["On every push","ONLY when outbox is empty and pop/peek is called. This amortizes: each element transferred at most once total across all operations.","Every n operations","After n/2 pushes"], correct: 1, explanation: "Lazy transfer: don't pour until outbox is empty. Elements sit in inbox until needed. Each element poured exactly once from inbox to outbox. Amortized O(1) across many operations." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through push, pop, peek, empty implementations.", why: { question: "Why can you keep popping from outbox without pouring again?", options: ["Bug — must repour","Outbox already reversed: oldest element is now on top. Keep popping until outbox empty. Only pour again when outbox exhausted.","Repour on every pop","Pour after every 2 pops"], correct: 1, explanation: "Once poured, outbox has elements in FIFO order (oldest on top). Pop multiple times without re-pouring. Only pour when outbox empty: lazy amortized approach." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Amortized O(1) vs worst-case O(n) for pop. When does worst-case matter?", why: { question: "When does the O(n) worst-case pop occur?", options: ["Every pop","When outbox is empty and must pour entire inbox. First pop after n consecutive pushes: O(n). Then next n-1 pops: O(1) each.","Never — always O(1)","When stack overflows"], correct: 1, explanation: "Pour occurs when outbox empty: O(n) for that specific pop. But subsequent pops O(1) until outbox empties again. Real-time systems needing guaranteed O(1) per op would need a different approach." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Amortized analysis: why O(1) per operation amortized?", why: { question: "How do you prove amortized O(1)?", options: ["It's not O(1)","Each element: pushed once to inbox (O(1)), popped from inbox once (O(1)), pushed to outbox once (O(1)), popped from outbox once (O(1)). 4 ops per element = O(1) amortized.","O(log n) amortized","O(n/2) amortized"], correct: 1, explanation: "Accounting method: each element costs 4 operations total (2 inbox + 2 outbox). Total cost for n elements = 4n. Per operation amortized = 4n/n = O(1)." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "What if pop is called when both stacks are empty?", options: ["Return -1","Guaranteed not called (problem constraint). But defensively: check empty() before pop. Return undefined/throw.","Stack underflow auto-raises","Return the last element popped"], correct: 1, explanation: "Problem guarantees valid calls. In production: check empty() before pop. The amortized analysis assumes n pushes before pops — empty() check handles actual empty state." } },
    ],
    dryrun: { input: "push(1), push(2), push(3)", steps: [{ label: "push(1),(2),(3)", state: "inbox=[1,2,3], outbox=[]" }, { label: "pop(): outbox empty → pour", state: "inbox=[], outbox=[3,2,1]" }, { label: "pop() outbox top=1", state: "return 1. outbox=[3,2]" }, { label: "peek()", state: "outbox top=2 → return 2" }], question: "push(1), push(2), push(3). inbox=[1,2,3], outbox=[]. pop(): outbox empty, pour inbox→outbox=[3,2,1]. pop outbox top=1. inbox=[]. outbox=[3,2]. peek()=?", options: ["2","3","1","4"], correct: 0, explanation: "outbox=[3,2] after pop(). Peek=outbox.top=2. FIFO: 1 was first pushed, then 2, then 3. After popping 1, next is 2. peek()=2." },
    pushback: { question: "What if you need both push and pop to be worst-case O(1)?", answer: "Use a doubly linked list instead. head = front (for dequeue), tail = back (for enqueue). Both O(1) worst case. Stacks can't achieve this." },
    followup: { question: "How would you implement a stack using two queues?", answer: "Push: enqueue to q1. Pop: move all but last from q1 to q2, dequeue last from q1. Swap q1/q2. O(n) per pop but O(1) push." },
    solution: { approach: "Two Stacks (Inbox/Outbox)", steps: ["inbox=[], outbox=[]", "push(val): inbox.append(val)", "pop(): if outbox empty: pour inbox→outbox (reverse); return outbox.pop()", "peek(): if outbox empty: pour; return outbox[-1]", "empty(): return not inbox and not outbox"], time: "O(1) amortized", space: "O(n)" },
    keywords: {
      understand: ["LIFO","FIFO","queue","stack","ordering","reversal","oldest","first"],
      brute: ["single stack","O(n)","bottom","reverse","scan","transfer"],
      pattern: ["lazy","transfer","amortized","pour","outbox","inbox","only when empty"],
      algorithm: ["push","pop","peek","empty","pour","outbox empty","reverse"],
      tradeoff: ["amortized","worst case","O(n)","pour","real-time","guaranteed"],
      complexity: ["amortized","O(1)","4 ops","accounting","total","n elements"],
      edge: ["empty","both empty","valid calls","guaranteed","underflow"],
    },
  },
  {
    id: "sq5", title: "Largest Rectangle in Histogram", difficulty: "Hard", pattern: "Stack / Queue",
    companies: ["Amazon","Google","Meta"], minLevel: 5,
    description: "Given an array of non-negative integers representing histogram bar heights, find the largest rectangle that can be formed.",
    example: { input: "heights=[2,1,5,6,2,3]", output: "10 (bars at index 2,3 with height 5 → 5×2=10)" },
    hints: ["Use a monotone increasing stack of indices.", "When a bar is shorter than the stack top, pop and calculate the rectangle for the popped bar.", "Append height=0 at the end to flush remaining bars."],
    clarify: { question: "What would you clarify?", options: ["Can heights be 0?","Return the area or the indices?","Are heights non-negative integers?","Width of each bar is 1?","What if all heights are 0?"], mustAsk: [0,2,3], explanation: "Heights ≥ 0. Return area (integer). Non-negative integers. Width=1 per bar. All zeros → area=0." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "For each bar, what is the largest rectangle it can be the HEIGHT of? What constrains the width?", why: { question: "What limits the width of a rectangle that has height h[i]?", options: ["Always the full array","Extends left until a bar shorter than h[i], and right until a bar shorter than h[i]. Width = right_limit - left_limit - 1.","Width is always 1","Width is always 2"], correct: 1, explanation: "A rectangle of height h[i] extends as far left and right as consecutive bars ≥ h[i]. The first shorter bar in each direction is the boundary." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you check every pair of bars as left-right boundaries? What's the complexity?", why: { question: "Why O(n²) for brute force?", options: ["O(n) is possible naively","For each pair (l,r): min height in [l,r] × width = area. n² pairs × O(n) min = O(n³). Or n² pairs with running min = O(n²).","O(n log n)","O(n) with prefix sums"], correct: 1, explanation: "n² bar pairs. For each pair: compute min height in range × width. With running min: O(n²). Stack approach reduces to O(n)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a monotone increasing stack identify, for each bar, the first shorter bar to its left and right?", why: { question: "What does the monotone increasing stack represent at each position?", options: ["All bars seen so far","Bars on stack are in increasing height order. Stack top = tallest current candidate. When a shorter bar arrives, top can't extend further right — calculate its max area.","Random ordering","The minimum height bars"], correct: 1, explanation: "Monotone increasing stack: each entry is a potential left boundary for future rectangles. When current bar < stack top: top's right boundary found. Pop and calculate." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: for each bar shorter than stack top, pop and compute area. How do you find width?", why: { question: "After popping index j, how do you compute the width of the rectangle?", options: ["Width = i - j","Width = i - stack.top - 1 (after pop). Left boundary = new stack top. Right boundary = current bar i (which triggered the pop). Width = i - new_top - 1.","Width = j - stack.top","Width = i"], correct: 1, explanation: "Pop j. Left bound = stack.top after pop (or -1 if empty). Right bound = i (current position, exclusive). Width = i - (stack.top after pop) - 1. Area = h[j] × width." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Monotone stack O(n) vs divide-and-conquer O(n log n). When does divide-and-conquer arise?", why: { question: "What's the divide-and-conquer approach?", options: ["Sort by height","Split at minimum height bar. Left sub-problem + right sub-problem + rectangle spanning full width at min height. O(n log n) average, O(n²) worst.","No other approach exists","Recursion on pairs"], correct: 1, explanation: "Divide-and-conquer: find min in range, recurse left and right. O(n log n) with balanced splits, O(n²) worst (sorted heights). Stack approach is O(n) always." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity? Why O(n) despite the while loop?", why: { question: "Why O(n) total for all pop operations?", options: ["Each iteration is O(n)","Each bar pushed once and popped at most once. Total pushes + pops ≤ 2n. Amortized O(1) per bar. O(n) total.","O(n log n)","O(n) per pop"], correct: 1, explanation: "Amortized: n bars, each pushed once and popped once. 2n stack operations total across entire algorithm. O(1) per bar amortized → O(n) total." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "Why append height=0 at the end? What does the sentinel do?", why: { question: "What happens without the sentinel 0 at the end?", options: ["Works fine","Bars remaining in stack at the end never get processed. Adding heights=0 forces all bars to pop and compute area. Cleaner than separate loop for remaining stack elements.","Error on empty stack","Stack auto-empties"], correct: 1, explanation: "Sentinel 0: shorter than everything, triggers popping of all remaining stack entries. Alternatively: after main loop, pop remaining stack entries explicitly. Sentinel is more elegant." } },
    ],
    dryrun: { input: "heights=[2,1,5,6,2,3,0]", steps: [{ label: "stack=[-1], i=0(h=2): push 0", state: "stack=[-1,0]" }, { label: "i=1(h=1<2): pop 0, width=1-(-1)-1=1, area=2", state: "push 1. stack=[-1,1]" }, { label: "i=2(h=5): push. i=3(h=6): push", state: "stack=[-1,1,2,3]" }, { label: "i=4(h=2<6): pop 3, width=4-2-1=1, area=6", state: "2<5: pop 2, width=4-1-1=2, area=10" }, { label: "push 4. i=5(h=3): push", state: "stack=[-1,1,4,5]" }, { label: "i=6(h=0): pop all, area=3,8,6", state: "max=10" }], question: "heights=[2,1,5,6,2,3,0] (with sentinel). At i=4(h=2): we pop bar at index 3 (h=6). width=4-2-1=1, area=6. Then pop bar at index 2 (h=5). width=4-1-1=2, area=10. What is the maximum area found so far?", options: ["10","12","6","8"], correct: 0, explanation: "Areas computed so far: 2 (bar 0), 6 (bar 3), 10 (bar 2). Max = 10. Final answer after processing all bars is also 10." },
    pushback: { question: "What if bars can have fractional heights?", answer: "Same algorithm — comparisons work for any ordered type. Float comparisons might have precision issues; use epsilon comparison for equality checks. O(n) unchanged." },
    followup: { question: "What is the 2D version — largest rectangle of 1s in a binary matrix?", answer: "For each row, treat row's column heights as histogram (increment if 1, reset to 0 if 0). Apply histogram algorithm for each row. O(m×n) total." },
    solution: { approach: "Monotone Increasing Stack", steps: ["Append 0 to heights (sentinel)", "stack=[-1]", "For each i: while heights[i]<heights[stack.top]: pop j; width=i-stack.top-1; area=heights[j]×width; update max", "Push i", "Return max area"], time: "O(n)", space: "O(n)" },
    keywords: {
      understand: ["rectangle","height","width","left boundary","right boundary","shorter","extends","area"],
      brute: ["O(n²)","pairs","min","running min","every","pair","width"],
      pattern: ["monotone increasing","left boundary","right boundary","shorter","pop","calculate","extends"],
      algorithm: ["pop","width","i","stack.top","after pop","sentinel","area","height"],
      tradeoff: ["O(n)","divide-and-conquer","O(n log n)","O(n²)","worst","stack"],
      complexity: ["O(n)","amortized","2n","push once","pop once","total"],
      edge: ["sentinel 0","remaining","flush","all zeros","single bar"],
    },
  },
  {
    id: "sq6", title: "Sliding Window Maximum", difficulty: "Hard", pattern: "Stack / Queue",
    companies: ["Meta","Amazon","Google"], minLevel: 5,
    description: "Given an array nums and integer k, return an array of the maximum of each sliding window of size k as it moves from left to right.",
    example: { input: "nums=[1,3,-1,-3,5,3,6,7], k=3", output: "[3,3,5,5,6,7]" },
    hints: ["Use a monotone decreasing deque (double-ended queue) of indices.", "Remove indices outside the current window from the front.", "Remove indices with smaller values from the back before adding new index."],
    clarify: { question: "What would you clarify?", options: ["Is k guaranteed ≤ n?","Can nums contain negative numbers?","Should output length be n-k+1?","Is k at least 1?","Are there duplicate values?"], mustAsk: [0,2,3], explanation: "k ≤ n guaranteed. Negatives allowed. Output length = n-k+1. k ≥ 1. Duplicates allowed — handle carefully with strict > vs ≥ comparison." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What makes finding the maximum of each window challenging when windows slide?", why: { question: "Why is naively recomputing max for each window O(n×k)?", options: ["It's O(n) because sliding shares work","New window = old window minus leftmost + new rightmost. But finding max of new window requires scanning k elements if previous max leaves. O(n×k) worst case.","O(n log n) from sorting","O(k) amortized"], correct: 1, explanation: "When the previous max exits the window, the next max requires scanning all k remaining elements. Worst case (descending array): every max leaves → O(n×k). Deque gives O(n)." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "Walk through the O(n×k) brute force. For which input is it worst case?", why: { question: "Which input makes brute force O(n×k)?", options: ["Random array","Descending sorted array: [k, k-1, ..., 2, 1, 0]. Each window's max is always the leftmost, which exits on every slide. Recompute max from scratch k times each.","Ascending sorted array","All same elements"], correct: 1, explanation: "Descending [k,k-1,...,1]: each window's max = leftmost element = exits immediately. Must recompute max from scratch each step. O(n) windows × O(k) each = O(n×k)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a monotone decreasing deque maintain the window maximum in O(1) per element?", why: { question: "Why remove smaller elements from the deque back before adding a new index?", options: ["To save space","A smaller element at an earlier index can NEVER be the maximum of any future window (the new larger element dominates and will also outlast it in the window). Remove it — it's useless.","For deque ordering","Indices only"], correct: 1, explanation: "Dominance: if nums[j] ≤ nums[i] and j < i, then nums[j] will never be the window max (nums[i] is larger AND later). Safe to evict. This keeps deque in strictly decreasing order." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: remove out-of-window from front, remove dominated from back, append index, record max when window full.", why: { question: "When do you start recording maximum values?", options: ["From i=0","When i >= k-1: the first complete window is formed. deque.front() holds the max index. Record nums[deque.front()].","When deque is full","After processing all elements"], correct: 1, explanation: "First complete window when i=k-1. From that point on, record nums[dq[0]] (deque front = max of current window) in output. Output length = n-k+1." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Deque O(n) vs max-heap O(n log k). When would you use a heap?", why: { question: "When might a max-heap be preferable over a deque?", options: ["Never — deque always better","If you need more than just the max (e.g., top-3 elements per window), a heap holds all candidates. Deque only gives max. Heap: O(n log k), deque: O(n).","Heap is always faster","Heap uses less memory"], correct: 1, explanation: "Deque gives ONLY the maximum in O(n). If you need k-th maximum per window or multiple top elements: use a heap O(n log k) since deque evicts smaller elements permanently." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(n) time despite the while loop in the algorithm?", options: ["While loop is O(k) per iteration","Each element added to deque at most once and removed at most once. Total deque operations ≤ 2n. Amortized O(1) per element → O(n) total.","O(n log n) from deque","O(n × k) always"], correct: 1, explanation: "Amortized: each index enters deque once (back push) and exits once (front pop OR back eviction). 2n total deque ops across all n iterations. O(n) overall." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "k=1 — what's the output?", options: ["Error","Every element is its own window of size 1. Output = nums itself. Deque holds one element at a time.","Empty output","Output has n-1 elements"], correct: 1, explanation: "k=1: window size 1. Output = [max of each 1-element window] = nums. Deque: each element pushed, output recorded, next element evicts (since it's newer). Correct." } },
    ],
    dryrun: { input: "nums=[1,3,-1,-3,5,3], k=3", steps: [{ label: "i=0(val=1): dq=[0]", state: "" }, { label: "i=1(val=3>1): remove 0 from back (1<3), dq=[1]", state: "" }, { label: "i=2(val=-1): dq=[1,2]. i>=k-1=2 → record nums[1]=3", state: "output=[3]" }, { label: "i=3(val=-3): dq=[1,2,3]. i=3, front=1: 3-1=2<k=3 → keep. record nums[1]=3", state: "output=[3,3]" }, { label: "i=4(val=5>-3,-1,3): evict 3,2,1 from back. dq=[4]. record nums[4]=5", state: "output=[3,3,5]" }, { label: "i=5(val=3): dq=[4,5]. record nums[4]=5", state: "output=[3,3,5,5]" }], question: "nums=[1,3,-1,-3,5,3,6,7], k=3. At i=2, the window is [1,3,-1]. The deque front holds index 1 (value 3). What do we record as output[0]?", options: ["3 — nums[deque.front()]=nums[1]=3","1 — first element","−1 — last element","The index 1"], correct: 0, explanation: "Deque front = index 1, nums[1]=3 = maximum of window [1,3,-1]. Record 3. output=[3,...]." },
    pushback: { question: "What if you need the MINIMUM of each sliding window instead?", answer: "Use a monotone INCREASING deque instead. Remove from back when nums[new] < nums[back] (keep increasing). Front gives minimum. Same O(n) time." },
    followup: { question: "How would you find the sum (not max) of each sliding window of size k?", answer: "No deque needed. Maintain running sum: add incoming element, subtract outgoing element. O(n) time, O(1) extra space. Much simpler than max problem." },
    solution: { approach: "Monotone Decreasing Deque", steps: ["dq = deque(), output = []", "For i in 0..n-1:", "  While dq and nums[i] >= nums[dq.back()]: pop back", "  Append i to back", "  If dq.front() <= i-k: pop front (out of window)", "  If i >= k-1: output.append(nums[dq.front()])"], time: "O(n)", space: "O(k)" },
    keywords: {
      understand: ["sliding window","maximum","window size","recompute","O(n×k)","exit","max leaves"],
      brute: ["descending","leftmost","exits","recompute","O(n×k)","worst case"],
      pattern: ["deque","decreasing","dominance","smaller earlier","evict","useless","future"],
      algorithm: ["front","back","out of window","i-k","k-1","complete window","record"],
      tradeoff: ["heap","top-k","O(n log k)","deque","O(n)","max only","multiple"],
      complexity: ["O(n)","amortized","2n","once in","once out","deque ops"],
      edge: ["k=1","output=nums","one element","window","single","correct"],
    },
  },
  {
    id: "sq7", title: "Decode String", difficulty: "Medium", pattern: "Stack / Queue",
    companies: ["Google","Amazon"], minLevel: 3,
    description: "Given an encoded string like '3[a2[bc]]', return its decoded form. The encoding rule: k[encoded_string] means encoded_string repeated k times. k is a positive integer.",
    example: { input: 's="3[a2[bc]]"', output: '"abcbcabcbcabcbc"' },
    hints: ["Use a stack to handle nested brackets.", "When you see '[': push current string and current count onto stack.", "When you see ']': pop count and previous string, repeat current string count times, append to previous."],
    clarify: { question: "What would you clarify?", options: ["Can brackets be nested multiple levels?","Is k always a single digit or can it be multi-digit?","Will input always be valid?","Can the encoded string before brackets be empty?","Is output always shorter than memory limit?"], mustAsk: [0,1,2], explanation: "Arbitrary nesting depth. k can be multi-digit (e.g., '12[a]'). Input guaranteed valid. Empty prefix like '3[2[a]]' is valid. Output fits in memory." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What makes nesting hard? How deep can brackets go?", why: { question: "Why does nesting require a stack (vs simple scan)?", options: ["It doesn't require a stack","Nested brackets create context that must be remembered: '2[3[a]]' means first resolve inner '3[a]'='aaa', then outer '2[aaa]'='aaaaaa'. Stack saves outer context while processing inner.","Recursion is the only way","Nesting depth is always 1"], correct: 1, explanation: "Each '[' suspends current string-building and starts a new inner context. Each ']' resolves inner context and resumes outer. Stack perfectly models this suspend/resume pattern." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you repeatedly scan and replace innermost brackets until done?", why: { question: "Why is repeated inner-scan O(n²) or worse?", options: ["It's O(n)","Each pass finds and expands one innermost bracket: O(n) scan + O(n×k) string expansion. For deeply nested input: O(depth × n × k) total. Stack processes each char once.","O(n log n)","O(n × k) total"], correct: 1, explanation: "Brute: find innermost '...k[str]...', replace, repeat. Each replacement O(n). Up to O(n) replacements for deeply nested strings. O(n²) scans minimum. Stack: one pass O(n × output_size)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "What do you push onto the stack when you see '['? What do you pop when you see ']'?", why: { question: "What goes on the stack at each '['?", options: ["The current character","Push (currentString, currentCount): save the string built so far and the repetition count. Start fresh inside the bracket.","Only the count","Only the string"], correct: 0, explanation: "At '[': push (currentStr, k) to stack. Reset currentStr='', k=0. Now build the inner string from scratch. At ']': pop (prevStr, repeatCount), new currentStr = prevStr + currentStr×repeatCount." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through character by character: digit accumulates k, '[' saves state, letters build string, ']' resolves.", why: { question: "How do you handle multi-digit numbers like k=12?", options: ["Only single digits supported","Accumulate: k = k * 10 + int(char) while processing digit characters before '['. Naturally handles multi-digit k.","Parse the whole number first","Use regex"], correct: 1, explanation: "Multi-digit: k=k*10+digit for each digit character. When '['arrives: push k (fully accumulated) and reset k=0. '12[a]' → k accumulates as 1→12 before '['. Correct." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Stack vs recursion for this problem. What are the trade-offs?", why: { question: "How would recursion solve this problem?", options: ["Recursion can't solve it","Recursive: parse character by character, call recursively when '[' found, return decoded string and end position when ']' found. More natural but uses call stack = O(depth) space.","Recursion uses O(1) space","Recursion is always better for string problems"], correct: 1, explanation: "Recursive approach: each '[' triggers a recursive call that returns when ']' found. Clean implementation. Call stack depth = nesting depth = O(depth). Explicit stack also O(depth). Equivalent space." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(n × maxK) time where maxK is the maximum repetition factor?", options: ["O(n) only","O(n × maxK): the output string can be exponentially large. Each string expansion: O(len × k). Total time proportional to output size, which can be O(n × maxK^depth).","O(n²)","O(n log n)"], correct: 1, explanation: "Output size drives complexity. '2[2[2[a]]]' = 'aaaaaaaa' (8 chars from 9-char input). String operations cost proportional to output length. Can be O(n × k^depth) in worst case." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "Input with no brackets like s='abc' — what's the output?", options: ["Error — must have brackets","'abc' unchanged. No '[' or ']' encountered, no pushes/pops, currentStr accumulates 'abc', final return is 'abc'.","Empty string","'abcabc'"], correct: 1, explanation: "No brackets: characters accumulate in currentStr = 'abc'. Stack never used. Return currentStr = 'abc'. No encoding present means no change needed." } },
    ],
    dryrun: { input: 's="2[ab3[c]]"', steps: [{ label: "k=2, see '['", state: "push('', 2). currentStr='', k=0" }, { label: "Add 'a','b'", state: "currentStr='ab'" }, { label: "k=3, see '['", state: "push('ab', 3). currentStr='', k=0" }, { label: "Add 'c'", state: "currentStr='c'" }, { label: "See ']': pop('ab', 3)", state: "currentStr='ab'+'c'×3='abccc'" }, { label: "See ']': pop('', 2)", state: "currentStr=''+'abccc'×2='abcccabccc'" }], question: "s='3[a2[bc]]'. After processing '3[a', we see '[' with k=2, currentStr='a'. What do we push to the stack?", options: ["('a', 2) — the current string 'a' and count 2, then reset currentStr='' and k=0","('a', 3) — wrong count","(2, 'a') — reversed order is fine","Just push 2"], correct: 0, explanation: "At '[': push (currentStr, k) = ('a', 2). Reset currentStr='' and k=0. Now build inner string starting from empty. Eventually inner resolves to 'bcbc', then outer: 'a'+'bcbc'×2='abcbcabcbc'. Wait — then outer '3' was set earlier... the full decode is '3[a2[bc]]'→'abcbcabcbcabcbc'." },
    pushback: { question: "What if the encoded string can contain letters with no preceding bracket (like 'x3[a]y')?", answer: "No change needed. Letters outside brackets simply accumulate in currentStr normally. 'x3[a]y' → 'x'+'aaa'+'y' = 'xaaay'. Algorithm handles this correctly already." },
    followup: { question: "How would you encode a string into the shortest possible run-length encoded form?", answer: "Find longest repeating substrings: for each position, find repeated units (e.g., 'aaaa'→'4[a]'). Optimal encoding is NP-hard for arbitrary nesting. Greedy: encode consecutive repetitions." },
    solution: { approach: "Stack-Based Parsing", steps: ["stack=[], currentStr='', k=0", "For char in s:", "  digit: k = k*10 + int(char)", "  '[': push(currentStr, k); currentStr=''; k=0", "  ']': prevStr,count=stack.pop(); currentStr=prevStr+currentStr×count", "  letter: currentStr += char", "Return currentStr"], time: "O(n × maxK)", space: "O(n)" },
    keywords: {
      understand: ["nesting","context","suspend","resume","stack","inner","outer","brackets"],
      brute: ["innermost","replace","repeated scan","O(n²)","pass","expansion"],
      pattern: ["push (str, k)","reset","pop","prevStr × count","bracket","inner context"],
      algorithm: ["k*10+digit","multi-digit","'[' push","']' pop","accumulate","currentStr"],
      tradeoff: ["recursion","call stack","depth","O(depth)","explicit stack","equivalent"],
      complexity: ["O(n × maxK)","output size","expansion","k^depth","string operations"],
      edge: ["no brackets","abc","unchanged","accumulate","no push","currentStr"],
    },
  },
  {
    id: "sq8", title: "Basic Calculator II", difficulty: "Medium", pattern: "Stack / Queue",
    companies: ["Meta","Amazon"], minLevel: 3,
    description: "Implement a basic calculator to evaluate a string expression containing non-negative integers and operators +, -, *, /. Integer division truncates toward zero. No parentheses.",
    example: { input: 's="3+2*2"', output: "7" },
    hints: ["Process * and / immediately (higher precedence) using a stack.", "Push operands with sign for + and - (defer until end).", "Sum the stack at the end for the final result."],
    clarify: { question: "What would you clarify?", options: ["Are parentheses included?","Can there be spaces in the string?","Is division always integer truncation toward zero?","Can the result be negative?","Are operands guaranteed to be non-negative?"], mustAsk: [0,1,2], explanation: "No parentheses in this version. Spaces may appear — skip them. Integer division truncates toward zero. Result can be negative (e.g., '1-2'=-1). Operands non-negative." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why can't you evaluate left-to-right? What does operator precedence require?", why: { question: "Why does '3+2*2' ≠ 10 if evaluated left-to-right?", options: ["It does equal 10","* has higher precedence than +. Must compute 2*2=4 first, then 3+4=7. Left-to-right gives wrong order.","Strings can't be evaluated","Precedence only matters for division"], correct: 1, explanation: "PEMDAS/BODMAS: * and / before + and -. Left-to-right evaluation ignores precedence. Stack lets us defer low-precedence operations and handle high-precedence immediately." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would two-pass evaluation work (first * and /, then + and -)?", why: { question: "What does the two-pass approach do?", options: ["Evaluate all operators left-to-right","Pass 1: scan and apply all * and / in-order. Pass 2: sum remaining values with + and -. O(n) two passes. Stack approach does this in one pass.","Use regex","Sort operators by precedence"], correct: 1, explanation: "Two-pass: scan and collapse all * and / (replace pairs with their result). Then scan and sum remaining values with signs. O(n) × 2 passes. Stack achieves same in one pass." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Stack strategy: push values for + and -, pop-compute-push for * and /. Why?", why: { question: "Why push value directly for '+' and '-'?", options: ["All operators work the same","'+' and '-' have lower precedence — we don't know if the value will be multiplied/divided later. Push with sign to defer until all * and / resolved.","Push the operator instead","Never push anything"], correct: 1, explanation: "'+' → push +value. '-' → push -value. Deferred until end because a higher-precedence * might come next. '*' and '/' → pop, compute, push result immediately (higher precedence, resolve now)." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: accumulate multi-digit numbers, track last operator, process on each operator or at end.", why: { question: "When do you process an operator and push to stack?", options: ["Immediately when seen","When you encounter an operator OR reach end of string: process the PREVIOUS operator with the accumulated number. Not the current operator — you need to read the next number first.","At the end only","Never — only sum at end"], correct: 1, explanation: "Process previous operator with current number when you see the NEXT operator (or reach end). Pattern: see op → apply prevOp to num → push/compute → update prevOp to current op → reset num." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Stack O(n) space vs no-stack O(1) space solution. How does the no-stack variant work?", why: { question: "How can you evaluate this in O(1) space?", options: ["Can't — need a stack","Track running total and last multiplication term. On +/-: commit last term to total, start new term. On */: apply to last term in-place. No stack needed. O(1) space.","Need O(n) always","O(log n) space"], correct: 1, explanation: "O(1) approach: total=0, lastTerm=0. '+': total+=lastTerm; lastTerm=+num. '-': total+=lastTerm; lastTerm=-num. '*': lastTerm*=num. '/': lastTerm//=num. Final: total+lastTerm." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(n) time and O(n) space for stack approach?", options: ["O(n log n) time","O(n) time: one character at a time, O(1) per char. O(n) space: stack can hold up to n/2 values (alternating +/- with multi-digit numbers).","O(n²) time","O(1) space for stack"], correct: 1, explanation: "Single pass: O(n) time. Stack size: up to n/2 entries (one per operand for + and - operations). O(n) space. No-stack variant: O(1) space, same O(n) time." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "Expression with spaces like '3 + 2' — how do you handle?", options: ["Fail — invalid input","Skip space characters when iterating. Skip whitespace, accumulate digits, process operators normally.","Tokenize first","Use split()"], correct: 1, explanation: "Spaces: skip with 'if char == \" \": continue'. Single-pass scanning naturally handles spaces between tokens. '3 + 2' → correctly evaluates to 5." } },
    ],
    dryrun: { input: 's="3+2*2"', steps: [{ label: "char='3': num=3", state: "" }, { label: "char='+': prevOp='+', push +3 to stack", state: "stack=[3], num=0, prevOp='+'" }, { label: "char='2': num=2", state: "" }, { label: "char='*': prevOp was '+' → push +2, now prevOp='*'", state: "stack=[3,2], num=0" }, { label: "char='2': num=2", state: "" }, { label: "End: prevOp='*' → pop 2, compute 2*2=4, push 4", state: "stack=[3,4]" }, { label: "Sum stack: 3+4=7", state: "return 7" }], question: 's="3+2*2". When we reach the end of the string with num=2 and prevOp=\'*\', what do we do?', options: ["Pop stack top (2), compute 2*2=4, push 4. Then sum stack: 3+4=7.","Push 2 and return stack sum=3+2+2=7","Multiply all stack values","Return the last number 2"], correct: 0, explanation: "At end: prevOp='*'. Pop 2, compute 2×2=4, push 4. Stack=[3,4]. Sum=7. This correctly handles operator precedence — * was deferred until its right operand was read." },
    pushback: { question: "How would you handle parentheses in the expression (Basic Calculator I)?", answer: "Use a stack to save (currentResult, sign) when '(' is seen. Compute inner expression. On ')': result = inner + sign × currentResult from stack. O(n) time." },
    followup: { question: "How would you evaluate an expression with all four operators AND parentheses?", answer: "Combine both: use stack to save state on '(', restore on ')'. Handle precedence with the same +/- push and */ immediate-compute pattern. O(n) time and space." },
    solution: { approach: "Stack with Precedence Handling", steps: ["stack=[], num=0, prevOp='+'", "For each char (+ end sentinel):", "  digit: num = num*10 + int(char)", "  operator or end: if prevOp in +-: push ±num; elif prevOp=='*': push stack.pop()*num; elif prevOp=='/': push int(stack.pop()/num)", "  prevOp=char; num=0", "Return sum(stack)"], time: "O(n)", space: "O(n)" },
    keywords: {
      understand: ["precedence","left-to-right","* before +","PEMDAS","defer","higher"],
      brute: ["two-pass","scan","collapse","*and/","then +and-","O(n)×2"],
      pattern: ["push with sign","defer","pop compute push","immediate","low precedence","high precedence"],
      algorithm: ["prevOp","num accumulate","end sentinel","sum stack","process on next op","multi-digit"],
      tradeoff: ["O(1) space","lastTerm","total","no stack","running","commit"],
      complexity: ["O(n)","one pass","stack size n/2","operands","alternating","linear"],
      edge: ["spaces","skip","whitespace","multi-digit","single pass","normal"],
    },
  },
  {
    id: "sq9", title: "Evaluate Reverse Polish Notation", difficulty: "Medium", pattern: "Stack / Queue",
    companies: ["Amazon","Meta"], minLevel: 3,
    description: "Evaluate the value of an arithmetic expression in Reverse Polish Notation (postfix). Valid operators are +, -, *, /. Integer division truncates toward zero.",
    example: { input: 'tokens=["2","1","+","3","*"]', output: "9  // ((2+1)*3)=9" },
    hints: ["Use a stack: push numbers, pop two operands when you see an operator.", "Order matters for - and /: second pop is left operand, first pop is right operand.", "Result is the single remaining value on the stack."],
    clarify: { question: "What would you clarify?", options: ["Division truncates toward zero or floors?","Can intermediate results overflow?","Are operands guaranteed to be integers?","Can the expression be invalid?","Are there unary operators?"], mustAsk: [0,2,3], explanation: "Truncates toward zero (Java/Python3 behavior). Intermediate results fit in 32-bit integer. All operands integers. Expression guaranteed valid. No unary operators." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What is RPN and why does it need no parentheses or precedence rules?", why: { question: "Why does RPN eliminate the need for parentheses?", options: ["It still needs them","Operands come before operators. No ambiguity about grouping — operator always applies to the two most recently seen unresolved operands. Order of operations encoded in token order.","Brackets are implicit","RPN uses a tree structure"], correct: 1, explanation: "RPN: operands before operators. '3+4*2' in RPN is '3 4 2 * +'. No ambiguity: * applies to 4 and 2 (most recent), then + applies to 3 and 8. Stack naturally evaluates this." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you convert RPN to infix and then evaluate? Why is the stack approach better?", why: { question: "Why is the stack approach directly superior?", options: ["Conversion is simpler","Converting to infix then evaluating: two O(n) passes, added complexity. Stack: single pass, direct evaluation, O(n) time O(n) space. No conversion needed.","Conversion is O(1)","Both are equivalent"], correct: 1, explanation: "Direct stack evaluation: single pass, no intermediate representation. Converting to infix then evaluating: redundant work. Stack is the canonical O(n) single-pass solution." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Stack invariant: after processing each token, stack contains all unresolved operands in order.", why: { question: "What invariant does the stack maintain throughout evaluation?", options: ["Stack is sorted","Stack holds all operands that have been seen but not yet consumed by an operator. At the end, one value remains = the result.","Stack holds operators","Stack grows monotonically"], correct: 1, explanation: "Invariant: at any point, stack = sequence of values awaiting operators. Operand → push. Operator → pop two, compute, push result. Final: stack has one element = answer." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: for each token, push if number, else pop two and apply operator.", why: { question: "For '-' and '/', which pop is the left operand?", options: ["First pop is left","Second pop is left operand. stack.pop() first = right, stack.pop() second = left. Order: left OP right. '5 3 -' → pop 3 (right), pop 5 (left), compute 5-3=2.","Doesn't matter for * and +","Left is always the bottom"], correct: 1, explanation: "LIFO: last pushed = first popped. '5 3 -': push 5, push 3. Pop → 3 (right), pop → 5 (left). 5-3=2. CRITICAL for - and /: wrong order gives wrong result." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Stack-based O(n) evaluation vs recursive/tree-based O(n). When would a tree be preferable?", why: { question: "When is building an expression tree preferable over stack evaluation?", options: ["Never — stack always better","When you need to evaluate the SAME expression multiple times with different variable substitutions. Build tree once O(n), evaluate O(n) each time with O(1) variable lookup.","Tree uses less memory","Tree evaluates faster"], correct: 1, explanation: "Expression tree: build once, evaluate many times with variable substitutions. Stack approach requires re-parsing each time. Single evaluation: stack wins (simpler). Multiple evaluations: tree wins." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(n) time and O(n) space?", options: ["O(n log n) due to stack operations","O(n) time: one pass, O(1) per token. O(n) space: stack can hold up to (n+1)/2 numbers in worst case (all numbers then operators).","O(n²) time","O(1) space"], correct: 1, explanation: "n tokens, O(1) per token: O(n) time. Worst case: n/2 operands, then n/2 operators. Stack grows to n/2. O(n) space. Final stack has exactly 1 element (valid RPN)." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "Single token ['42'] — what's the result?", options: ["Error — need operator","42 — push 42, end of tokens. Stack has [42]. Return stack.pop()=42.","0","Need at least 2 tokens"], correct: 1, explanation: "Single number: push to stack, no operators encountered. Return stack.pop()=42. Valid RPN with one token = the number itself." } },
    ],
    dryrun: { input: 'tokens=["2","1","+","3","*"]', steps: [{ label: "'2': push 2", state: "stack=[2]" }, { label: "'1': push 1", state: "stack=[2,1]" }, { label: "'+': pop 1 (right), pop 2 (left), compute 2+1=3, push 3", state: "stack=[3]" }, { label: "'3': push 3", state: "stack=[3,3]" }, { label: "'*': pop 3 (right), pop 3 (left), compute 3*3=9, push 9", state: "stack=[9]" }, { label: "End: return stack.pop()=9", state: "" }], question: 'tokens=["2","1","+","3","*"]. After processing "+", the stack is [3]. We then push 3. When we process "*", what do we pop first?', options: ["3 (the second 3, most recently pushed — this is the right operand)","2 (the original 2)","1 (the original 1)","3 (the result of 2+1, which is the left operand)"], correct: 0, explanation: "Stack after pushing second '3': [3, 3]. Pop → 3 (right operand, the literal '3'). Pop → 3 (left operand, result of 2+1). Compute 3*3=9. Push 9. Return 9." },
    pushback: { question: "What if division should floor toward negative infinity instead of truncating toward zero?", answer: "In Python, use // operator (floors). In other languages: if (a/b < 0 and a%b != 0) result -= 1. One-line fix: int(a/b) truncates, a//b floors. Choose based on spec." },
    followup: { question: "How would you convert an infix expression to RPN (Shunting-Yard algorithm)?", answer: "Shunting-Yard: scan tokens. Operands → output. Operators → pop stack to output while stack top has higher/equal precedence, then push. '(' → push. ')' → pop to output until '('. O(n)." },
    solution: { approach: "Stack-Based Postfix Evaluation", steps: ["stack = []", "For each token:", "  if number: stack.push(int(token))", "  if operator: b=stack.pop(); a=stack.pop(); stack.push(apply(a, op, b))", "Return stack.pop()"], time: "O(n)", space: "O(n)" },
    keywords: {
      understand: ["RPN","postfix","no parentheses","order","operands before","unambiguous"],
      brute: ["convert","infix","two passes","redundant","single pass","direct"],
      pattern: ["invariant","unresolved","operands","one value","push","pop two"],
      algorithm: ["second pop left","first pop right","order","b=pop","a=pop","a OP b"],
      tradeoff: ["expression tree","multiple evaluations","variables","single use","stack better","tree reuse"],
      complexity: ["O(n)","one pass","O(1) per token","n/2 numbers","stack size","linear"],
      edge: ["single token","['42']","no operator","push return","valid RPN","42"],
    },
  },
];
