export const STACK_QUEUE_PROBLEMS = [
  {
    id: "sq1", title: "Valid Parentheses", difficulty: "Easy", pattern: "Stack / Queue",
    companies: ["Meta","Amazon","Google"], minLevel: 1,
    description: "Given a string of brackets '(', ')', '{', '}', '[', ']', determine if the input string is valid. Every open bracket must be closed by the same type in correct order.",
    example: { input: 's="()[]{}"', output: "true" },
    hints: ["Use a stack: push on open bracket, pop and match on close bracket.", "If stack has leftovers at end, string is invalid.", "On close bracket: stack must be non-empty and top must match."],
    clarify: { question: "What would you clarify?", options: ["Only bracket characters or other characters too?","Empty string — valid or invalid?","Is case sensitivity relevant?","Can string have whitespace?","What about nested brackets?"], mustAsk: [0,1], explanation: "Only bracket characters. Empty string is valid (vacuously). Case not relevant — no letter brackets. No whitespace. Nested brackets must be properly ordered and closed." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What makes this problem 'last in, first out'? Why is a stack the natural choice?", why: { question: "Why does this problem have LIFO structure?", options: ["Random order","The most recently opened bracket must be closed first. Nesting forces LIFO: ( [ { } ] ) — innermost closed before outermost.","Queues work better"], correct: 1, explanation: "LIFO: in '([{', the last opened '{' must be closed first. Stack naturally models this — push on open, peek/pop on close, last pushed = expected close." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you repeatedly replace matched pairs until empty or no matches? What's the cost?", why: { question: "Why is replacement approach O(n²)?", options: ["It's O(n)","Scan for innermost pair, remove (O(n)), repeat. Worst case n/2 passes × O(n) each = O(n²).","It's correct but O(n log n)"], correct: 1, explanation: "Repeatedly scan and remove matched pairs: '()' → '', '{}' → '', etc. Each scan O(n), up to n/2 replacements. O(n²). Stack approach is O(n)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does the stack encode the 'expected close bracket' at every point in the string?", why: { question: "What does the stack contain at any point?", options: ["All open brackets seen","Stack top = most recently opened bracket = the NEXT expected close bracket. Stack encodes all pending 'IOUs' in order.","Closed brackets"], correct: 1, explanation: "Every unmatched open bracket is an 'IOU'. Stack holds them in order, top = most recent IOU = next expected close. When close arrives: pop and verify match." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through the algorithm for each character type.", why: { question: "When do you return false immediately (not wait for end)?", options: ["Never — always check at end","When close bracket arrives but: (a) stack is empty (no matching open) or (b) top doesn't match the close. Fail immediately.","Only at end"], correct: 1, explanation: "Fail fast: on ')' with empty stack → false. On ')' when top is '[' → false. No point continuing — the string is already invalid." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Stack approach O(n) space — can you do O(1) space? What's the catch?", why: { question: "Why can't you do O(1) space for general valid parentheses?", options: ["You can — use counters","Multiple bracket types: ')' requires knowing type of open bracket. Counter for '(' can't distinguish between '(' and '['. O(1) only works for single bracket type.","Always O(n) space required"], correct: 1, explanation: "Single type (e.g. only ()): O(1) counter. Multi-type: need to know WHICH bracket was opened. Stack holds bracket identity. No way to retrieve that in O(1) space." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space of stack solution?", why: { question: "Why O(n) space worst case?", options: ["Always O(1)","If all characters are open brackets '((((': push all n → stack size n. O(n) space worst case.","O(log n)"], correct: 1, explanation: "String of all open brackets: push all n → O(n) stack. Average case for balanced strings: O(n/2). O(n) time (single pass), O(n) space worst." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "List all edge cases.", why: { question: "String ']' — what happens?", options: ["Returns true","Close bracket with empty stack → return false immediately","Stack error"], correct: 1, explanation: "']' arrives, stack empty: no matching open. Return false. Also: '([)]' — passes naive count but stack catches mismatch: '(' then '[' then ')' doesn't match '['. Return false." } },
    ],
    dryrun: { question: 's="({[]})". Process: push (, push {, push [, then ]: pop [ matches ], push nothing. Then }: pop { matches }. Then ): pop ( matches ). Stack empty at end?', options: ["Yes — return true","No — one item left","Error on ]"], correct: 0, explanation: "Stack operations: push (, push {, push [. ']' arrives: pop [, matches. '}' arrives: pop {, matches. ')' arrives: pop (, matches. Stack empty → return true." },
    pushback: { challenge: "What if you also need to return the index of the first invalid bracket?", options: ["Track current index alongside stack operations. Return current index when mismatch or stack underflow detected.","Can't do this with a stack","Need two passes"], correct: 0, explanation: "Track index: when ')' arrives and stack empty or mismatch, return current_index. At end, if stack non-empty, return index of first unmatched open (store indices in stack alongside brackets)." },
    followup: { question: "What is the minimum number of bracket reversals to make a string valid?", options: ["Count unmatched opens (a) and closes (b) after stack processing. Answer: ceil(a/2) + ceil(b/2). O(n).","Always n/2 reversals","Need DP"], correct: 0, explanation: "After processing with stack: remaining unmatched opens = a, unmatched closes = b. Each reversal fixes one open (→ close) or one close (→ open), handling two imbalances. Total: ceil(a/2) + ceil(b/2)." },
    solution: { approach: "Stack with Bracket Matching", steps: ["stack = [], pairs = {')':'(', '}':'{', ']':'['}","For each char: if open bracket: push","If close: if stack empty or stack.top != pairs[char]: return false; pop","Return stack is empty"], time: "O(n)", space: "O(n)" },
  },
  {
    id: "sq2", title: "Min Stack", difficulty: "Medium", pattern: "Stack / Queue",
    companies: ["Amazon","Meta","Google"], minLevel: 3,
    description: "Design a stack that supports push, pop, top, and getMin — all in O(1) time. getMin returns the minimum element in the current stack.",
    example: { input: 'push(-2), push(0), push(-3), getMin()→-3, pop(), getMin()→-2', output: "All operations O(1)" },
    hints: ["Track minimum alongside each element, or use a second auxiliary stack.", "When pushing: also push the current minimum (including new element) to min stack.", "When popping: pop from both stacks."],
    clarify: { question: "What would you clarify?", options: ["Must getMin be O(1) — not O(n)?","What if pop or top called on empty stack?","Can elements be negative?","Must it handle duplicates correctly?","Is there a space limit?"], mustAsk: [0,1,3], explanation: "O(1) required for all operations — key constraint. Empty stack behavior: undefined/guaranteed not called per problem. Negatives allowed. Duplicates: getMin after popping a duplicate min must return previous min." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why is getMin O(n) with a regular stack? What information do you need to precompute?", why: { question: "Why can't a regular stack do getMin in O(1)?", options: ["It can — just check top","Must scan all elements to find minimum — O(n). No way to jump to minimum without tracking it.","Sort the stack"], correct: 1, explanation: "Regular stack: elements added in arbitrary order. To find min, scan all: O(n). Need to track minimum incrementally as elements are pushed/popped." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you track min with a separate sorted structure? Why is it overkill?", why: { question: "Why is a sorted set overkill for tracking min?", options: ["It's not — sorted set is efficient","Sorted set: O(log n) insert/delete. But we only need the minimum — O(log n) is worse than needed. A second stack achieves O(1) for min.","Can't use sorted set with stack"], correct: 1, explanation: "Only need min, not full sorted order. Sorted set wastes work. Min stack (second stack tracking running minimums) is O(1) insert/delete/query — simpler and faster." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does an auxiliary 'min stack' track the minimum at every point in history?", why: { question: "What does the min stack contain at any point?", options: ["All minimums ever seen","Current minimum at each depth. Top of min stack = current minimum. When main stack pops, min stack syncs to reveal previous minimum.","Just the global minimum"], correct: 1, explanation: "Min stack mirrors main stack depth. Each push to main stack: push min(newVal, minStack.top) to min stack. Pop syncs. At any depth, minStack.top = minimum of elements up to that depth." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through push, pop, top, and getMin implementations.", why: { question: "Why push to min stack on EVERY push, not just when new element is smaller?", options: ["Only push when new minimum","Must maintain same depth as main stack. If pop() always pops both, they must always be in sync. Selective push breaks sync.","Doesn't matter"], correct: 1, explanation: "Min stack must mirror main stack depth so pop() can be coordinated. Always push min(new, currentMin) keeps them in sync and O(1) per operation." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Auxiliary min stack (2x space) vs pair encoding (same space) — describe both.", why: { question: "What's the pair encoding approach?", options: ["Store value and index","Store (value, currentMin) as pair in single stack. Same O(1) operations, avoids second stack object but same space asymptotically.","Can't do with single stack"], correct: 1, explanation: "Pair approach: push (val, currentMin) as tuple. getMin: return stack.top[1]. Same O(1) all ops, same O(n) space, no second stack object. Either is accepted." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity for all operations?", why: { question: "Why O(n) space for min stack approach?", options: ["O(1) — we only store minimums","Both main and min stacks grow proportionally to pushes. n pushes → n entries in each stack → O(n) total.","O(n²)"], correct: 1, explanation: "Worst case: n pushes, each grows both stacks. 2n total entries = O(n) space. Each operation O(1). Constant-size overhead beyond the elements themselves." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases involve duplicates?", why: { question: "push(3), push(3), pop(), getMin() — what is the min after pop?", options: ["Undefined","3 — the duplicate 3 remains. Min stack correctly returns 3 as min for remaining stack.","Error"], correct: 1, explanation: "push(3): minStack→[3]. push(3): minStack→[3,3] (min stays 3). pop(): both stacks pop, minStack top=3. getMin()=3. Duplicates work correctly because min stack always syncs." } },
    ],
    dryrun: { question: "push(-2): main=[-2], min=[-2]. push(0): main=[-2,0], min=[-2,-2]. push(-3): main=[-2,0,-3], min=[-2,-2,-3]. getMin()=?. pop(). getMin()=?", options: ["getMin()=-3, then after pop, getMin()=-2","getMin()=0, then -2","getMin()=-3, then 0"], correct: 0, explanation: "Top of min stack = -3. getMin()=-3. pop() removes both stacks' tops: main=[-2,0], min=[-2,-2]. New min top=-2. getMin()=-2. Correct." },
    pushback: { challenge: "What if you need getMax as well as getMin?", options: ["Add a second auxiliary max stack alongside min stack. Same approach: push max(val, maxStack.top). O(1) all ops. O(n) space per stack.","Sorted structure needed","Can't have both O(1)"], correct: 0, explanation: "MaxStack: same pattern. Push max(newVal, maxStack.top). getMax: return maxStack.top. Pop: sync maxStack. Three stacks total: main, min, max. All O(1), O(n) each." },
    followup: { question: "Design a queue using two stacks (enqueue/dequeue in amortized O(1))?", options: ["Two stacks: inbox and outbox. Enqueue: push to inbox. Dequeue: if outbox empty, pour inbox→outbox (reverse). Then pop from outbox. Amortized O(1).","Queues can't be built from stacks efficiently","Need circular buffer"], correct: 0, explanation: "Queue via two stacks: each element moves at most twice (inbox→outbox). n dequeues after n enqueues: n total pours = O(n) for n ops = O(1) amortized. Classic interview pattern." },
    solution: { approach: "Auxiliary Min Stack", steps: ["Two stacks: main and minStack","push(val): main.push(val); minStack.push(min(val, minStack.top or val))","pop(): main.pop(); minStack.pop()","top(): return main.top()","getMin(): return minStack.top()"], time: "O(1) all ops", space: "O(n)" },
  },
  {
    id: "sq3", title: "Daily Temperatures", difficulty: "Medium", pattern: "Stack / Queue",
    companies: ["Amazon","Google","Meta"], minLevel: 3,
    description: "Given daily temperatures, return an array where answer[i] is the number of days until a warmer temperature. If no warmer day exists, answer[i] = 0.",
    example: { input: "temps=[73,74,75,71,69,72,76,73]", output: "[1,1,4,2,1,1,0,0]" },
    hints: ["Use a monotone decreasing stack of indices.", "When current temp > temp at stack top, we found the 'next warmer' day.", "Store indices (not values) so you can compute the day gap."],
    clarify: { question: "What would you clarify?", options: ["Return number of days or the index?","What if no future warmer temperature exists?","Are temperatures positive integers?","Can there be equal temperatures?","Guaranteed at least one temperature?"], mustAsk: [0,1,3], explanation: "Return day count (gap). No warmer future: answer[i]=0. Temperatures are positive. Equal temperatures don't count as 'warmer'. At least one temperature." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "This is a 'next greater element' problem. What are you computing for each index?", why: { question: "What is the 'next greater element' here?", options: ["Global maximum temperature","For each day i: the first day j>i where temps[j]>temps[i]. Answer is j-i (0 if none exists).","Average temperature"], correct: 1, explanation: "Next Greater Element: for each i, find smallest j>i with nums[j]>nums[i]. Here: answer[i]=j-i. Classic pattern solved optimally with monotone stack." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you check every future day for each current day? What's the complexity?", why: { question: "Why O(n²) for brute force?", options: ["n days × up to n future days per check = O(n²) total comparisons","O(n log n) from sorting","Single pass works"], correct: 0, explanation: "For each i: scan j=i+1 to n-1 until temps[j]>temps[i]. Worst case (decreasing temps): n-1 + n-2 + ... + 1 = O(n²) comparisons." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a monotone decreasing stack efficiently find the next greater element?", why: { question: "Why does a monotone decreasing stack work for 'next greater'?", options: ["Stacks are always efficient","Stack maintains candidates waiting for their 'next greater'. When new element arrives and is larger than top: top found its next greater. Pop and record answer.","Sorted order needed"], correct: 1, explanation: "Monotone decreasing stack: each element waits on stack until a warmer day arrives. When temps[i]>temps[stack.top]: the top element's next warmer is day i. Pop, compute gap. Stack never has an element that found its warmer." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: push indices, pop when warmer found. Store indices not values — why?", why: { question: "Why store indices in the stack (not temperatures)?", options: ["Indices are smaller","Need the DAY number to compute answer[stackTop] = i - stackTop. Temperature value is retrieved as temps[stackTop].","Convention only"], correct: 1, explanation: "answer[j] = i - j where j is the index of the day and i is the current day. Storing index j allows both: lookup temp (temps[j]) and compute gap (i - j)." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Monotone stack O(n) vs brute force O(n²). When is brute force acceptable?", why: { question: "For what array sizes does brute force become impractical?", options: ["Always impractical","n>10^4: O(n²)=10^8 ops, approaching timeout at ~1s. n>10^5: O(n²)=10^10, definitely timeout. Monotone stack O(n) handles n=10^5 easily.","Always acceptable"], correct: 1, explanation: "Modern CPU: ~10^8-10^9 simple ops/sec. n=10^4 brute: 10^8 ops — borderline. n=10^5: 10^10 — too slow. Monotone stack: 10^5 ops — instant. Always use stack for production." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Why is the monotone stack O(n) even though it has a nested-looking structure?", why: { question: "Why O(n) total and not O(n²) for the pop operations?", options: ["Each pop is O(1)","Each element pushed exactly once and popped at most once. Total pushes + pops ≤ 2n. Amortized O(1) per element.","Pushes are rare"], correct: 1, explanation: "Amortized analysis: n elements, each enters stack once and leaves once. 2n total operations regardless of how many pops happen per iteration. O(n) total." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What happens to indices still in the stack when the loop ends?", why: { question: "Remaining stack indices after processing all days — what is their answer?", options: ["Error — should be empty","answer[i]=0 for all remaining. No future warmer day found. Initialize answer array to 0 to handle this automatically.","Must pop and set 0"], correct: 1, explanation: "Initialize answer=[0]*n. Remaining stack indices never had a warmer day found — they stay 0 (default). No need to explicitly set after loop." } },
    ],
    dryrun: { question: "temps=[73,74,75,71]. stack=[]. i=0(73): push 0. i=1(74): 74>73, pop 0, answer[0]=1-0=1, push 1. i=2(75): 75>74, pop 1, answer[1]=2-1=1, push 2. i=3(71): 71<75, push 3. Loop ends. What remains?", options: ["stack=[2,3], answer[2]=0, answer[3]=0","answer=[1,1,0,0] — correct","Stack cleared automatically"], correct: 1, explanation: "Final answer: [1,1,0,0]. answer[0]=1, answer[1]=1, set during loop. answer[2]=0, answer[3]=0 (defaults). Stack=[2,3] — no warmer days found for them. Correct." },
    pushback: { challenge: "What if you need the previous greater element instead of the next?", options: ["Process array right-to-left with same monotone stack. Or: process left-to-right but pop when current > top (prev greater found for current). O(n).","Completely different algorithm","Reverse the array then apply same algorithm"], correct: 0, explanation: "Previous greater element: process left-to-right. Maintain increasing stack. For each i: while stack.top < temps[i]: this element's previous greater isn't stack.top — actually: when popping stack for next greater, remaining top = previous greater. Or reverse array for same logic." },
    followup: { question: "What if temperatures can repeat and you need next STRICTLY warmer — not next greater or equal?", options: ["Same algorithm — the condition is already strict: temps[i] > temps[stack.top], not >=. Equal temps do not trigger a pop.","Need to handle equal specially","Add equals as special case"], correct: 0, explanation: "The > condition already handles this. Equal temperatures don't trigger pops — they get pushed onto stack, waiting for a strictly warmer day. No change needed to the algorithm." },
    solution: { approach: "Monotone Decreasing Stack", steps: ["answer=[0]*n, stack=[]","For i in 0..n-1: while stack and temps[i]>temps[stack.top]: j=stack.pop(); answer[j]=i-j","stack.push(i)","Return answer"], time: "O(n)", space: "O(n)" },
  },
];
