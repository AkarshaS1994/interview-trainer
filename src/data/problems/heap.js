export const HEAP_PROBLEMS = [
  {
    id: "h1", title: "Kth Largest Element in Array", difficulty: "Medium", pattern: "Heap",
    companies: ["Meta","Amazon","Google"], minLevel: 3,
    description: "Given integer array and k, return the kth largest element. It is the kth largest in sorted order, not necessarily distinct.",
    example: { input: "nums=[3,2,1,5,6,4], k=2", output: "5" },
    hints: ["Min-heap of size k: smallest element in heap = kth largest seen so far.", "When heap exceeds size k, pop the minimum.", "Final heap top = answer."],
    clarify: { question: "What would you clarify?", options: ["Kth largest in sorted order or kth distinct?","Can k be larger than array length?","Can there be duplicates?","Memory constraints?","Return value or index?"], mustAsk: [0,1,2], explanation: "Sorted order not distinct (so [3,3,3] k=2 returns 3). k ≤ n guaranteed. Duplicates fine. Return the value." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Clarify: kth largest in sorted (with duplicates) order. What's the kth largest of [3,2,3,1,2,4,5,5,6] k=4?", why: { question: "Answer: [6,5,5,4,3,3,2,2,1] sorted desc. 4th = ?", options: ["4","3","5"], correct: 0, explanation: "Sorted descending: 6,5,5,4,3,3,2,2,1. 4th = 4." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would sorting solve this? What's the complexity?", why: { question: "Why is sorting O(n log n) overkill?", options: ["It isn't overkill — simplest approach","We don't need full sort — only need the kth element","Sorting uses extra space"], correct: 1, explanation: "Full sort to get one element is wasteful. Partial selection algorithms (QuickSelect, heap) do better — O(n) avg or O(n log k)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a min-heap of size k give you the kth largest? Walk through the invariant.", why: { question: "Why a MIN-heap for kth LARGEST?", options: ["Min-heap accidentally works","Min-heap of size k: always contains k largest seen. Heap's minimum = smallest of the k largest = kth largest overall.","Should use max-heap"], correct: 1, explanation: "Min-heap of size k: when heap full and new element > heap.top(), pop min (not in top-k) and push new. heap.top() = kth largest at all times." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through the min-heap algorithm step by step.", why: { question: "When do you add to the heap vs ignore an element?", options: ["Always add","Add if heap size < k or nums[i] > heap.top(). If heap grows beyond k, pop min.","Only add if nums[i] > k"], correct: 1, explanation: "Heap size < k: always add. Heap full: if nums[i] > heap.top(), pop min, push nums[i]. If nums[i] ≤ heap.top(): skip (it can't be in top-k)." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Min-heap O(n log k) O(k) vs QuickSelect O(n) avg O(n²) worst O(1) space. When does each win?", why: { question: "When is QuickSelect preferable despite O(n²) worst case?", options: ["Never — heap is always safer","Large n, small k, need best average time and O(1) space. Heap: always O(n log k) regardless.","QuickSelect is always faster"], correct: 1, explanation: "QuickSelect: O(n) average, O(1) extra space — beats heap for large n. But O(n²) worst case (random pivot on sorted input). Randomized QuickSelect reduces this risk." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space for heap approach? Why O(n log k) not O(n log n)?", why: { question: "Why O(log k) per operation not O(log n)?", options: ["Same thing","Heap size is bounded by k — each insert/delete is O(log k), not O(log n)","Heap operations are O(1)"], correct: 1, explanation: "Heap never exceeds size k. O(log k) per insertion. n insertions = O(n log k). Since k ≤ n, O(n log k) ≤ O(n log n). Much better when k << n." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "k=1: what's the answer?", options: ["Minimum element","Maximum element","Undefined"], correct: 1, explanation: "k=1: 1st largest = maximum. Heap approach returns this correctly: heap of size 1 ends up containing the max." } },
    ],
    dryrun: { question: "nums=[3,2,1,5,6,4], k=2. After processing all elements with min-heap of size 2, what is in the heap?", options: ["[5,6]","[4,5]","[6]"], correct: 0, explanation: "Process: push 3,2→heap=[2,3]. Push 1: 1<heap.top=2, skip. Push 5: 5>2, pop 2, push 5→[3,5]. Push 6: 6>3, pop 3, push 6→[5,6]. Push 4: 4<heap.top=5, skip. heap=[5,6], top=5." },
    pushback: { challenge: "What if the array is a stream and you need the kth largest after each new element?", options: ["Maintain min-heap of size k. Add() method: push to heap, if heap.size()>k pop min. top() returns kth largest. O(log k) per element.","Must rebuild heap for each element","Can't do efficiently with streams"], correct: 0, explanation: "KthLargest class: constructor builds heap from initial array. add(val): push to heap, if size>k pop min. Return heap.top(). O(log k) per add — classic streaming application." },
    followup: { question: "What if you need the top-k most frequent elements (not just kth largest)?", options: ["Count frequencies O(n), then use min-heap of size k on (count,element) pairs. O(n log k).","Sort by frequency O(n log n)","Bucket sort by frequency O(n)"], correct: 0, explanation: "Top-K Frequent: HashMap for frequencies O(n), then min-heap of size k keyed by frequency O(n log k). Or bucket sort by frequency O(n) if you want linear." },
    solution: { approach: "Min-Heap of Size k", steps: ["Init min-heap","For each num: push to heap","If heap.size() > k: pop min","Return heap.top()"], time: "O(n log k)", space: "O(k)" },
    keywords: {
      understand: ["kth largest","sorted order","duplicates","value","min-heap","size k"],
      brute: ["sort","O(n log n)","overkill","full sort","partial","wasteful"],
      pattern: ["min-heap","k largest","invariant","top","kth","smallest of k"],
      algorithm: ["push","pop min","heap.top","size > k","O(log k)","skip"],
      tradeoff: ["QuickSelect","O(n)","O(1)","O(n²)","worst case","space","average"],
      complexity: ["O(n log k)","time","O(k)","space","bounded","size k","log k"],
      edge: ["k=1","maximum","min","streaming","kth"],
    },
  },
  {
    id: "h2", title: "Merge K Sorted Lists", difficulty: "Hard", pattern: "Heap",
    companies: ["Meta","Amazon","Google"], minLevel: 5,
    description: "Merge k sorted linked lists and return one sorted list.",
    example: { input: "lists=[[1,4,5],[1,3,4],[2,6]]", output: "[1,1,2,3,4,4,5,6]" },
    hints: ["Min-heap stores the current head of each list.", "Pop min, add to result, push next from same list.", "Heap size is always at most k."],
    clarify: { question: "What would you clarify?", options: ["Can lists be empty?","Can k be 0?","Are lists already sorted?","Return new list or modify in-place?","Any constraints on total nodes n?"], mustAsk: [0,1,2], explanation: "Empty lists: skip them. k=0: return null. Lists are sorted (given). Return new merged list." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What makes this harder than merging 2 sorted lists? Why does naive k-way comparison cost O(k) per step?", why: { question: "Why O(kn) for naive approach?", options: ["For each of n total nodes, compare k list heads to find minimum — O(k) per node × n nodes","We sort k times","Lists are unsorted"], correct: 0, explanation: "n total nodes. At each step, compare current heads of up to k lists to find minimum. O(k) per node = O(kn) total." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would collecting all values and sorting work? Why is this suboptimal?", why: { question: "Why is collect-all-sort O(n log n) considered brute force here?", options: ["It isn't brute force","Ignores that lists are already sorted — extra log n factor is wasted work","n log n is always optimal for sorting"], correct: 1, explanation: "Lists are pre-sorted. Merging sorted sequences optimally = O(n log k), not O(n log n). Using the sorted property saves a factor of log(n/k)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a min-heap always give you the current global minimum across all k lists?", why: { question: "Why does heap size stay at k (not grow to n)?", options: ["We limit it artificially","We only push when we pop — one out, one in. Heap always has one head per non-exhausted list.","Heap grows to n initially"], correct: 1, explanation: "Init: push k heads. Each step: pop min (add to result), push next node from same list. Size stays ≤ k at all times." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through the heap merge algorithm step by step.", why: { question: "What do you store in the heap beyond just the node value?", options: ["Just the value","(value, list_index) or (value, node) — need to know which list to advance after popping","Just the node pointer"], correct: 1, explanation: "Store (node.val, list_index, node) or just (node.val, node). Need the node to find its next pointer. Tie-breaking on list_index prevents comparison errors." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Min-heap O(n log k) vs divide-and-conquer merge O(n log k) — same complexity. When is D&C preferable?", why: { question: "What's the practical advantage of divide-and-conquer merge?", options: ["D&C is always better","D&C naturally parallelizes — merge pairs independently on separate threads. Heap is inherently sequential.","D&C uses less space"], correct: 1, explanation: "Parallel systems: D&C splits k lists into pairs, each pair merged independently. Map-reduce friendly. Heap: sequential by design." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity? What does n represent?", why: { question: "Why O(n log k) not O(n log n)?", options: ["Same thing","Heap size is k — O(log k) per push/pop. n total nodes = O(n log k). Since k ≤ n, this is ≤ O(n log n).","Heap operations are O(1)"], correct: 1, explanation: "n = total nodes across all lists. Each pushed/popped once = O(log k) each. Total O(n log k)." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases should you handle?", why: { question: "k=1: what's the answer?", options: ["Error","Return the single list as-is — no merging needed","Build heap of size 1 — works but wasteful"], correct: 1, explanation: "k=1: return lists[0] directly. k=0: return null. Empty lists: skip when initializing heap." } },
    ],
    dryrun: { question: "lists=[[1,4,5],[1,3,4],[2,6]]. After popping min and pushing next, what is in the heap?", options: ["[(1,list1),(1,list0),(2,list2)] initially → pop 1 (list0), push 4 → [(1,list1),(2,list2),(4,list0)]","[(1,1,2)] after first pop","[(4,5),(1,3,4),(2,6)]"], correct: 0, explanation: "Init heap: (1,list0),(1,list1),(2,list2). Pop min=1(list0), add to result. Push next from list0 = 4. Heap now: (1,list1),(2,list2),(4,list0)." },
    pushback: { challenge: "What if lists are given as arrays instead of linked lists?", options: ["Same algorithm — use index pointers instead of node.next. Push (value, list_idx, element_idx). Advance element_idx after pop.","Must convert to linked lists first","Need different algorithm"], correct: 0, explanation: "Arrays: store (arr[list_idx][elem_idx], list_idx, elem_idx) in heap. After pop, push (arr[list_idx][elem_idx+1], list_idx, elem_idx+1) if in bounds. Same O(n log k)." },
    followup: { question: "What if instead of linked lists you have k sorted streams (infinite, from network/disk)?", options: ["Same heap approach — just maintain k stream pointers. Pull next element from stream when needed. Works perfectly for streaming.","Must buffer all data first","Can't merge infinite streams"], correct: 0, explanation: "Streaming merge: exact same algorithm. The heap abstraction works perfectly — pop minimum, pull next from same stream. Handles any finite or infinite sorted sequence." },
    solution: { approach: "Min-Heap of List Heads", steps: ["Push (node.val, list_idx, node) for each non-null head","While heap not empty: pop min, append to result","If node.next exists: push (next.val, list_idx, next)","Return merged list head"], time: "O(n log k)", space: "O(k)" },
    keywords: {
      understand: ["k lists","global minimum","O(k) per node","naive","heap","comparison"],
      brute: ["collect all","sort","O(n log n)","ignore sorted","wasted","merge"],
      pattern: ["min-heap","k heads","size k","pop","push next","always k"],
      algorithm: ["list_index","node","pop","push","next","tie-breaking","pair"],
      tradeoff: ["divide-and-conquer","parallel","pairs","map-reduce","sequential","heap"],
      complexity: ["O(n log k)","time","O(k)","space","bounded","log k","n nodes"],
      edge: ["k=1","k=0","empty","null","return directly","skip"],
    },
  },
  {
    id: "h3", title: "Find Median from Data Stream", difficulty: "Hard", pattern: "Heap",
    companies: ["Meta","Amazon","Google"], minLevel: 5,
    description: "Design a data structure that supports adding integers from a stream and finding the median at any time.",
    example: { input: "addNum(1), addNum(2), findMedian()→1.5, addNum(3), findMedian()→2", output: "MedianFinder class" },
    hints: ["Two heaps: max-heap for lower half, min-heap for upper half.", "Balance heaps so sizes differ by at most 1.", "Median = top of larger heap, or average of both tops."],
    clarify: { question: "What would you clarify?", options: ["Is the stream finite or infinite?","Can there be duplicates?","Integer or float values?","Must findMedian() be O(1)?","Memory constraints?"], mustAsk: [0,1,3], explanation: "Infinite stream (otherwise just sort). Duplicates fine. findMedian O(1) is the target — that's what makes this interesting." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why can't you sort after every insertion? What data structure keeps the median accessible?", why: { question: "Why is sorting after each insertion too slow?", options: ["It isn't — sort is O(n log n)","O(n log n) per insertion × n insertions = O(n² log n) total. We need O(log n) per insertion.","Sorting doesn't give median"], correct: 1, explanation: "n insertions × O(n log n) each = O(n² log n). Target: O(log n) per insert, O(1) per findMedian." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would a sorted array or BST work? What are the complexities?", why: { question: "What's the bottleneck with a sorted array approach?", options: ["Finding median is slow","Insertion into sorted array is O(n) due to shifting — O(n) per insert × n = O(n²) total","Array uses too much space"], correct: 1, explanation: "Sorted array: findMedian O(1), insertion O(n) (binary search finds pos, shifting takes O(n)). O(n²) total insertions." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Two heaps: max-heap for lower half, min-heap for upper half. What invariants do they maintain?", why: { question: "What are the two invariants that must hold after every insertion?", options: ["Both heaps same size always","1) maxHeap.top ≤ minHeap.top (order invariant). 2) |maxHeap.size - minHeap.size| ≤ 1 (balance invariant).","Min-heap always larger"], correct: 1, explanation: "Order: every element in lower half ≤ every element in upper half. Balance: sizes differ by at most 1. These guarantee O(1) median access." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through the insertion algorithm that maintains both invariants.", why: { question: "Why push to maxHeap first, then potentially move top to minHeap?", options: ["Order doesn't matter","Ensures all elements are checked against maxHeap.top for proper partitioning before balancing","MinHeap first is equivalent"], correct: 1, explanation: "Push to maxHeap first (even if element is large). Pop maxHeap.top to minHeap (this ensures order invariant). If sizes unbalanced, rebalance. Elegant and correct." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Two heaps O(log n) insert O(1) median vs sorted container O(log n) insert O(1) median. What's the difference?", why: { question: "When would a balanced BST (like Python's SortedList) be preferred?", options: ["Never","When you need more than just median — kth element, count < x, range queries. BST enables O(log n) for all these.","BST is always faster"], correct: 1, explanation: "Two heaps: optimized for median only. Balanced BST (order-statistics tree): supports any rank query in O(log n). More general but slightly more complex." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time complexity for addNum and findMedian?", why: { question: "Why O(log n) for addNum not O(1)?", options: ["Heap insertion is O(1)","Heap push/pop: O(log n) due to bubble-up/bubble-down","We sort during insertion"], correct: 1, explanation: "Heap push: O(log n). Heap pop: O(log n). At most 2 pushes + 2 pops per addNum = O(log n). findMedian: just read heap tops = O(1)." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "findMedian called with 0 elements — what happens?", options: ["Return 0","Throw error or return null — undefined behavior on empty stream","Return 0.0"], correct: 1, explanation: "Empty stream has no median. Throw exception or return null. Guard at start of findMedian: if both heaps empty → error." } },
    ],
    dryrun: { question: "After addNum(1), addNum(2), addNum(3): what are the two heaps and what does findMedian return?", options: ["maxHeap=[2,1], minHeap=[3] → median=2","maxHeap=[1], minHeap=[2,3] → median=2","maxHeap=[3,2], minHeap=[1] → median=2.5"], correct: 0, explanation: "After insertions with balancing: maxHeap (lower half) = [2,1] (max=2), minHeap (upper half) = [3] (min=3). maxHeap has one more element → median = maxHeap.top = 2." },
    pushback: { challenge: "What if you also need to support removing elements from the stream?", options: ["Lazy deletion: mark elements as deleted in a set. When popping heaps, skip deleted elements. Amortized O(log n).","Must rebuild heaps on each delete","Can't delete from heaps efficiently"], correct: 0, explanation: "Lazy deletion: keep a deleted set. On pop, if top is in deleted set, discard and pop again. Rebalance after. Amortized O(log n) per operation. Complex but works." },
    followup: { question: "What if instead of median you need the 75th percentile?", options: ["Adjust heap sizes: lower heap has 75% of elements, upper has 25%. Top of lower = 75th percentile. Same two-heap approach.","Need three heaps","Must sort all elements"], correct: 0, explanation: "Generalize: maintain lower heap with ⌊0.75n⌋ elements, upper with ⌈0.25n⌉. After each insertion, rebalance. Top of lower heap = 75th percentile. O(log n) per insert." },
    solution: { approach: "Two Heaps (Max + Min)", steps: ["maxHeap (lower half), minHeap (upper half)","addNum: push to maxHeap, move top to minHeap","Rebalance: if minHeap larger, move top to maxHeap","findMedian: if equal size → average tops; else → larger heap's top"], time: "O(log n) add, O(1) findMedian", space: "O(n)" },
    keywords: {
      understand: ["median","stream","O(log n)","O(1)","sort","insertion","design"],
      brute: ["sorted array","O(n)","shifting","insertion","O(n²)","bottleneck"],
      pattern: ["two heaps","max-heap","min-heap","order invariant","balance invariant","halves"],
      algorithm: ["push maxHeap","pop to minHeap","rebalance","size","invariants"],
      tradeoff: ["BST","range queries","kth element","O(log n)","general","more complex"],
      complexity: ["O(log n)","addNum","O(1)","findMedian","bubble-up","push pop"],
      edge: ["empty","0 elements","null","error","both heaps empty"],
    },
  },
  {
    id: "h4", title: "Last Stone Weight", difficulty: "Easy", pattern: "Heap",
    companies: ["Amazon","Google"], minLevel: 1,
    description: "Stones are smashed together: take two heaviest stones x and y (y >= x). If y > x: stone y-x remains. If y==x: both destroyed. Return weight of last remaining stone, or 0 if none.",
    example: { input: "stones=[2,7,4,1,8,1]", output: "1" },
    hints: ["Always smash the two heaviest stones — use a max-heap.", "Pop two, compute result, push back if non-zero.", "Repeat until 0 or 1 stones remain."],
    clarify: { question: "What would you clarify?", options: ["Always use the two heaviest (not any two)?","Return 0 if all stones destroyed?","Are stone weights positive?","All stones same weight possible?","Stones array can be empty?"], mustAsk: [0,1,2], explanation: "Two heaviest. Return 0 if no stone left. Weights > 0. Same weights: both destroyed. Non-empty guaranteed (1+ stones)." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why do you always smash the two heaviest stones? What would happen if you smashed lighter ones first?", why: { question: "Why heaviest two? What if you smashed two lighter stones?", options: ["Order doesn't matter","Smashing light stones: small reductions, heavy stone persists. Smashing heaviest: max potential cancellation. Greedy: maximize reduction per step.","Lightest gives more reduction"], correct: 1, explanation: "Always smash heaviest to maximize potential cancellation. Light stone vs heavy stone: small reduction (difference). Two heavies: might cancel entirely. Greedy maximum reduction strategy." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would sorting after every smash work? What's the complexity?", why: { question: "Why is re-sorting after each step O(n² log n)?", options: ["O(n log n) total","At most n smashes × O(n log n) sort each = O(n² log n). Max-heap: O(n) build + O(n log n) total pops.","O(n) total"], correct: 1, explanation: "n stones, at most n smashes. Each sort: O(n log n). Total: O(n² log n). Max-heap: build O(n), each pop+push O(log n). Total O(n log n)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a max-heap always give you the two heaviest stones in O(log n)?", why: { question: "Why max-heap instead of min-heap for this problem?", options: ["Either works","Need the LARGEST stones each step. Max-heap's top = current max. Two consecutive pops = two heaviest.","Min-heap for largest"], correct: 1, explanation: "Max-heap: always access largest O(1). Two pops: O(log n) each. Push result: O(log n). Total per smash: O(log n). n smashes = O(n log n)." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: pop y (largest), pop x (second largest), compute y-x, push if > 0.", why: { question: "When do you NOT push anything back?", options: ["Always push","When y == x: both destroyed, y-x=0. Don't push 0 — no stone remains from this smash.","When only one stone"], correct: 1, explanation: "y == x: both destroyed. y-x = 0. Push nothing. Next iteration: two fewer stones. y > x: push y-x. Continue until 0 or 1 stone." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Max-heap O(n log n) vs sorted array O(n²). Why is heap preferred for repeated max-access?", why: { question: "What is the bottleneck with a sorted array for this problem?", options: ["Finding max is slow","Removing from front/middle of sorted array: O(n) shifting. Heap: O(log n) push/pop. For n repeated operations, O(n²) vs O(n log n).","Arrays can't find max"], correct: 1, explanation: "Sorted array: pop max = O(1), but push and maintain sorted = O(n). n operations: O(n²). Heap: push/pop O(log n) each. n operations: O(n log n). Heap dramatically better." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why at most n smashes?", options: ["Infinite smashes possible","Each smash: either removes 2 stones (equal) or replaces 2 with 1. Stones decrease by at least 1 per smash. At most n-1 smashes to reach ≤ 1 stone.","n² smashes"], correct: 1, explanation: "Start: n stones. Each smash: n decreases by 1 (replace 2 with 1) or by 2 (equal stones). At most n-1 smashes. Each: O(log n). Total O(n log n). Space O(n) for heap." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "stones=[1]: single stone, answer?", options: ["0","1 — single stone, no smash needed. Return stones[0]=1.","Error"], correct: 1, explanation: "Single stone: heap.size()=1, no smash needed. Loop condition requires 2+ stones. Return heap.top() = 1." } },
    ],
    dryrun: { question: "stones=[2,7,4,1,8,1]. Max-heap=[8,7,4,2,1,1]. Smash 8,7: y=8,x=7, push 1. heap=[4,2,1,1,1]. Smash 4,2: y=4,x=2, push 2. heap=[2,1,1,1]. Smash 2,1: y=2,x=1, push 1. heap=[1,1,1]. Smash 1,1: y=x, no push. heap=[1]. Return?", options: ["0","1","2"], correct: 1, explanation: "Return heap.top()=1. Trace: 8-7=1 → heap=[4,2,1,1,1]. 4-2=2 → heap=[2,1,1,1]. 2-1=1 → heap=[1,1,1]. 1-1=0 (equal, no push) → heap=[1]. One stone remains. Answer: 1." },
    pushback: { challenge: "What if instead of heaviest two, you must smash specific stones by index?", options: ["No longer max-heap problem. Simulate directly using provided indices. O(n) if just tracking remaining.","Same max-heap","Sort by index"], correct: 0, explanation: "Specified indices: no heap needed. Simulate the smashes in order, update stone weights, skip zeros. O(n) simulation." },
    followup: { question: "What if after smashing you get the PRODUCT instead of difference?", options: ["Same max-heap structure. Pop two, push product (or 0 if equal, or handle differently). O(n log n). Algorithm identical, just change the operation.","Different algorithm","Products grow too large"], correct: 0, explanation: "Product variant: same pop two, compute product, push back. Max-heap still needed for 'heaviest'. O(n log n). Handle overflow if needed (BigInteger)." },
    solution: { approach: "Max-Heap", steps: ["Build max-heap from stones","While heap.size() >= 2: y=pop(), x=pop()","If y > x: push(y-x)","Return heap.top() if non-empty else 0"], time: "O(n log n)", space: "O(n)" },
    keywords: {
      understand: ["heaviest","smash","max","cancel","greedy","reduction","two largest"],
      brute: ["re-sort","O(n² log n)","after each","O(n log n)","max-heap","n smashes"],
      pattern: ["max-heap","largest","two pops","top","O(log n)","consecutive"],
      algorithm: ["pop y","pop x","y-x","push","equal","both destroyed","not push"],
      tradeoff: ["sorted array","O(n)","O(n²)","heap","O(log n)","O(n log n)"],
      complexity: ["O(n log n)","O(n)","space","n-1 smashes","decrease","at most"],
      edge: ["single stone","return top","1","no smash","one element"],
    },
  },
  {
    id: "h5", title: "Top K Frequent Elements", difficulty: "Medium", pattern: "Heap",
    companies: ["Meta","Amazon","Google"], minLevel: 3,
    description: "Given integer array nums and integer k, return the k most frequent elements. You may return the answer in any order.",
    example: { input: "nums=[1,1,1,2,2,3], k=2", output: "[1,2]" },
    hints: ["Count frequencies with a HashMap.", "Use min-heap of size k on (frequency, element) pairs.", "Or: bucket sort by frequency for O(n) solution."],
    clarify: { question: "What would you clarify?", options: ["Is it guaranteed k ≤ number of unique elements?","Can there be duplicate frequencies?","Return elements in any order?","What if k equals unique element count?","Can k be 0?"], mustAsk: [0,2,3], explanation: "k ≤ unique count guaranteed. Duplicate frequencies: any k elements with tied highest frequency. Any order. k=unique count: return all elements. k>=1." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What's the difference between 'kth largest element' and 'kth most frequent element'?", why: { question: "What data structure replaces sorting for frequency-based selection?", options: ["Sort by value","Count frequencies with HashMap first. Then find kth highest frequency. Similar structure to kth largest, but on frequency counts not values.","Sort frequencies directly"], correct: 1, explanation: "Two steps: (1) Count frequencies O(n). (2) Find k elements with highest frequency (same as kth largest but on freq). HashMap + min-heap or bucket sort." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would count-then-sort work? What's the complexity?", why: { question: "Count + sort by frequency: O(n log n) for sort. Why can we do better?", options: ["O(n log n) is optimal","We don't need all elements sorted — just top k. Min-heap O(n log k). Bucket sort O(n).","Sort is always needed"], correct: 1, explanation: "Sort-all: O(n log n). We only need top k from n unique elements. Min-heap: O(n log k). Bucket sort: O(n). Partial selection is faster than full sort." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a min-heap of size k on (frequency, element) give you the top-k frequent?", why: { question: "Why min-heap with size k for top-k MOST frequent?", options: ["Should be max-heap","Min-heap of size k: always keeps k most-frequent seen. Heap min = least frequent among top-k. When new freq > heap min: pop min, push new. Heap.top = kth most frequent.","Frequency doesn't matter"], correct: 1, explanation: "Same pattern as kth largest: min-heap of size k. Keyed by frequency. Heap contains k elements with highest frequencies. When new element's freq > heap.min.freq: swap." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: build freq map, then heap. Alternative: bucket sort where bucket[i] = elements with freq i.", why: { question: "How does bucket sort achieve O(n) for this problem?", options: ["Bucket sort is O(n log n)","Frequencies bounded by n (max freq = n). Create n buckets. Bucket[freq] = list of elements with that frequency. Scan from bucket[n] down, take k elements.","Can't bucket sort frequencies"], correct: 1, explanation: "Bucket sort: O(n) space (n buckets). Build in O(n). Scan from highest frequency down, collect elements until k elements found. O(n) total — no sorting." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Heap O(n log k) vs bucket sort O(n). When is bucket sort not applicable?", why: { question: "Limitation of bucket sort approach?", options: ["No limitation","Frequencies must be bounded integers. For frequency values 1..n: works. For arbitrary scores/weights (floating point, unbounded): bucket sort fails.","Bucket sort is always better"], correct: 1, explanation: "Bucket sort requires integer frequencies in known range [1..n]. Works perfectly here. Won't work if 'frequency' is a float or an arbitrary score outside [1..n]." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space for heap vs bucket sort approaches?", why: { question: "Why O(n log k) for heap vs O(n) for bucket sort?", options: ["Both O(n log k)","Heap: n elements × O(log k) per push/pop = O(n log k). Bucket: O(n) build freq map + O(n) build buckets + O(n) collect = O(n).","Same complexity"], correct: 1, explanation: "Heap: O(n log k). Bucket sort: O(n). Bucket sort is asymptotically better. But heap generalizes to any comparator. Trade off: generality vs speed." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "k equals number of unique elements — answer?", options: ["Error","Return all unique elements — k = unique count, every element is in top-k.","Empty array"], correct: 1, explanation: "k = unique count: return all unique elements. Heap naturally holds all unique elements when k = unique count. Bucket: collect from all non-empty buckets." } },
    ],
    dryrun: { question: "nums=[1,1,1,2,2,3], k=2. Freq: {1:3, 2:2, 3:1}. Min-heap (by freq): insert (3,1)→heap=[(3,1)]. Size<2: insert (2,2)→heap=[(2,2),(3,1)]. Insert (1,3): 1<heap.min.freq=2→skip. Heap=?", options: ["[(2,2),(3,1)] → return [1,2]","[(1,3),(2,2)] → return [2,3]","[(3,1),(2,2)] → same"], correct: 0, explanation: "Heap contains (freq=2,elem=2) and (freq=3,elem=1). Both are top-2 frequent. Return elements: [1,2]. Correct — 1 appears 3 times, 2 appears 2 times." },
    pushback: { challenge: "What if you need elements ordered from most frequent to least frequent?", options: ["Max-heap: push all (freq, elem) pairs, pop k times. O(n + k log n). Or: sort top-k result by frequency desc. O(k log k).","Same min-heap with reversal","Can't order with heap"], correct: 0, explanation: "Ordered output: either (1) max-heap, pop k times O(n + k log n), or (2) after getting top-k elements via min-heap, sort them by frequency O(k log k). Depends on whether you need all n or just k." },
    followup: { question: "What if you need top-k most frequent WORDS (strings) sorted lexicographically when tied in frequency?", options: ["Min-heap with comparator: (freq desc, word asc). When freq tie: lexicographically smaller word has higher priority.","Same algorithm","Word frequency requires different structure"], correct: 0, explanation: "Top K Frequent Words: same approach but comparator = (-frequency, word). This ensures higher frequency wins; for ties, alphabetically smaller word wins. Python: (-freq, word) tuples in heap." },
    solution: { approach: "HashMap + Min-Heap of Size k", steps: ["Count frequencies with HashMap","Build min-heap of size k: key = frequency","For each unique element: if heap.size()<k or freq>heap.top.freq: push; if size>k pop min","Return all elements in heap"], time: "O(n log k)", space: "O(n)" },
    keywords: {
      understand: ["frequency","HashMap","count","k most frequent","top-k","min-heap"],
      brute: ["count","sort","O(n log n)","all","partial","faster"],
      pattern: ["min-heap","size k","freq","top-k","pop min","kth most frequent"],
      algorithm: ["freq map","heap","bucket sort","bucket[freq]","scan","collect k"],
      tradeoff: ["bucket sort","O(n)","integer","bounded","float","generalizes"],
      complexity: ["O(n log k)","heap","O(n)","bucket","trade-off","generality"],
      edge: ["k=unique","all elements","empty","tied frequencies","any order"],
    },
  },
  {
    id: "h6", title: "K Closest Points to Origin", difficulty: "Medium", pattern: "Heap",
    companies: ["Meta","Amazon"], minLevel: 3,
    description: "Given an array of points on the X-Y plane and an integer k, return the k closest points to the origin (0,0). Distance is Euclidean. You may return the answer in any order.",
    example: { input: "points=[[1,3],[-2,2]], k=1", output: "[[-2,2]] (distance²=8 < 10)" },
    hints: [
      "Use distance² = x²+y² to avoid square roots — preserves ordering.",
      "Max-heap of size k: if heap exceeds k, pop the farthest. Heap contains k closest.",
      "Alternatively, QuickSelect on distance gives O(n) average.",
    ],
    clarify: { question: "What would you clarify?", options: ["Return k closest in any order?","Ties in distance — return any k?","Use Euclidean distance (not Manhattan)?","Can k > points.length?"], mustAsk: [0,1,2], explanation: "Any order fine. Ties: any k points at tied distance are acceptable. Euclidean distance (x²+y² without sqrt). k ≤ points.length guaranteed." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🎯", prompt: "Why use distance² instead of actual distance? What problem does this solve?", why: "Avoids floating-point sqrt computation while preserving relative ordering of distances.", options: ["sqrt is needed for accuracy","distance² = x²+y² preserves ordering: if dist(A)<dist(B) then dist²(A)<dist²(B). Avoids float arithmetic, faster.","Use Manhattan distance instead","No difference — just preference"], correct: 1 },
      { id: "brute", label: "Brute Force", emoji: "💪", prompt: "How would computing all distances then sorting work? Complexity?", why: "Full sort gives correct answer but wastes work when we only need k of n.", options: ["Sort all by dist²: O(n log n). Return first k. Simple but overkill.","O(n) — just scan","O(k log n) — merge sort variant","Sort only k elements"], correct: 0 },
      { id: "pattern", label: "Pattern", emoji: "🔍", prompt: "Why does a max-heap of size k give the k closest points?", why: "Keep k smallest distances by evicting the largest whenever heap overflows.", options: ["Min-heap gives closest","Max-heap of size k: top = farthest of k closest. If new point closer than top: swap. Heap always holds k closest seen.","Heap sorts all n points","Use priority queue of size n"], correct: 1 },
      { id: "algorithm", label: "Algorithm", emoji: "⚙️", prompt: "Walk through: for each point, push dist² to max-heap; if size > k, pop max.", why: "Pop the farthest whenever we exceed k, ensuring heap always has the k closest.", options: ["Push all then pop k times","For each point: push (dist², point). If heap.size > k: pop max (farthest). Final heap = k closest. O(n log k).","Pop when dist² < heap.top","Only push if dist² < heap.top"], correct: 1 },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Heap O(n log k) vs QuickSelect O(n) average. When does each win?", why: "QuickSelect wins on avg for large n; heap is safer with bounded worst-case.", options: ["Heap always wins","QuickSelect O(n) avg, O(n²) worst, O(1) space. Heap O(n log k) guaranteed. For huge n with small k, QuickSelect's avg wins; heap safer.","QuickSelect is always better","Only sorting works"], correct: 1 },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space for the max-heap approach?", why: "n insertions each O(log k) since heap is bounded at size k.", options: ["O(n²) time, O(n) space","O(n log k) time, O(k) space — heap capped at k. Better than O(n log n) when k << n.","O(n) time, O(1) space","O(k log n) time, O(k) space"], correct: 1 },
      { id: "edge", label: "Edge Cases", emoji: "🎯", prompt: "What if k equals the number of points? What if all points are equidistant?", why: "k=n: return all points. Ties: any k acceptable.", options: ["Error if k=n","k=n: return all points (no eviction occurs). Equidistant: return any k — heap handles naturally.","Must sort equidistant points","Return none"], correct: 1 },
    ],
    dryrun: {
      input: "points=[[1,3],[-2,2]], k=1",
      steps: [
        { label: "Compute distances²", state: "[1,3]→10, [-2,2]→8" },
        { label: "Push (10,[1,3])", state: "heap=[(10,[1,3])], size=1=k, no pop" },
        { label: "Push (8,[-2,2])", state: "heap size=2>k=1, pop max=(10,[1,3])" },
        { label: "heap=[(8,[-2,2])]", state: "Return [[-2,2]]" },
      ],
      question: "After processing both points with max-heap k=1, what is returned?",
      options: ["[[-2,2]] (dist²=8 closest)","[[1,3]] (dist²=10)","Both points","Empty"],
      correct: 0,
      explanation: "Max-heap of size 1: push [1,3] (dist²=10). Push [-2,2] (dist²=8), size=2>1, pop max=10. Heap=[(8,[-2,2])]. Return [[-2,2]].",
    },
    pushback: { question: "What if the data is streaming and you need the k closest after every new point?", answer: "Maintain a max-heap of size k. For each incoming point: compute dist², push to heap. If size > k: pop max. Heap always holds k closest seen so far. O(log k) per point." },
    followup: { question: "How would you find the k farthest points instead?", answer: "Use a min-heap of size k: pop the minimum (closest) when heap exceeds k. Heap retains k largest distances. Same O(n log k) complexity, just flip heap type." },
    solution: { approach: "Max-Heap of Size k", steps: ["For each point: dist² = x²+y²","Push (dist², point) to max-heap","If heap.size > k: pop max (farthest)","Return all points remaining in heap"], time: "O(n log k)", space: "O(k)" },
    keywords: {
      understand: ["distance²","x²+y²","sqrt","ordering","float","Euclidean"],
      brute: ["sort","O(n log n)","all distances","overkill","k of n"],
      pattern: ["max-heap","size k","evict farthest","k closest","invariant","overflow"],
      algorithm: ["push","pop max","size > k","farthest","heap","O(log k)"],
      tradeoff: ["QuickSelect","O(n)","O(n²) worst","O(1) space","guaranteed","avg"],
      complexity: ["O(n log k)","O(k) space","bounded","k<<n","n insertions"],
      edge: ["k=n","equidistant","all points","ties","any k"],
    },
  },
  {
    id: "h7", title: "Task Scheduler", difficulty: "Medium", pattern: "Heap",
    companies: ["Meta","Amazon"], minLevel: 3,
    description: "Given a list of CPU tasks and a cooldown n, find the minimum number of intervals (including idle) to finish all tasks. Same task must be separated by at least n intervals.",
    example: { input: "tasks=['A','A','A','B','B','B'], n=2", output: "8 (A→B→idle→A→B→idle→A→B)" },
    hints: [
      "The answer is max(total tasks, (maxFreq-1)*(n+1) + countOfMaxFreq).",
      "Greedy: always schedule the most frequent available task next.",
      "If no valid task is available in a slot, insert idle.",
    ],
    clarify: { question: "What would you clarify?", options: ["Are task names just labels (order doesn't matter)?","Can we have idle intervals?","n=0 means no cooldown?","Return count of intervals or the actual schedule?"], mustAsk: [0,1,2], explanation: "Tasks are labels — only frequency matters, not order. Idle counts as an interval. n=0: no cooldown, answer = total tasks. Return count." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🎯", prompt: "Why do identical tasks need cooldown separation? What determines the minimum length?", why: "The most frequent task forces a minimum cycle length of n+1, creating the lower bound.", options: ["Always n+1 intervals","Most frequent task (maxFreq) must repeat. Each cycle: maxFreq-1 gaps of size n+1. Plus tail tasks. min = max(total, (maxFreq-1)*(n+1)+countOfMaxFreq).","Random scheduling works","Sort by frequency"], correct: 1 },
      { id: "brute", label: "Brute Force", emoji: "💪", prompt: "How would simulating every interval step by step work?", why: "Step-by-step simulation is correct but needs careful tracking of cooldowns.", options: ["Simulate each interval: pick most frequent available task (not in cooldown). Insert idle if none available. O(total_intervals × unique_tasks).","Sort tasks alphabetically","Random order then fix","Only count total tasks"], correct: 0 },
      { id: "pattern", label: "Pattern", emoji: "🔍", prompt: "Explain the formula: max(total_tasks, (maxFreq-1)*(n+1) + countOfMaxFreq).", why: "The formula captures the two cases: cooldown is bottleneck vs tasks fill all slots.", options: ["Always (maxFreq-1)*(n+1)+countOfMaxFreq","(maxFreq-1)*(n+1)+countOfMaxFreq = min slots needed if maxFreq task drives schedule. total_tasks = min if tasks fill gaps. Answer = max of both.","total_tasks is always enough","maxFreq * (n+1) always"], correct: 1 },
      { id: "algorithm", label: "Algorithm", emoji: "⚙️", prompt: "Walk through the formula derivation for tasks=['A','A','A','B','B','B'], n=2.", why: "Concrete example: maxFreq=3, countOfMaxFreq=2, total=6.", options: ["max(6, (3-1)*(2+1)+2) = max(6,8) = 8","max(6, 3*(2+1)) = max(6,9) = 9","6 + 2 = 8","3 * 3 = 9"], correct: 0 },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Formula O(n) vs heap simulation O(total_intervals × log k). When does simulation matter?", why: "Simulation is needed when you must output the actual schedule, not just count.", options: ["Formula always sufficient","Formula: O(n) count only. Simulation: O(intervals × log k) — needed if you must output the schedule. For count, formula wins.","Simulation always better","Both give same output"], correct: 1 },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity of the formula approach?", why: "One frequency-counting pass plus O(1) formula evaluation.", options: ["O(n²) time, O(n) space","O(n) time (count frequencies), O(1) extra space (formula uses only maxFreq and countOfMaxFreq).","O(n log n) time","O(n) time, O(n) space for freq map"], correct: 1 },
      { id: "edge", label: "Edge Cases", emoji: "🎯", prompt: "What if n=0? What if all tasks are distinct?", why: "n=0 removes cooldown; all distinct means tasks perfectly fill slots.", options: ["n=0: error","n=0: answer=total tasks (formula gives max(total, total)=total). All distinct: each fills one interval, no idle needed.","n=0: answer=0","All distinct: answer=n+1"], correct: 1 },
    ],
    dryrun: {
      input: "tasks=['A','A','A','B','B','B'], n=2",
      steps: [
        { label: "Count frequencies", state: "A:3, B:3" },
        { label: "maxFreq=3, countOfMaxFreq=2, total=6", state: "" },
        { label: "Formula: (3-1)*(2+1)+2 = 2*3+2 = 8", state: "" },
        { label: "max(6, 8) = 8", state: "Schedule: A→B→idle→A→B→idle→A→B" },
      ],
      question: "What is the minimum number of intervals needed?",
      options: ["6 (no idle needed)","7","8","9"],
      correct: 2,
      explanation: "(maxFreq-1)*(n+1)+countOfMaxFreq = (3-1)*3+2 = 8. total=6. max(6,8)=8. Two idle slots are needed.",
    },
    pushback: { question: "What if the tasks had different execution times (not all 1 unit)?", answer: "Weighted task scheduler: heap simulation required. Each cycle pick highest-frequency available task, respecting cooldowns by tracking ready-times. Formula breaks — simulation is the only general approach." },
    followup: { question: "How would you reconstruct the actual schedule (not just the count)?", answer: "Use a max-heap and a cooldown queue. Each round: pop most frequent, add to schedule. After executing, push to cooldown queue with available_time = current_time + n + 1. Pull from cooldown to heap when available. O(intervals × log k)." },
    solution: { approach: "Frequency Formula", steps: ["Count task frequencies","maxFreq = max frequency; countOfMaxFreq = number of tasks with maxFreq","formula = (maxFreq-1)*(n+1) + countOfMaxFreq","Return max(tasks.length, formula)"], time: "O(n)", space: "O(1)" },
    keywords: {
      understand: ["cooldown","same task","n intervals apart","maxFreq","cycle","lower bound"],
      brute: ["simulate","each interval","available","idle","O(intervals × tasks)"],
      pattern: ["formula","(maxFreq-1)*(n+1)","countOfMaxFreq","max","total","bottleneck"],
      algorithm: ["maxFreq=3","count=2","(3-1)*(2+1)+2=8","max(6,8)","idle","cycle"],
      tradeoff: ["count vs schedule","simulation","O(n)","O(intervals × log k)","output"],
      complexity: ["O(n)","O(1) space","freq map","formula","constant extra"],
      edge: ["n=0","all distinct","no idle","total=formula","single task"],
    },
  },
  {
    id: "h8", title: "Reorganize String", difficulty: "Medium", pattern: "Heap",
    companies: ["Meta","Amazon"], minLevel: 3,
    description: "Given string s, rearrange its characters so that no two adjacent characters are the same. Return any valid rearrangement, or '' if impossible.",
    example: { input: "s='aab'", output: "'aba'" },
    hints: [
      "If any character appears more than ceil(n/2) times, it's impossible.",
      "Max-heap by frequency: always pick the most frequent character that differs from the previous.",
      "Greedily interleave: pop top, if it equals previous character pop second and push first back.",
    ],
    clarify: { question: "What would you clarify?", options: ["Return any valid arrangement or a specific one?","What if multiple valid arrangements exist?","Lowercase letters only?","What constitutes 'impossible'?"], mustAsk: [0,3], explanation: "Return any valid rearrangement. Multiple valid: any is fine. Lowercase only. Impossible iff maxFreq > ceil(n/2) — one char would be forced adjacent to itself." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🎯", prompt: "When is rearrangement impossible? Derive the impossibility condition.", why: "If most-frequent char appears too often, it must be adjacent to itself.", options: ["Always possible","Impossible if maxFreq > ceil(n/2). If char appears k times, needs at least k-1 other chars between each occurrence. n - maxFreq < maxFreq - 1 → impossible.","Only 'aaaa' is impossible","Two same chars always adjacent"], correct: 1 },
      { id: "brute", label: "Brute Force", emoji: "💪", prompt: "How would generating all permutations and checking validity work?", why: "Exponential permutations make brute force infeasible for long strings.", options: ["O(n!) permutations, each O(n) check. Infeasible for n>10.","O(n²) comparisons","Sort then check","Random shuffle until valid"], correct: 0 },
      { id: "pattern", label: "Pattern", emoji: "🔍", prompt: "Why does always picking the most frequent available character greedily produce a valid result?", why: "Greedy most-frequent keeps high-count chars spread out, avoiding forced adjacency.", options: ["Greedy doesn't work for strings","Greedy: always use most-frequent char not equal to previous. Reduces highest count fastest, preventing any char from dominating adjacent slots.","Alternating chars always works","Sort by frequency descending"], correct: 1 },
      { id: "algorithm", label: "Algorithm", emoji: "⚙️", prompt: "Walk through max-heap approach: pop top, if same as previous pop second, push back.", why: "Track previous character to avoid placing the same char consecutively.", options: ["Pop two at once always","prev=null. Each step: pop (freq,char). If char==prev.char: pop second (freq2,char2), push first back, use char2. Else use char. Push char back if freq-1>0.","Sort then place alternately","Only use heap of size 2"], correct: 1 },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Heap O(n log k) vs interleaving O(n). What is the interleaving trick?", why: "Interleaving leverages sorted positions directly without a heap.", options: ["Heap always necessary","Interleave: sort by freq desc. Place chars at positions 0,2,4... then 1,3,5.... O(n) after sort. Works because most-frequent chars get even indices first.","Both are O(n log k)","Random placement then fix"], correct: 1 },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space for heap approach?", why: "n characters each pushed/popped from heap of at most 26 chars.", options: ["O(n²) time","O(n log 26) = O(n) time (heap bounded at 26 lowercase letters). O(n) space for result.","O(n log n) time","O(26) time — constant"], correct: 1 },
      { id: "edge", label: "Edge Cases", emoji: "🎯", prompt: "What if the string has only one unique character? What if all characters have equal frequency?", why: "Single char with n>1 is impossible; equal frequencies always work.", options: ["Single char always valid","Single char, n>1: maxFreq=n > ceil(n/2) for n>1 → impossible, return ''. Equal freqs: always valid — interleave evenly.","Single char: return as-is","Equal freqs: impossible"], correct: 1 },
    ],
    dryrun: {
      input: "s='aab'",
      steps: [
        { label: "Frequencies: a→2, b→1", state: "maxFreq=2, n=3, ceil(3/2)=2, possible" },
        { label: "Max-heap: [(2,a),(1,b)]", state: "prev=null" },
        { label: "Pop (2,a), char≠prev, result='a', push (1,a)", state: "heap=[(1,a),(1,b)], prev=a" },
        { label: "Pop (1,a), char==prev(a)! Pop (1,b), push (1,a) back, result='ab'", state: "heap=[(1,a)], prev=b" },
        { label: "Pop (1,a), char≠prev(b), result='aba'", state: "Done" },
      ],
      question: "What is the reorganized string for 'aab'?",
      options: ["'aab' (invalid — same adjacent)","'aba'","'baa' (invalid)","'' (impossible)"],
      correct: 1,
      explanation: "maxFreq=2 ≤ ceil(3/2)=2, so possible. Greedy max-heap produces 'aba': a,b,a — no two adjacent chars are same.",
    },
    pushback: { question: "What if you need to minimize the number of adjacent same-character pairs (not eliminate them entirely)?", answer: "Different problem. Greedy still useful but goal changes. Arrange by interleaving sorted frequency groups. For a full minimization, use DP or greedy by always placing most-frequent chars as far apart as possible." },
    followup: { question: "What if the constraint is no same characters within distance k (not just adjacent)?", answer: "Generalize: enforce k-gap cooldown. Max-heap + cooldown queue: after placing a char, add to cooldown queue and release only after k positions. Same structure as Task Scheduler. O(n log 26) = O(n)." },
    solution: { approach: "Max-Heap by Frequency", steps: ["Check impossibility: maxFreq > ceil(n/2) → return ''","Build max-heap of (frequency, char) pairs","Each step: pop top; if same as prev, pop second and push first back","Append chosen char; push back with freq-1 if > 0","Return result string"], time: "O(n log 26) = O(n)", space: "O(n)" },
    keywords: {
      understand: ["impossible","maxFreq","ceil(n/2)","adjacent","rearrange","condition"],
      brute: ["O(n!)","permutations","check","infeasible","n>10","exponential"],
      pattern: ["greedy","most frequent","not equal to prev","spread out","dominant","count"],
      algorithm: ["pop top","prev","char==prev","pop second","push back","freq-1"],
      tradeoff: ["interleave","O(n)","sorted positions","even indices","no heap","same result"],
      complexity: ["O(n log 26)","O(n)","bounded 26","heap size","O(n) space","result"],
      edge: ["single char","impossible","n>1","equal freq","valid","ceil"],
    },
  },
];
