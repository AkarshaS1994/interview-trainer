export const TWO_POINTER_PROBLEMS = [
  {
    id: "tp1", title: "Remove Duplicates from Sorted Array", difficulty: "Easy", pattern: "Two Pointers",
    companies: ["Amazon","Meta","Google"], minLevel: 1,
    description: "Given a sorted array in non-decreasing order, remove duplicates in-place. Return k (count of unique elements). First k elements must hold the result.",
    example: { input: "nums=[0,0,1,1,1,2,2,3,3,4]", output: "k=5, nums=[0,1,2,3,4,...]" },
    hints: ["One pointer tracks where to write, one reads ahead.", "Write pointer only advances when a new unique value is found.", "Array is sorted — duplicates are always adjacent."],
    clarify: { question: "What would you clarify?", options: ["Must I do it in-place?","What happens to elements beyond k?","Is the array guaranteed sorted?","Do I return k or the array?","Can the array be empty?"], mustAsk: [0,1,3], explanation: "In-place required. Beyond k: can be anything. Return k. Empty array → k=0. Array is sorted (key constraint)." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why does 'sorted' make this problem easier? What property of sorted arrays matters?", why: { question: "Why are duplicates always adjacent in a sorted array?", options: ["They aren't","By definition of sorting: equal elements are grouped together. No need to scan the whole array for duplicates.","Sorting creates new duplicates"], correct: 1, explanation: "In a sorted array, all copies of value X appear consecutively. Scan left-to-right: whenever current value differs from previous, it's a new unique value." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you copy unique values to a new array? Why is that not in-place?", why: { question: "Why is new array approach O(n) space?", options: ["It's O(1)","Allocating an n-element array to store unique values = O(n) extra space","Arrays are space-free"], correct: 1, explanation: "Separate array holds up to n unique values. Problem requires in-place to achieve O(1) extra space." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Describe the slow/fast two-pointer setup. What does each pointer represent?", why: { question: "What does the slow (write) pointer track?", options: ["Number of duplicates","The next position to write a unique value. Advances only on new unique find.","The end of array"], correct: 1, explanation: "slow = write boundary. fast = read pointer scanning all elements. When nums[fast] != nums[slow], unique found: slow++, write nums[fast] to nums[slow]." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through the algorithm. When exactly do you advance slow vs just fast?", why: { question: "When does slow advance?", options: ["Every iteration","Only when nums[fast] != nums[slow] — a new unique value found","When fast > slow"], correct: 1, explanation: "fast always advances. slow advances only on new unique: slow++; nums[slow]=nums[fast]. Otherwise just fast++. Final k = slow+1." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "This is O(1) space. If space were unlimited, is there a simpler approach?", why: { question: "What's the benefit of the in-place approach over collect-unique-then-copy?", options: ["In-place is never better","Avoids allocation and a second pass. For large arrays (millions), allocation cost matters. Also: satisfies the interview constraint.","In-place uses more CPU"], correct: 1, explanation: "In-place: O(1) space, single pass. New array: O(n) space, two passes (collect + copy back). In-place wins on both metrics." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(n) time?", options: ["Two pointers → O(n²)","fast traverses array once, slow stays ≤ fast. Total iterations = n. Linear.","O(n log n) for comparison"], correct: 1, explanation: "fast visits each of n elements exactly once. slow ≤ fast, never passes it. Total work: O(n). Space: O(1) — two pointer variables only." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "Empty array nums=[]: what do you return?", options: ["1","0 — no unique elements","Error"], correct: 1, explanation: "Empty: fast never enters loop, slow=0, return 0. Single element: fast loops once but nums[0]==nums[0] always true... actually slow=0 initially, return slow+1=1. Correct." } },
    ],
    dryrun: { question: "nums=[1,1,2]. slow=0, fast=0. fast=1: nums[1]=1 == nums[0]=1, skip. fast=2: nums[2]=2 != nums[0]=1. slow=1, nums[1]=2. Return?", options: ["k=2, nums=[1,2,2]","k=3","k=1"], correct: 0, explanation: "slow=1, return slow+1=2. First k=2 elements: nums[0]=1, nums[1]=2. Result: k=2, [1,2,...]. Correct." },
    pushback: { challenge: "What if duplicates are allowed at most twice (not just once)?", options: ["Change condition: allow if slow<2 OR nums[slow-2] != nums[fast]. Same two-pointer. O(n).","Completely different approach","Need to count duplicates explicitly"], correct: 0, explanation: "Remove Duplicates II: slow<2 always writes (first two elements OK). Otherwise: only write if new value differs from what's 2 positions back. Same two-pointer pattern, generalized." },
    followup: { question: "What if the array is NOT sorted — remove all duplicates from an unsorted array in O(n) time?", options: ["Hash set: track seen values, write only new ones using slow pointer. O(n) time O(n) space.","Sort first, then use slow/fast","Can't do O(n) without sorting"], correct: 0, explanation: "Unsorted: can't rely on adjacency. Hash set O(n) space tracks seen values. Slow pointer writes unique values. Two passes or one pass with set." },
    solution: { approach: "Slow/Fast Two Pointers", steps: ["if nums empty: return 0","slow=0","For fast in 1..n-1: if nums[fast] != nums[slow]: slow++; nums[slow]=nums[fast]","Return slow+1"], time: "O(n)", space: "O(1)" },
    keywords: {
      understand: ["sorted","adjacent","duplicate","unique","in-place","contiguous","consecutive","property"],
      brute: ["new array","O(n)","space","copy","extra","allocate","unique","collect"],
      pattern: ["slow","fast","write","read","pointer","advance","unique","in-place"],
      algorithm: ["slow++","nums[fast]","nums[slow]","advance","unique","write","return","k"],
      tradeoff: ["O(1)","space","in-place","two passes","allocation","single pass","constraint"],
      complexity: ["O(n)","time","O(1)","space","linear","once","fast","slow"],
      edge: ["empty","single","return 0","return 1","sorted","all same"],
    },
  },
  {
    id: "tp2", title: "Container With Most Water", difficulty: "Medium", pattern: "Two Pointers",
    companies: ["Meta","Amazon","Google"], minLevel: 3,
    description: "Given n non-negative integers representing heights of lines, find two lines that together with x-axis form a container holding the most water.",
    example: { input: "height=[1,8,6,2,5,4,8,3,7]", output: "49 (lines at index 1 and 8, height=min(8,7)=7, width=7)" },
    hints: ["Start with widest possible container (left=0, right=n-1).", "To potentially increase area, move the pointer with the shorter height.", "Area = min(height[l], height[r]) * (r - l)."],
    clarify: { question: "What would you clarify?", options: ["Can heights be 0?","Must the container use vertical lines only (no tilting)?","Is the array always non-negative?","Return max area or the indices?","Can two lines be at same index?"], mustAsk: [0,1,3], explanation: "Heights ≥ 0. Vertical lines only, no tilting. Return area. Index 0 to n-1, can't use same line twice." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What determines the area between two lines? Why does a shorter line limit capacity?", why: { question: "Why is area = min(height[l], height[r]) × (r - l)?", options: ["Average height × width","Water can only fill to the height of the SHORTER line — taller one doesn't matter beyond that. Width = index distance.","Max height × width"], correct: 1, explanation: "Water overflows the shorter line. Height bounded by min(left, right). Width = distance between indices. Area = min × (r-l)." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you check every pair? What's the complexity?", why: { question: "Why O(n²) for brute force?", options: ["n lines × n lines = n² pairs, each O(1) area calc","Sorting required","Not O(n²)"], correct: 0, explanation: "All pairs (i,j) where i<j: n*(n-1)/2 pairs = O(n²). Each area computation O(1). Total O(n²)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Why start with the widest possible container and move inward? What's the greedy insight?", why: { question: "Why move the pointer with the SHORTER height inward?", options: ["Always move left pointer","Moving the taller pointer can only decrease (or maintain) area — width decreases, height bounded by same shorter line. No gain possible.","Move pointer with larger height"], correct: 1, explanation: "If we move the taller pointer: new height still bounded by shorter line (or less). Width shrinks. Area can only decrease. Only moving shorter pointer gives a chance to find taller line." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through the two-pointer approach. Why does this greedy cover all optimal pairs?", why: { question: "Why doesn't the greedy miss the optimal pair?", options: ["It might miss","When we discard a pointer position, we've already computed the best area for that position (it was paired with the farthest possible partner). No better pair exists for it.","We try all pairs"], correct: 1, explanation: "At each step, the current pointer is paired with its optimal (farthest) partner. Moving it inward is the only way to improve area. Every position is considered as the 'limiting' side exactly once." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "O(n²) brute vs O(n) two-pointer. Can you prove greedy correctness?", why: { question: "What property makes the greedy provably correct?", options: ["Greedy is always correct on arrays","After computing area(l,r): if height[l]<height[r], any pair (l, r') where r'<r has smaller width AND same height limit (height[l]). Moving l is the ONLY option with improvement potential.","We prove by induction"], correct: 1, explanation: "Key invariant: when we move pointer p (the shorter one), all pairs involving p with indices between current positions have already been shown suboptimal. By exhaustion, greedy is correct." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space of two-pointer solution?", why: { question: "Why O(n) time?", options: ["Two pointers → O(n²)","l and r start at ends, move inward. Total moves = n-1. Each O(1). Linear.","O(n log n) from sorting"], correct: 1, explanation: "l starts at 0, r at n-1. Each step, one pointer moves inward. They meet after n-1 steps. O(n). Space: O(1) — two pointers, max area variable." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "height=[1,1] — what is the answer?", options: ["0","1 (min(1,1) × (1-0) = 1)","2"], correct: 1, explanation: "Two lines, both height 1. Area = min(1,1) × 1 = 1. l=0, r=1: area=1. l==r: loop ends. Return 1." } },
    ],
    dryrun: { question: "height=[1,8,6,2,5,4,8,3,7]. l=0(h=1), r=8(h=7). area=min(1,7)×8=8. Move l (shorter). l=1(h=8), r=8(h=7). area=min(8,7)×7=49. Move r. l=1(h=8), r=7(h=3). area=min(8,3)×6=18. Continue... What is the final answer?", options: ["49","56","42"], correct: 0, explanation: "Maximum area seen is 49, at l=1(h=8), r=8(h=7). All subsequent pairs yield ≤49. Final answer: 49." },
    pushback: { challenge: "What if you also need to return the actual indices (not just the area)?", options: ["Track bestL and bestR alongside maxArea. Update when new max found. Same O(n). Return (bestL, bestR, maxArea).","Need different algorithm","Use brute force — two pointers can't track indices"], correct: 0, explanation: "Same algorithm: when area > maxArea, update maxArea=area, bestL=l, bestR=r. Return indices at end. Zero extra complexity." },
    followup: { question: "What if lines can be tilted — the container height is the line height × sin(angle)?", options: ["Tilt optimization is a continuous problem — binary search or calculus. Two-pointer no longer applies directly.","Same two-pointer works","Always tilt to 90°"], correct: 0, explanation: "Tilted lines: area becomes a continuous function of both line positions and angles. No longer a simple grid problem. Two-pointer greedy relies on discrete, monotone structure that breaks with tilt." },
    solution: { approach: "Two Pointers from Ends", steps: ["l=0, r=n-1, maxArea=0","While l < r: area=min(h[l],h[r])*(r-l); maxArea=max(maxArea,area)","If h[l] < h[r]: l++ else r--","Return maxArea"], time: "O(n)", space: "O(1)" },
    keywords: {
      understand: ["min","height","area","width","shorter","water","overflow","bounded"],
      brute: ["O(n²)","pairs","every","check","n²","nested","combination"],
      pattern: ["greedy","widest","inward","shorter","move","two pointers","left","right"],
      algorithm: ["l","r","min","area","maxArea","shorter","inward","move"],
      tradeoff: ["greedy","correct","invariant","shorter","width","O(n)","O(n²)"],
      complexity: ["O(n)","time","O(1)","space","linear","n-1","moves"],
      edge: ["two elements","equal height","zero","minimum","all same"],
    },
  },
  {
    id: "tp3", title: "3Sum", difficulty: "Medium", pattern: "Two Pointers",
    companies: ["Meta","Amazon","Google","NVIDIA"], minLevel: 3,
    description: "Given integer array, find all unique triplets [nums[i], nums[j], nums[k]] such that i≠j≠k≠i and nums[i]+nums[j]+nums[k]==0.",
    example: { input: "nums=[-1,0,1,2,-1,-4]", output: "[[-1,-1,2],[-1,0,1]]" },
    hints: ["Sort first, then fix one element and use two-pointer for the rest.", "Skip duplicate values for the fixed element.", "Skip duplicate values inside the inner two-pointer loop too."],
    clarify: { question: "What would you clarify?", options: ["Can the same element be used multiple times?","Should triplets be unique (no duplicate sets)?","Are all integers negative/positive?","Return triplets or indices?","What if no triplets exist?"], mustAsk: [0,1,4], explanation: "Each element used at most once (different indices). No duplicate triplets in output. Return triplets (values). No triplets → return empty list." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why is 3Sum harder than 2Sum? What makes avoiding duplicates tricky?", why: { question: "Why does 3Sum require more care than 2Sum?", options: ["Same difficulty","Two-sum has one unique solution guaranteed. 3Sum: multiple triplets, duplicates in input → same triplet multiple ways. Must deduplicate.","3Sum is actually simpler"], correct: 1, explanation: "3Sum: array can have duplicates, must avoid duplicate triplets in output. E.g. [-1,-1,0,1]: [-1,0,1] would appear twice without skipping. Need explicit dedup logic." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you check all triplets naively? What's the complexity?", why: { question: "Why O(n³) for brute force 3Sum?", options: ["Three nested loops: O(n) × O(n) × O(n) = O(n³) pairs to check","Hash map speeds it up","Sort reduces to O(n²)"], correct: 0, explanation: "Three nested loops (i,j,k) with i<j<k: n*(n-1)*(n-2)/6 triplets = O(n³). Each check O(1). Dedup with set: O(n³) time, O(n) space." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does sorting enable a two-pointer approach, reducing O(n³) to O(n²)?", why: { question: "Why does sorting allow two-pointer for the inner search?", options: ["Sorting is unrelated","After fixing nums[i], need two values summing to -nums[i]. Sorted subarray → two-pointer finds this in O(n) instead of O(n²) nested loops.","Sort enables binary search only"], correct: 1, explanation: "Sort: O(n log n). Fix i (outer loop: O(n)). Inner: two-pointer l=i+1, r=n-1 finds target sum in O(n). Total: O(n²). Better than O(n³) brute." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through the full algorithm. Where exactly do you skip duplicates?", why: { question: "Why skip duplicates at BOTH the outer and inner loops?", options: ["Only outer loop needs dedup","Outer: skip same nums[i] to avoid same fixed value. Inner: after finding triplet, skip same nums[l] and nums[r] to avoid same triplet with different indices.","No dedup needed after sort"], correct: 1, explanation: "Outer skip: if i>0 and nums[i]==nums[i-1], continue. Inner skip after adding triplet: while l<r and nums[l]==nums[l-1]: l++. Same for r. Both needed." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Sorting+two-pointer O(n²) vs hash-set O(n²). What are the differences?", why: { question: "Advantage of sort+two-pointer over hash set approach?", options: ["Hash set is always better","Two-pointer: O(1) extra space. Hash set: O(n) extra space. Both O(n²) time. Two-pointer also cleaner dedup.","Sort is slower"], correct: 1, explanation: "Hash set approach also O(n²) time but O(n) space per outer iteration. Sort+two-pointer: O(1) extra space per iteration (excluding output). Also, dedup with sorted array is simpler — just skip adjacent equals." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Final time and space complexity?", why: { question: "Why O(n²) not O(n² log n)?", options: ["Sort adds log factor","Sort is O(n log n) — dominated by O(n²) outer×inner. Total: O(n²).","Inner loop is O(n log n)"], correct: 1, explanation: "Sort: O(n log n). Two-pointer nested: O(n) outer × O(n) inner = O(n²). Dominated by O(n²). Sort is absorbed." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "All zeros nums=[0,0,0,0]: what is the output?", options: ["[]","[[0,0,0]] — exactly one unique triplet summing to 0","[[0,0,0],[0,0,0]]"], correct: 1, explanation: "Sorted: [0,0,0,0]. i=0, l=1, r=3. Sum=0 → add [0,0,0]. Skip duplicates: l advances to end. i=1: nums[1]==nums[0] → skip. Output: [[0,0,0]]." } },
    ],
    dryrun: { question: "nums=[-4,-1,-1,0,1,2] (sorted). i=0(val=-4): l=1, r=5. -4+(-1)+2=-3<0, l++. -4+(-1)+2=-3<0, l++. -4+0+2=-2<0, l++. -4+1+2=-1<0, l++. l>=r, next i. i=1(val=-1): l=2, r=5. -1+(-1)+2=0! Add [-1,-1,2]. Skip dups. What's next triplet found?", options: ["[-1,0,1] when l=3,r=4","[-1,1,2]","No more triplets"], correct: 0, explanation: "After adding [-1,-1,2]: skip dup for -1 (nums[2]). l=3(0), r=4(1). -1+0+1=0! Add [-1,0,1]. l++,r--. l>=r. i=2: nums[2]=nums[1]=-1, skip. i=3: val=0, l=4,r=5. 0+1+2=3>0, r--. l>=r. Done. Output: [[-1,-1,2],[-1,0,1]]." },
    pushback: { challenge: "What if you need 4Sum (four elements summing to target)?", options: ["Add another outer loop: O(n³). Fix i, j (two loops), then two-pointer for remaining two. Same dedup strategy.","Completely new algorithm","3Sum is the furthest this pattern extends"], correct: 0, explanation: "4Sum: two outer loops (i,j), two-pointer inner. O(n³). Same sorting+dedup strategy. General k-Sum: recursively reduce to 2Sum. k-2 outer loops + two-pointer = O(n^(k-1))." },
    followup: { question: "What is the number of triplets summing to target (count, not the triplets)?", options: ["Same algorithm but increment counter instead of appending. Dedup by counting distinct triplets. O(n²).","Different algorithm needed","3Sum count is NP-hard"], correct: 0, explanation: "3Sum count: sort + two-pointer, but when sum==target and nums[l]==nums[r], count combinations (r-l). When l and r differ, handle multiplicities carefully. Still O(n²)." },
    solution: { approach: "Sort + Two Pointers", steps: ["Sort nums","For i in 0..n-3: if i>0 and nums[i]==nums[i-1]: skip","l=i+1, r=n-1; while l<r: check sum","If sum==0: add triplet, skip dups; if sum<0: l++; if sum>0: r--"], time: "O(n²)", space: "O(1) extra" },
    keywords: {
      understand: ["triplet","unique","duplicate","sort","zero","target","sum","dedup"],
      brute: ["O(n³)","nested","three","loops","every","triplet","check"],
      pattern: ["sort","fix","outer","two-pointer","inner","reduce","O(n²)"],
      algorithm: ["skip","duplicate","adjacent","l","r","triplet","sum","equals"],
      tradeoff: ["O(1)","space","hash set","O(n)","sort","dedup","cleaner"],
      complexity: ["O(n²)","time","O(1)","sort","dominated","outer","inner"],
      edge: ["all zeros","empty","negative","no triplets","duplicates"],
    },
  },
  {
    id: "tp4", title: "Valid Palindrome", difficulty: "Easy", pattern: "Two Pointers",
    companies: ["Meta","Amazon"], minLevel: 1,
    description: "Given a string s, return true if it is a palindrome, considering only alphanumeric characters and ignoring cases.",
    example: { input: 's="A man, a plan, a canal: Panama"', output: "true" },
    hints: ["Use two pointers — one from each end.", "Skip non-alphanumeric characters.", "Compare characters case-insensitively."],
    clarify: { question: "What would you clarify?", options: ["Include only alphanumeric (letters and digits)?","Ignore case?","What about empty string — palindrome?","Only ASCII?","What counts as alphanumeric?"], mustAsk: [0,1,2], explanation: "Only alphanumeric chars. Case-insensitive. Empty string is a palindrome. ASCII only. Letters and digits." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What exactly is a palindrome? How does filtering alphanumeric chars change the comparison?", why: { question: "Why do we ignore non-alphanumeric characters?", options: ["Bug in the problem","Problem definition: only consider letters and digits. Spaces, punctuation don't count.","Makes it harder"], correct: 1, explanation: "Real-world strings have punctuation and spaces. The problem asks: if you extract only alphanumeric chars and lowercase them, does the result read the same forwards and backwards?" } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you build a filtered string and then check if it equals its reverse?", why: { question: "What's the space complexity of building a filtered string?", options: ["O(1)","O(n) — create new string of filtered lowercase chars, reverse it, compare. Two O(n) strings.","O(log n)"], correct: 1, explanation: "Build filtered string: O(n) space. Reverse: O(n) space. Compare: O(n). Total O(n) time O(n) space. Two-pointer approach avoids the extra O(n) space." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How do two pointers from opposite ends check palindrome without extra space?", why: { question: "Why two pointers from each end converging inward?", options: ["Arbitrary","Palindrome: s[0]==s[n-1], s[1]==s[n-2], etc. Two pointers check pairs symmetrically. Skip non-alphanumeric by advancing the pointer without comparing.","Must be sorted first"], correct: 1, explanation: "Left and right pointers move inward. At each step: skip non-alphanumeric, compare characters. If mismatch: not palindrome. If pointers cross: palindrome confirmed." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: skip non-alphanumeric on each side, then compare case-insensitively.", why: { question: "What order should you skip non-alphanumeric vs compare?", options: ["Compare first","Skip on both sides first, then compare. Otherwise you compare a letter to a space.","Order doesn't matter"], correct: 0, explanation: "Algorithm: while left<right: advance left past non-alphanumeric; advance right past non-alphanumeric; compare s[left].lower()==s[right].lower(); if mismatch return false; else left++, right--." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Build-then-check O(n) space vs two-pointer O(1) space. When does the extra allocation matter?", why: { question: "When does O(1) space matter over O(n)?", options: ["Never — strings are small","Large inputs (gigabyte logs, DNA sequences): extra O(n) allocation doubles memory usage. In-place two-pointer avoids this.","O(1) is always better"], correct: 1, explanation: "For large strings or memory-constrained environments, avoiding O(n) allocation is significant. Two-pointer processes characters in-place without any extra data structure." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(n) time even with skipping?", options: ["Skipping adds extra iterations","Left and right each traverse the string at most once. Total character inspections ≤ 2n. O(n).","O(n²) due to skipping"], correct: 1, explanation: "Each character is inspected at most twice (once by left, once by right). Total: O(2n) = O(n). Space: O(1) — only two pointer variables." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "String with only non-alphanumeric chars like ' ': is it a palindrome?", options: ["No","Yes — filtering removes all chars, leaving empty string. Empty string is vacuously a palindrome.","Error"], correct: 1, explanation: "Filter removes all non-alphanumeric: empty string remains. Empty string is a palindrome by convention. Two-pointer loop never enters (left >= right immediately). Return true." } },
    ],
    dryrun: { question: 's="A man". Filtered lowercase: "aman". Left=0(a), right=3(n). a≠n: not palindrome. But wait — the full string "A man, a plan, a canal: Panama". What does filtered lowercase become?', options: ["amanaplanacanalpanama — palindrome","amanaplanacanalpanama — not palindrome","Error on punctuation"], correct: 0, explanation: "Filtered: 'amanaplanacanalpanama'. Two pointers: a-a, m-m, a-a, n-n, a-a, p-p, l-l, a-a, n-n, a-a, c-c, a-a, n-n, a-a, l-l, p-p, a-a, n-n, a-a, m-m (21 chars). Return true." },
    pushback: { challenge: "What if you need to check if any permutation of the string is a palindrome?", options: ["Count character frequencies. Palindrome possible iff at most one character has odd count. O(n) time O(1) or O(k) space.","Same two-pointer approach","Sort then check palindrome"], correct: 0, explanation: "Palindrome permutation: at most one char can have odd frequency (the middle char). Count freqs in O(n). Check: how many odd counts. If ≤1: yes. O(n) time, O(alphabet) space." },
    followup: { question: "What if you can delete at most one character from the string — can it become a palindrome?", options: ["Two pointers: when mismatch at (l,r), try skipping s[l] or s[r]. Check if remaining substring is palindrome. O(n) time.","Need DP","Can't solve with two pointers"], correct: 0, explanation: "Valid Palindrome II: on mismatch at l,r — try isPalin(l+1,r) or isPalin(l,r-1). If either is true: return true. O(n) total, O(1) space." },
    solution: { approach: "Two Pointers with Skip", steps: ["left=0, right=n-1","While left<right: skip non-alphanumeric on both sides","Compare s[left].lower() == s[right].lower()","If mismatch: return false; else left++, right--","Return true"], time: "O(n)", space: "O(1)" },
    keywords: {
      understand: ["palindrome","alphanumeric","ignore","case","filter","letters","digits","reverse"],
      brute: ["filter","reverse","compare","O(n)","space","new string","extra"],
      pattern: ["left","right","inward","skip","compare","converge","two pointers"],
      algorithm: ["skip","alphanumeric","lower","compare","left++","right--","mismatch"],
      tradeoff: ["O(1)","space","allocation","in-place","large","memory","extra"],
      complexity: ["O(n)","time","O(1)","space","linear","twice","pointer"],
      edge: ["empty","spaces only","single char","all punctuation","true"],
    },
  },
  {
    id: "tp5", title: "Sort Colors", difficulty: "Medium", pattern: "Two Pointers",
    companies: ["Meta","Google"], minLevel: 3,
    description: "Given an array with values 0 (red), 1 (white), 2 (blue), sort in-place so reds come first, whites in the middle, blues at the end. Must use O(1) space.",
    example: { input: "nums=[2,0,2,1,1,0]", output: "[0,0,1,1,2,2]" },
    hints: ["Use three pointers: low, mid, high.", "Invariant: nums[0..low-1]=0, nums[low..mid-1]=1, nums[high+1..n-1]=2.", "Swap based on nums[mid]: 0→swap with low, 2→swap with high, 1→advance mid."],
    clarify: { question: "What would you clarify?", options: ["Only values 0,1,2?","Must it be done in-place (O(1) space)?","Single pass or multiple passes allowed?","Return sorted array or modify in place?","What if array is already sorted?"], mustAsk: [0,1,2], explanation: "Only 0,1,2. Must be in-place. The challenge is to do it in a single pass (Dutch National Flag). Modify in place. Already sorted: no extra work needed." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Why can't you use a standard sort here efficiently? What's special about only 3 distinct values?", why: { question: "Why is the Dutch National Flag problem solvable in O(n) rather than O(n log n)?", options: ["Sorting is always O(n log n)","Only 3 distinct values means counting sort or three-way partition works in O(n). General sort doesn't exploit this structure.","Must use comparison sort"], correct: 1, explanation: "3 distinct values: counting sort O(n) or Dutch National Flag O(n) one-pass. General sort O(n log n) doesn't exploit the small value range. Known-range values → linear sort." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would counting sort work (two passes)? What's its space complexity?", why: { question: "Why does counting sort use O(1) space here?", options: ["It uses O(n) space","Count 0s, 1s, 2s (3 variables). Reconstruct: write count0 zeros, count1 ones, count2 twos. O(1) space (3 counts), O(n) time, but TWO passes.","O(n) space needed"], correct: 1, explanation: "Count: count0, count1, count2 (3 variables). Fill array in second pass. O(1) space. But 2 passes. Dutch National Flag does same in 1 pass." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "What invariant do the three pointers low, mid, high maintain at each step?", why: { question: "What does the invariant state?", options: ["All three regions are equal size","nums[0..low-1]=all 0s. nums[low..mid-1]=all 1s. nums[high+1..n-1]=all 2s. Mid to high = unprocessed.","Low=0, high=n-1 always"], correct: 1, explanation: "Three-way partition invariant: left of low = 0s, low to mid-1 = 1s, right of high = 2s, mid to high = unexplored. When mid > high: done." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through what happens when nums[mid]=0, 1, or 2.", why: { question: "When nums[mid]=2 and we swap with high, why don't we advance mid?", options: ["Bug — should advance","After swap: nums[mid] has unknown value (what was at high). Must examine nums[mid] again before advancing. So only high-- without mid++.","We always advance mid"], correct: 1, explanation: "Swap(mid,high), high--: the value now at mid came from the unprocessed region (nums[high]). It's unknown — must process it next. Don't advance mid. When nums[mid]=0 or 1: known region so can advance mid." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "One-pass Dutch National Flag vs two-pass counting sort — both O(n) O(1). When does one pass matter?", why: { question: "Practical advantage of one-pass over two-pass?", options: ["No practical difference","Cache locality: scan array once (one-pass) vs twice (two-pass). In memory-constrained environments or with streaming data, one-pass is essential.","Two-pass is always better"], correct: 1, explanation: "Streaming data: one-pass processes elements as they arrive. Cache: array read once vs twice. For large arrays or streaming, fewer passes matter. Algorithmic elegance too." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity? Why does each element get examined at most twice?", why: { question: "Why O(n) despite the seemingly complex swap logic?", options: ["O(n log n) hidden","mid advances or high decreases each iteration. Total iterations ≤ 2n (each element processed at most twice). O(n).","O(n²) due to swaps"], correct: 1, explanation: "Each iteration: either mid advances or high decreases. Both move monotonically. Total steps ≤ n for mid (0 to n-1) + n for high (n-1 to 0) = 2n. O(n)." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "Array=[1]: what happens?", options: ["Error","mid=0, high=0. nums[mid]=1: mid++. mid>high: exit. Array stays [1]. Correct.","Infinite loop"], correct: 1, explanation: "Single element 1: low=0, mid=0, high=0. nums[mid]=1: mid++. Now mid=1>high=0: loop exits. Array [1] unchanged. Correct." } },
    ],
    dryrun: { question: "nums=[2,0,2,1,1,0]. low=0,mid=0,high=5. nums[mid]=2: swap(0,5)→[0,0,2,1,1,2], high=4. nums[mid=0]=0: swap(0,0)→same, low=1,mid=1. nums[mid=1]=0: swap(1,1)→same, low=2,mid=2. nums[mid=2]=2: swap(2,4)→[0,0,1,1,2,2], high=3. nums[mid=2]=1: mid=3. nums[mid=3]=1: mid=4. mid>high=3. Done.", options: ["[0,0,1,1,2,2]","[0,1,1,0,2,2]","[0,0,2,1,1,2]"], correct: 0, explanation: "Final array: [0,0,1,1,2,2]. Three-way partition correctly sorted all 0s to front, 2s to back, 1s in middle in a single pass." },
    pushback: { challenge: "What if there are 4 colors (0,1,2,3)?", options: ["Two passes: two-color partition twice. Or: four-pointer approach (complex). Or counting sort O(1) space. Extend DNF is non-trivial.","Same DNF algorithm with 4 pointers","Sort normally"], correct: 0, explanation: "4 colors: no clean one-pass extension. Counting sort (count 0s,1s,2s,3s, overwrite) is simplest: O(n) time O(1) space, 2 passes. Generalizable but elegant DNF approach doesn't extend cleanly." },
    followup: { question: "What if you needed to do this for an array with k distinct values?", options: ["k-way quicksort partition (O(n log k) if k buckets), or counting sort O(n + k) time O(k) space. Counting sort is often simplest.","Same DNF","Binary sort"], correct: 0, explanation: "k colors: counting sort O(n) time O(k) space (count each value, reconstruct). Radix sort for large ranges. DNF generalizes poorly beyond 3 partitions." },
    solution: { approach: "Dutch National Flag (Three Pointers)", steps: ["low=0, mid=0, high=n-1","While mid <= high:","If nums[mid]==0: swap(low,mid), low++, mid++","If nums[mid]==1: mid++","If nums[mid]==2: swap(mid,high), high-- (don't advance mid)"], time: "O(n)", space: "O(1)" },
    keywords: {
      understand: ["0","1","2","red","white","blue","in-place","linear","three values"],
      brute: ["counting","count0","count1","count2","two pass","O(1)","fill","reconstruct"],
      pattern: ["low","mid","high","three pointers","invariant","partition","unprocessed"],
      algorithm: ["swap","low++","mid++","high--","invariant","0s","1s","2s"],
      tradeoff: ["one pass","two pass","streaming","cache","DNF","locality","elegant"],
      complexity: ["O(n)","time","O(1)","space","monotonically","2n","iterations"],
      edge: ["single","all same","already sorted","all zeros","all twos"],
    },
  },
];
