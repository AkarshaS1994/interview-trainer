export const INTERVAL_PROBLEMS = [
  {
    id: "iv1", title: "Merge Intervals", difficulty: "Medium", pattern: "Intervals",
    companies: ["Meta","Amazon","Google"], minLevel: 3,
    description: "Given an array of intervals, merge all overlapping intervals and return the resulting non-overlapping intervals.",
    example: { input: "intervals=[[1,3],[2,6],[8,10],[15,18]]", output: "[[1,6],[8,10],[15,18]]" },
    hints: ["Sort intervals by start time.", "Merge current interval with last in output if they overlap.", "Overlap condition: current.start <= last.end."],
    clarify: { question: "What would you clarify?", options: ["What does 'overlapping' mean — touching endpoints count?","Is the output order important?","Are intervals given in sorted order?","Can intervals be empty or have start > end?","Return merged intervals in any order or sorted?"], mustAsk: [0,2,3], explanation: "[1,4] and [4,5] overlap (touch at 4 — merge to [1,5]). Output sorted by start. Input may be unsorted. Valid intervals: start <= end. Return sorted by start." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "When do two intervals overlap? Draw the 4 possible configurations.", why: { question: "When do intervals [a,b] and [c,d] with a≤c overlap?", options: ["Always when a<c","When c <= b (the second interval starts before the first ends). Overlap: [a, max(b,d)].","When a+b > c+d"], correct: 1, explanation: "After sorting by start: if intervals[i].start <= intervals[i-1].end — they overlap (or touch). Merge: [prev.start, max(prev.end, curr.end)]. No overlap if curr.start > prev.end." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you merge without sorting — repeatedly scan for overlapping pairs?", why: { question: "Why is unsorted repeated-scan approach O(n²)?", options: ["One scan is enough","Each pass might only merge one pair. Up to n pairs, each requiring an O(n) scan = O(n²) total. Also modifies list mid-iteration (messy).","Sort is the only O(n log n) part"], correct: 1, explanation: "Without sort: must compare all pairs — O(n²). Each merge may create new overlaps requiring re-scan. Sort first: single O(n) linear scan suffices after O(n log n) sort." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Why does sorting by start time enable a single linear scan?", why: { question: "Why does sort-by-start mean you only need to compare consecutive intervals?", options: ["You still need to compare all pairs","After sorting: any interval can only overlap with the immediately previous merged interval (not anything earlier). Proof: if c>b and d>b, then d>c>b — already handled.","Adjacent only by coincidence"], correct: 1, explanation: "Key insight: after sort by start, if interval i doesn't overlap with merged[last], neither will any earlier merged interval (since merged intervals are sorted and non-overlapping). Only need to compare with last." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: sort, then for each interval either merge into last output or append.", why: { question: "How do you merge: take max of ends or min?", options: ["min(prev.end, curr.end)","max(prev.end, curr.end) — merged interval must cover BOTH original intervals. Larger end wins.","curr.end always replaces prev.end"], correct: 1, explanation: "Merge: new interval is [prev.start, max(prev.end, curr.end)]. Example: [1,10] and [2,5] → [1,10] (not [1,5]). The merged interval can't shrink." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Sort-then-merge O(n log n) — can you do O(n) without sorting?", why: { question: "Can you merge intervals in O(n) without sorting?", options: ["No — impossible","If start times have bounded range (e.g. 0..10^4): use counting sort, then scan. O(n) time but O(range) space — only practical for small ranges.","Yes with hash map"], correct: 1, explanation: "Counting sort on start times: O(n + range). For large ranges (0..10^9): not practical. Comparison sort O(n log n) is the general solution. O(n) is only achievable with range constraints." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(n) space for output?", options: ["Always one merged interval","Output can have up to n intervals (no overlaps: each becomes its own). Must store all: O(n) space.","O(log n)"], correct: 1, explanation: "No overlapping intervals: n intervals → n in output. O(n) space for output. Sort: O(n log n). Merge scan: O(n). Total: O(n log n) time, O(n) space." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "Single interval [[1,5]] — output?", options: ["[]","[[1,5]] — already merged. No overlaps to process. Append to output.","Error"], correct: 1, explanation: "Single interval: sort is no-op. Loop: output=[[1,5]], first interval appended directly. Return [[1,5]]. Also: all intervals identical [[1,3],[1,3]] → [[1,3]]." } },
    ],
    dryrun: { question: "intervals=[[1,3],[2,6],[8,10],[15,18]] sorted. output=[]. Process [1,3]: output=[[1,3]]. Process [2,6]: 2<=3 overlap → merge to [1,max(3,6)]=[1,6]. Process [8,10]: 8>6 no overlap → append. Process [15,18]: 15>10 → append. Output?", options: ["[[1,6],[8,10],[15,18]]","[[1,6],[8,18]]","[[1,3],[2,6],[8,10],[15,18]]"], correct: 0, explanation: "Final output: [[1,6],[8,10],[15,18]]. [1,3] and [2,6] merged. [8,10] and [15,18] don't overlap with each other or previous. Three merged intervals." },
    pushback: { challenge: "What if you need to insert a new interval into an already merged list (Insert Interval)?", options: ["Binary search for insertion point, merge with all overlapping intervals. O(n) since merging may touch many intervals. O(log n) to find start, O(k) to merge k overlapping.","Re-run full merge — O(n log n)","Can't insert into merged list efficiently"], correct: 0, explanation: "Insert Interval: find where new interval starts (binary search or linear). Merge with all overlapping intervals one pass. Overall O(n) — must shift intervals in worst case." },
    followup: { question: "How would you find the total covered length of all merged intervals?", options: ["Sum (end-start) for each merged interval after merging step. O(n).","Sum all original intervals","Max end - min start"], correct: 0, explanation: "After merging: total covered = sum of (merged[i][1] - merged[i][0]) for all i. Single pass over merged output. O(n)." },
    solution: { approach: "Sort + Linear Merge", steps: ["Sort intervals by start time","result=[]","For each interval: if result empty or curr.start > result.last.end: append","Else: result.last.end = max(result.last.end, curr.end)","Return result"], time: "O(n log n)", space: "O(n)" },
  },
  {
    id: "iv2", title: "Meeting Rooms II", difficulty: "Medium", pattern: "Intervals",
    companies: ["Meta","Amazon","Google"], minLevel: 3,
    description: "Given meeting intervals [start, end], return the minimum number of conference rooms required so no two meetings in the same room overlap.",
    example: { input: "intervals=[[0,30],[5,10],[15,20]]", output: "2 (rooms: {[0,30]}, {[5,10],[15,20]})" },
    hints: ["Sort by start time. Use a min-heap of end times.", "For each meeting: if earliest ending room is free (end ≤ current start), reuse it.", "Heap size at end = minimum rooms needed."],
    clarify: { question: "What would you clarify?", options: ["Does end time include or exclude endpoint? ([0,30] and [30,40] — same room?)?","Can meetings start exactly when another ends?","Return room count or actual assignment?","Empty input — return 0?","Are intervals given sorted?"], mustAsk: [0,1,3], explanation: "Typically [0,30] and [30,40] can reuse same room (end is exclusive or meetings back-to-back). Return room count only. Empty → 0. Input not necessarily sorted." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What is the connection between 'min rooms needed' and 'max overlapping meetings at any instant'?", why: { question: "Why does min rooms = max concurrent meetings?", options: ["Always 2 rooms","Each overlapping meeting needs its own room at that instant. Max concurrent meetings = the peak load = minimum rooms to handle the peak.","Can share rooms by waiting"], correct: 1, explanation: "At the busiest moment, k meetings overlap simultaneously. Each needs a distinct room. Before/after that peak: rooms can be reused. So min rooms = max k = max number of simultaneously ongoing meetings." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you sweep through all time points checking concurrent meetings?", why: { question: "Why is time-sweep brute force potentially O(n × T) where T is time range?", options: ["O(n²)","For each time unit in [0, maxEndTime]: count active meetings. T can be 10^9+ (large range) × n meetings = slow.","O(n log n)"], correct: 1, explanation: "Sweep all time units: O(T × n). For T=10^5, n=10^4: 10^9 ops. Events-based sweep: O(n log n) — only care about start/end events, not every time unit." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Why does a min-heap of end times efficiently track which rooms are free?", why: { question: "What does the min-heap top represent?", options: ["Next meeting to start","The room whose meeting ends EARLIEST. If that meeting ends before current meeting starts: that room is free to reuse.","Total rooms used"], correct: 1, explanation: "Min-heap: tracks all active meetings by end time. Minimum = earliest room to free up. Compare to current meeting's start: if heap.top ≤ curr.start, room is available. Heap size = rooms currently occupied." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: sort by start, for each meeting check heap top, pop if free, push current end.", why: { question: "Why push current meeting's END time (not start) to the heap?", options: ["Mistake — push start time","Heap tracks when each room becomes FREE (end time). Comparing future meeting's start to heap top checks room availability.","Push both start and end"], correct: 1, explanation: "Push end time: when future meeting checks, it asks 'is any room free?' = 'does any room end before I start?' = 'is min(heap) ≤ my start?'. End times are the freeing events." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Min-heap O(n log n) vs sorting starts and ends separately — are both valid?", why: { question: "How does the two-sorted-arrays approach work?", options: ["Invalid — must track which room","Sort starts and ends separately. Two pointers: if start < earliest_end: need new room. Else: free a room (advance end pointer). O(n log n), O(n) space.","Only heap approach works"], correct: 1, explanation: "Two arrays trick: sort starts[], sort ends[]. i=j=0, rooms=0. While i<n: if starts[i]<ends[j]: rooms++; i++. Else: j++ (room freed); i++. max rooms = answer. No heap needed." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space of min-heap approach?", why: { question: "Why O(n log n)?", options: ["O(n) — one pass","Sort: O(n log n). For each of n meetings: heap push/pop O(log n). Total: O(n log n).","O(n²)"], correct: 1, explanation: "Sort: O(n log n). Loop: n iterations × O(log n) heap ops = O(n log n). Total O(n log n). Space: O(n) for heap (all meetings could overlap)." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "All meetings overlap (e.g. [[1,10],[1,10],[1,10]]): answer?", options: ["1 — same time","3 — all 3 overlap at every moment. Each needs its own room.","2"], correct: 1, explanation: "All identical meetings: sort is no-op. Each meeting: heap top (previous end=10) > start=1, so new room needed. Heap grows to size 3. Return 3." } },
    ],
    dryrun: { question: "intervals=[[0,30],[5,10],[15,20]] sorted by start. heap=[]. [0,30]: heap empty → push 30. heap=[30], rooms=1. [5,10]: 30>5 → new room, push 10. heap=[10,30], rooms=2. [15,20]: min heap=10 ≤ 15 → pop 10, push 20. heap=[20,30]. rooms stays?", options: ["2 — rooms=2 (never increased beyond 2)","3","1"], correct: 0, explanation: "[15,20] reuses the room that freed at 10. Pop 10, push 20. Heap size stays 2. Max heap size throughout = 2. Answer = 2 rooms." },
    pushback: { challenge: "What if you also need to OUTPUT which meetings go in which room?", options: ["Assign room IDs alongside heap: heap stores (endTime, roomId). When reusing, assign same roomId. Track assignment array. Same O(n log n).","Can't track assignments with heap","Sort first, then assign greedily"], correct: 0, explanation: "Track assignments: heap stores (end_time, room_id). When room reused: assign same room_id to current meeting. When new room: room_id++. Store mapping meeting→room. Same complexity." },
    followup: { question: "What is the maximum number of non-overlapping meetings you can schedule in a single room?", options: ["Greedy: sort by END time, greedily pick meetings that don't overlap. Activity Selection Problem. O(n log n).","Pick shortest meetings","Use DP: O(n²)"], correct: 0, explanation: "Activity Selection: sort by end time. Greedily pick earliest-ending meeting, skip overlapping ones. Maximizes count. O(n log n). Classic greedy — same structure as interval scheduling." },
    solution: { approach: "Sort + Min-Heap of End Times", steps: ["Sort intervals by start time","heap = min-heap of end times","For each interval: if heap.top <= interval.start: pop (reuse room)","Push interval.end to heap","Return heap.size()"], time: "O(n log n)", space: "O(n)" },
  },
];
