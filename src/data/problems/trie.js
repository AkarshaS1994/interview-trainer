export const TRIE_PROBLEMS = [
  {
    id: "tri1", title: "Implement Trie", difficulty: "Medium", pattern: "Trie",
    companies: ["Meta","Amazon","Google"], minLevel: 3,
    description: "Implement a Trie (prefix tree) with insert, search, and startsWith operations. search returns true if the word is in the trie. startsWith returns true if any inserted word has the given prefix.",
    example: { input: 'insert("apple"), search("apple")→true, search("app")→false, startsWith("app")→true', output: "Implement all three in O(word length)" },
    hints: ["Each TrieNode has an array/map of 26 children and a boolean isEnd.", "insert: walk/create nodes for each character.", "search differs from startsWith only by checking isEnd at the last node."],
    clarify: { question: "What would you clarify?", options: ["Only lowercase English letters?","Is search case-sensitive?","Can the same word be inserted multiple times?","What if we search before any insert?","Maximum word length / number of words?"], mustAsk: [0,1,3], explanation: "Only lowercase letters a-z. Case-sensitive (all lowercase). Duplicate inserts are benign — isEnd already true. Search on empty trie returns false. Typical constraints: word length ≤26, up to 3×10^4 operations." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What problem does a Trie solve that a HashSet cannot? What is the key advantage?", why: { question: "What can a Trie do that a HashSet cannot efficiently?", options: ["Nothing — HashSet is always better","Prefix queries: 'does any word start with prefix X?' HashSet: O(n×L) scan. Trie: O(L) where L=prefix length. Trie shares prefixes among words.","Trie is faster for exact lookup"], correct: 1, explanation: "HashSet: exact lookup O(L), but prefix search O(n×L) (check all n words). Trie: prefix traversal O(L) regardless of word count. Trie excels at autocomplete, spell-check, IP routing." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would a list of strings implement insert, search, startsWith? What's the cost?", why: { question: "Why is list-based search O(n×L) while Trie is O(L)?", options: ["Both are O(n×L)","List: scan all n words, compare character by character up to L. Trie: follow path for L characters, done — no scan of other words.","Trie is O(n)"], correct: 1, explanation: "List search: must check each of n words. For search and startsWith: O(n×L) worst case. Trie: path for the query word is exactly L nodes — O(L) regardless of how many words are stored." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Describe the TrieNode structure. What does each node represent?", why: { question: "What does a single TrieNode represent?", options: ["A whole word","One character in a path from root. Root represents empty prefix. Node at depth d represents d-character prefix from root to this node.","A character and its count"], correct: 1, explanation: "Node = one character position. Path from root to node = a prefix. isEnd flag = whether any inserted word ends here. children[0..25] = possible next characters." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through insert('cat') and search('ca') step by step.", why: { question: "What is the only difference between search and startsWith?", options: ["startsWith is always true","search checks isEnd==true at last character's node. startsWith only checks the node exists — doesn't care about isEnd.","No difference"], correct: 1, explanation: "Both: traverse nodes for each character, return false if any node missing. search: AND check isEnd at final node. startsWith: return true if traversal succeeds, regardless of isEnd." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Array of 26 children vs HashMap children — when do you prefer each?", why: { question: "When is HashMap preferable to array[26] for children?", options: ["Always use array","When alphabet is large or sparse (e.g., Unicode, arbitrary strings): HashMap uses space proportional to actual children, not full alphabet. Array wastes space if alphabet is large.","Always use HashMap"], correct: 1, explanation: "Fixed small alphabet (26 lowercase): array[26] is O(26)=O(1) per node, fast index access. Large/variable alphabet: HashMap saves space. Both O(L) for insert/search." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity for insert, search, startsWith?", why: { question: "Why all three operations are O(L) where L = word/prefix length?", options: ["O(n×L)","Each op traverses exactly L nodes, one per character. No backtracking, no scanning other words. O(L) per operation.","O(1) with hash"], correct: 1, explanation: "Single path traversal of L nodes per operation. Each node visit O(1) (array index or hash lookup). Total per operation: O(L). Space: O(n×L) worst case (all unique characters in all words)." } },
      { id: "edge", label: "Edge Cases", emoji: "🎯", prompt: "What edge cases should you handle?", why: { question: "search('') — empty string — what should it return?", options: ["Error","true — root node is always created; isEnd=false by default but empty string is trivially in any trie that exists. Depends on problem: usually false unless explicitly inserted.","false always"], correct: 1, explanation: "Edge cases: empty string (usually returns false for search, true for startsWith — empty is prefix of everything), search before any insert (nodes don't exist → false), word not inserted (nodes may exist from other words → check isEnd)." } },
    ],
    dryrun: { question: 'insert("app"), insert("apple"). Now search("app") and search("apple"). Trie: root→a→p→p(isEnd=true)→l→e(isEnd=true). search("app"): traverse a,p,p. isEnd=true. Return?', options: ["true (isEnd=true at last 'p')","false — 'apple' was also inserted","true for both","false for both"], correct: 2, explanation: 'search("app"): node at second p has isEnd=true → return true. search("apple"): traverse a,p,p,l,e. Node e has isEnd=true → return true. Both correct.' },
    pushback: { challenge: "How would you add a delete operation?", options: ["Traverse to word end, unset isEnd. Then backtrack deleting nodes with no children and isEnd=false. O(L).","Set isEnd=false only — don't remove nodes","Can't delete without full rebuild"], correct: 0, explanation: "Delete: first check word exists. Set isEnd=false at end node. Then backtrack: if node has no children and isEnd=false, delete it (free memory). Stop backtracking if node has other children or isEnd." },
    followup: { question: "How would you implement autocomplete — return all words with a given prefix?", options: ["Traverse to prefix end (O(L)). Then DFS from that node collecting all words with isEnd=true. Total O(L + P) where P = total chars in all matching words.","Return random word with prefix","Can't do with Trie"], correct: 0, explanation: "Autocomplete: prefix traversal O(L) to reach prefix node. DFS from there: collect all paths to isEnd=true nodes. Each such path = one complete word. O(L + total output size)." },
    solution: { approach: "TrieNode with Array Children", steps: ["TrieNode: children=array[26], isEnd=false","insert: for each char, create node if missing, walk to next","search: walk chars, return false if missing, return node.isEnd at end","startsWith: walk chars, return false if missing, return true at end"], time: "O(L) per operation", space: "O(n×L) total" },
    keywords: {
      understand: ["prefix","HashSet","O(L)","search","autocomplete","n×L","shared","trie"],
      brute: ["list","scan","n words","O(n×L)","check","every","word"],
      pattern: ["TrieNode","children","isEnd","depth","path","prefix","array[26]"],
      algorithm: ["insert","search","startsWith","isEnd","traverse","create","missing"],
      tradeoff: ["array[26]","HashMap","alphabet","sparse","Unicode","space","O(1)"],
      complexity: ["O(L)","time","O(n×L)","space","path","traversal","single"],
      edge: ["empty string","search before insert","duplicate","not inserted","isEnd"],
    },
  },
  {
    id: "tri2", title: "Search Suggestions System", difficulty: "Medium", pattern: "Trie",
    companies: ["Amazon","Meta"], minLevel: 3,
    description: "Given a list of products and a search word, return a list of lists: for each prefix of searchWord, return up to 3 lexicographically smallest products with that prefix.",
    example: { input: 'products=["mobile","mouse","moneypot","monitor","mousepad"], searchWord="mouse"', output: '[["mobile","moneypot","monitor"],["mobile","moneypot","monitor"],["mouse","mousepad"],["mouse","mousepad"],["mouse","mousepad"]]' },
    hints: ["Sort products lexicographically first — then Trie or binary search both work.", "Trie: insert all products, then for each prefix traverse and collect up to 3 results.", "Binary search on sorted array: for each prefix, find first word >= prefix, take next 3 that share the prefix."],
    clarify: { question: "What would you clarify?", options: ["At most 3 suggestions per prefix?","Lexicographically smallest 3?","Case-sensitive?","What if fewer than 3 products match?","Can products be duplicated?"], mustAsk: [0,1,3], explanation: "At most 3, lexicographically smallest. Case-sensitive (all lowercase). Fewer than 3: return all that match. Duplicates allowed in input — both can appear in output." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What does 'for each prefix of searchWord' mean? How many queries does that generate?", why: { question: "If searchWord='mouse', how many prefix queries are there?", options: ["1 — just 'mouse'","5 — 'm','mo','mou','mous','mouse'","26 — all prefixes up to length 26"], correct: 1, explanation: "One query per character typed: m, mo, mou, mous, mouse. 5 prefixes for length-5 word. In general: L prefixes for searchWord of length L." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "For each prefix, scan all products and filter. What's the complexity?", why: { question: "Brute force complexity for all prefixes?", options: ["O(n×L) total","O(n×L²) — for each of L prefixes, scan n products checking prefix match up to L chars: L²×n","O(n log n)"], correct: 1, explanation: "L prefixes × n products × L prefix check = O(n×L²). With sort (for lex order): O(n log n + n×L²). For large inputs this is slow." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a Trie avoid redundant prefix work? How does pre-sorting help?", why: { question: "Why sort products before inserting into Trie?", options: ["Sort is required for Trie","Sorted insertion: DFS from prefix node encounters products in lex order. Stop DFS after collecting 3 — no need to visit entire subtree.","No need to sort"], correct: 1, explanation: "Pre-sort: DFS from prefix node collects suggestions in lex order automatically. Stop after 3 found. Without sort: must collect all then sort — more work." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Alternative: binary search on sorted array — how does it work without a Trie?", why: { question: "How does binary search find all products with a given prefix?", options: ["Binary search finds one, linear scan finds rest","Binary search: find first index i where products[i] >= prefix. Then scan i, i+1, i+2, ... while product still has the prefix. At most 3. O(log n + 3L).","Can't binary search for prefixes"], correct: 1, explanation: "Sort products. For prefix p: find first product >= p. Check products[i].startsWith(p). Take at most 3 consecutive products that pass. Binary search O(log n), check 3 × O(L). Clean, no Trie needed." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Trie vs sorted array + binary search — when would you choose Trie in production?", why: { question: "When is Trie preferable over sorted array + binary search?", options: ["Always use Trie","Dynamic products: inserting/deleting products is O(L) in Trie vs O(n) in sorted array (re-sort or insert-in-order). Trie wins when products change frequently.","Always use binary search"], correct: 1, explanation: "Static products: sorted array + binary search is simpler. Dynamic (insertions/deletions): Trie O(L) insert vs sorted array O(n). Real autocomplete systems use Trie + frequency ranking." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Compare time complexity: Trie approach vs binary search approach.", why: { question: "Binary search approach time complexity?", options: ["O(n×L²)","Sort O(n log n) + L queries × O(log n + 3L) = O(n log n + L(log n + L))","O(n + L)"], correct: 1, explanation: "Binary search: O(n log n) sort + L prefixes × (O(log n) search + O(3L) prefix check) = O(n log n + L × log n). For fixed L, O(n log n). Trie: O(n×L) build + O(L² + 3×output). Often comparable." } },
      { id: "edge", label: "Edge Cases", emoji: "🎯", prompt: "What edge cases exist?", why: { question: "searchWord='xyz' and no products start with 'x' — output?", options: ["Error","List of L empty lists: [[], [], [], ...]. Each prefix query returns empty.","null"], correct: 1, explanation: "No matching products: each prefix returns []. Output is L sublists, all empty. Binary search finds no match, returns []. Trie traversal fails at first node, returns []." } },
    ],
    dryrun: { question: 'products=["mobile","mouse","moneypot"] sorted: ["mobile","moneypot","mouse"]. searchWord="mo". Prefix "m": find first >= "m": "mobile". Check startsWith("m"): all 3 match. Take first 3. Prefix "mo": find first >= "mo": "mobile". startsWith("mo"): "mobile"✓, "moneypot"✓, "mouse"✓. Output?', options: ['[["mobile","moneypot","mouse"],["mobile","moneypot","mouse"]]', '[["mobile","moneypot"],["moneypot","mouse"]]', "Error — only 2 prefixes", "[[], []]"], correct: 0, explanation: 'Prefix "m": ["mobile","moneypot","mouse"] (all start with m, take 3). Prefix "mo": ["mobile","moneypot","mouse"] (all start with mo, take 3). Correct first two outputs.' },
    pushback: { challenge: "What if you need top 3 by FREQUENCY (most searched), not lexicographic order?", options: ["Store frequency counts. Use Trie with max-heap per node (top-3 frequent). Or: sort by frequency desc before insert. More complex but same O(L) query.","Can't rank by frequency with Trie","Sort by frequency descending then take top 3 per prefix linearly"], correct: 0, explanation: "Frequency-ranked: each Trie node stores top-3 by frequency (small heap). Insert updates heaps along path. Query: return heap at prefix node. O(L×3 log 3) per insert, O(L) per query." },
    followup: { question: "How would you handle the case where searchWord can contain typos (fuzzy matching)?", options: ["Trie + BFS/DFS with edit distance budget. At each node, allow up to k character mismatches. O(n×26^k) worst case — feasible for k=1.","Only exact Trie match","Sort handles typos"], correct: 0, explanation: "Fuzzy Trie search: DFS from root with (node, remaining_budget). At each level: match = recurse normally, mismatch = recurse with budget-1. Collect words at isEnd nodes. Exponential in k but k is small (≤2 in practice)." },
    solution: { approach: "Sort + Binary Search", steps: ["Sort products lexicographically","For each prefix p of searchWord: binary search first index >= p","Scan up to 3 products checking startsWith(p)","Append matches to result"], time: "O(n log n + L²)", space: "O(1) extra" },
    keywords: {
      understand: ["prefix","L","searchWord","queries","per character","typed","suggestions"],
      brute: ["O(n×L²)","scan","filter","every","check","prefix","sort"],
      pattern: ["sorted","DFS","3","lex order","stop","pre-sort","trie"],
      algorithm: ["binary search","first","startsWith","3","scan","consecutive","O(log n)"],
      tradeoff: ["static","dynamic","O(L)","O(n)","Trie","sorted array","insert"],
      complexity: ["O(n log n)","L","log n","3L","prefix","binary search","sort"],
      edge: ["no match","empty","L empty lists","not found","xyz"],
    },
  },
  {
    id: "tri3", title: "Longest Word in Dictionary", difficulty: "Easy", pattern: "Trie",
    companies: ["Google","Amazon"], minLevel: 1,
    description: "Given an array of strings words, return the longest word in words that can be built one character at a time by other words in words. If there is more than one possible answer, return the longest word with the smallest lexicographical order. If there is no answer, return ''.",
    example: { input: 'words = ["w","wo","wor","word","world"]', output: '"world" — each prefix "w","wo","wor","word" is in the list' },
    hints: ["Sort words first — lexicographic order means shorter equal-length ties are handled automatically.", "Insert all words into a Trie, marking isEnd. Then DFS/BFS only along nodes where isEnd=true.", "At each isEnd node, record the word if it's longer than the current best."],
    clarify: {
      question: "What would you clarify before coding?",
      why: "Clarifying ties and what 'buildable' means prevents wrong output.",
      answer: "Only lowercase letters. Tie-breaking: longest word; among same-length, lexicographically smallest. A word is buildable if every strict prefix is also in words.",
    },
    steps: [
      {
        id: "understand", label: "Understand", emoji: "🎯",
        prompt: "What does 'built one character at a time' mean? Give an example where a word fails.",
        why: "Grounding the constraint prevents trying to build from non-existent prefixes.",
        options: [
          "Every prefix of the word (including the word itself) must be in the input array",
          "Just the first and last characters must be in the array",
          "The word must be the concatenation of two other words",
          "The word must appear at least twice",
        ],
        correct: 0,
      },
      {
        id: "brute", label: "Brute Force", emoji: "💪",
        prompt: "How would you check every word naively? What is the complexity?",
        why: "Seeing O(n²×L) motivates a better structure.",
        options: [
          "For each word, check if every prefix is in a set. O(n×L) per word → O(n²×L) total",
          "Sort all words, return the last one",
          "BFS from every character in the alphabet",
          "Check pairs of words for substring match",
        ],
        correct: 0,
      },
      {
        id: "pattern", label: "Pattern", emoji: "🔍",
        prompt: "How does a Trie encode 'all prefixes exist' without re-checking each one?",
        why: "The Trie path from root to any node IS the set of all prefixes — isEnd flags tell us which are real words.",
        options: [
          "Each path root→node is a prefix; if every node on that path has isEnd=true, every prefix is a valid word",
          "Trie stores frequencies, pick the most frequent",
          "Trie is unnecessary — a sorted array suffices",
          "Only check leaf nodes for isEnd",
        ],
        correct: 0,
      },
      {
        id: "algorithm", label: "Algorithm", emoji: "⚙️",
        prompt: "Walk through: insert all words, then BFS/DFS only visiting isEnd nodes. Why BFS level-by-level works well here.",
        why: "BFS processes shorter words first; we only expand a node if the current word is valid — equivalent to checking all prefixes.",
        options: [
          "BFS: start at root, enqueue children with isEnd=true, track word at each node. Deepest reachable isEnd node = answer",
          "DFS without any isEnd check",
          "Sort descending by length, return first word whose prefixes all exist in a set",
          "Use dynamic programming on word lengths",
        ],
        correct: 0,
      },
      {
        id: "tradeoff", label: "Trade-off", emoji: "⚖️",
        prompt: "HashSet approach vs Trie: which is simpler? Which is faster?",
        why: "For this specific problem a HashSet is often cleaner; Trie shines when prefix queries are more complex.",
        options: [
          "HashSet: sort words, iterate — O(n log n + n×L). Trie: build O(n×L) + BFS O(n×L). Similar complexity; HashSet simpler to code",
          "Trie is always O(1) and HashSet is always O(n)",
          "HashSet cannot solve this problem",
          "Trie requires O(n²) space",
        ],
        correct: 0,
      },
      {
        id: "complexity", label: "Complexity", emoji: "📊",
        prompt: "What is the time and space complexity of the Trie approach?",
        why: "n = number of words, L = max word length.",
        options: [
          "Time O(n×L) to build + O(n×L) BFS = O(n×L). Space O(n×L) for the Trie",
          "Time O(n²), Space O(n)",
          "Time O(n log n), Space O(1)",
          "Time O(L²), Space O(n×L²)",
        ],
        correct: 0,
      },
      {
        id: "edge", label: "Edge Cases", emoji: "🎯",
        prompt: "What if no word is buildable? What if two words share the same length?",
        why: "Return '' for no answer; sort ensures lex-smallest is encountered first in BFS.",
        options: [
          "No buildable word → return ''. Equal length tie → sort lexicographically first, BFS picks lex-smallest among equals automatically",
          "Return null on no answer",
          "Return the first word in the input array",
          "Ties are impossible if input has no duplicates",
        ],
        correct: 0,
      },
    ],
    dryrun: {
      input: 'words = ["a","banana","app","appl","ap","apply","apple"]',
      steps: [
        { label: "Sort words", state: '["a","ap","app","appl","apple","apply","banana"]' },
        { label: "Insert all into Trie, mark isEnd", state: "Trie built with isEnd at each word's last node" },
        { label: "BFS from root, only visit isEnd nodes", state: 'Visit "a"(len1), "ap"(len2), "app"(len3), "appl"(len4)' },
        { label: "From appl node, both apple and apply are children with isEnd", state: 'Best so far: "appl" (len4)' },
        { label: "Expand apple(len5) and apply(len5) — both reachable", state: '"apple" < "apply" lex — apple wins' },
      ],
      question: 'After BFS, "banana" node is reachable only if "b","ba","ban","bana","banan" are in words. Are they?',
      options: [
        '"banana" is NOT buildable — its prefixes are not in the input; answer is "apple"',
        '"banana" is buildable because it appears in the array',
        "Both apple and banana qualify; return the longer one",
        "Return empty string — no word is buildable",
      ],
      correct: 0,
      explanation: '"banana" requires "b","ba","ban","bana","banan" — none are in the list. "apple" is buildable via "a"→"ap"→"app"→"appl"→"apple". Answer: "apple".',
    },
    pushback: {
      question: "Could you solve this without a Trie, using only a HashSet?",
      answer: "Yes: sort words alphabetically. Iterate; for each word check if every prefix is in a HashSet. If yes, update answer. O(n×L) after sort. Trie avoids repeated prefix lookups but both are O(n×L) in practice.",
    },
    followup: {
      question: "What if you wanted the k longest buildable words, not just the longest one?",
      answer: "BFS: track all words at the deepest reachable levels. Or: collect all buildable words during BFS, sort by (-length, lex order), return first k. O(n×L + n log n).",
    },
    solution: {
      approach: "Trie + BFS on isEnd nodes",
      steps: [
        "Insert all words into Trie, set isEnd at each word's terminal node",
        "BFS from root; enqueue children only if isEnd=true",
        "Track longest word seen (by depth, break ties lexicographically — sort first)",
        "Return best word or '' if queue empties without finding any",
      ],
      time: "O(n×L)",
      space: "O(n×L)",
    },
    keywords: {
      understand: ["prefix","buildable","every prefix","input array","one character at a time","strict prefix"],
      brute: ["O(n²×L)","check every word","every prefix","set lookup","naive","scan"],
      pattern: ["Trie","isEnd","path","BFS","DFS","prefix-encoded","root to node"],
      algorithm: ["BFS","isEnd","enqueue","deepest","level","longest","lex smallest"],
      tradeoff: ["HashSet","sort","O(n×L)","simpler","Trie","same complexity","cleaner"],
      complexity: ["O(n×L)","build","BFS","space","Trie nodes","n words","L length"],
      edge: ["no answer","empty string","tie","same length","lex order","sort first"],
    },
  },
  {
    id: "tri4", title: "Replace Words", difficulty: "Medium", pattern: "Trie",
    companies: ["Meta","Amazon"], minLevel: 3,
    description: "In English, we have a concept called root, which can be followed by some other word to form another longer word — called a successor. Given a dictionary of roots and a sentence, replace all the successors in the sentence with the root. If a successor can be replaced by more than one root, use the shortest root.",
    example: { input: 'dictionary = ["cat","bat","rat"], sentence = "the cattle was rattled by the battery"', output: '"the cat was rat by the bat"' },
    hints: ["Build a Trie of all roots. For each word in the sentence, traverse the Trie until you hit an isEnd marker.", "The first isEnd encountered is the shortest root — return it immediately.", "If you exhaust the Trie without hitting isEnd, keep the original word."],
    clarify: {
      question: "What would you clarify before starting?",
      why: "Understanding tie-breaking and sentence structure avoids edge-case bugs.",
      answer: "Use shortest matching root. Sentence words are lowercase, space-separated. If no root matches, keep the word as-is.",
    },
    steps: [
      {
        id: "understand", label: "Understand", emoji: "🎯",
        prompt: "What does 'shortest root' mean? If roots are 'cat' and 'ca', which replaces 'cattle'?",
        why: "The problem asks for the shortest matching root — the first isEnd hit during Trie traversal.",
        options: [
          "'ca' replaces 'cattle' because it is shorter and matches the start of 'cattle'",
          "'cat' always wins because it is more specific",
          "Both replace — return the lexicographically smaller one",
          "Only exact word matches are replaced",
        ],
        correct: 0,
      },
      {
        id: "brute", label: "Brute Force", emoji: "💪",
        prompt: "For each sentence word, check all roots for a prefix match. Complexity?",
        why: "O(words × roots × L) motivates building an index (Trie).",
        options: [
          "O(W × R × L): for each of W words, check each of R roots, compare up to L chars. Sort roots by length to get shortest first",
          "O(W + R) — just concatenate",
          "O(W × L²) — check every substring",
          "O(R log R) — binary search on roots",
        ],
        correct: 0,
      },
      {
        id: "pattern", label: "Pattern", emoji: "🔍",
        prompt: "How does a Trie of roots let you find the shortest matching root in O(L)?",
        why: "The Trie encodes all roots; traversing a word character by character, the first isEnd hit is the shortest root.",
        options: [
          "Traverse Trie along word's characters; first node with isEnd=true is the shortest root. Stop immediately.",
          "Go to the deepest possible node, then backtrack to find isEnd",
          "Use DFS to collect all matching roots, then pick shortest",
          "Hash each root and compare hash of word prefixes",
        ],
        correct: 0,
      },
      {
        id: "algorithm", label: "Algorithm", emoji: "⚙️",
        prompt: "Walk through replacing 'cattle' with roots Trie containing 'cat' and 'ca'.",
        why: "Step-by-step shows why we stop at the first isEnd, not the longest match.",
        options: [
          "Traverse c→a (isEnd=true at 'ca') → return 'ca' immediately without checking further",
          "Traverse c→a→t (isEnd=true at 'cat') → return 'cat'",
          "Traverse full word 'cattle', then backtrack to find shortest match",
          "Return 'cattle' unchanged because multiple roots match",
        ],
        correct: 0,
      },
      {
        id: "tradeoff", label: "Trade-off", emoji: "⚖️",
        prompt: "Why is the Trie approach better than sorting roots by length and scanning?",
        why: "Sorting and scanning is O(R×L) per word; Trie is O(L) per word regardless of R.",
        options: [
          "Trie: O(L) per word query independent of R. Sorted scan: O(R×L) per word. Trie wins when R is large.",
          "Sorted scan is always faster due to cache locality",
          "Both are O(1) with hashing",
          "Trie only helps when all roots share a common prefix",
        ],
        correct: 0,
      },
      {
        id: "complexity", label: "Complexity", emoji: "📊",
        prompt: "What is the total time complexity? Let R = roots, W = sentence words, L = max length.",
        why: "Build Trie once in O(R×L); query each sentence word in O(L).",
        options: [
          "Build O(R×L) + query O(W×L) = O((R+W)×L)",
          "O(R×W×L) — check every root against every word",
          "O(W log R) — binary search per word",
          "O(R×L²) — nested prefix checks",
        ],
        correct: 0,
      },
      {
        id: "edge", label: "Edge Cases", emoji: "🎯",
        prompt: "What happens when no root matches? When a root itself appears as a sentence word?",
        why: "Graceful fallback and self-matching roots are common in interviews.",
        options: [
          "No match → keep original word. Root appears as word → root matches itself at isEnd, replace with itself (no change)",
          "No match → return empty string",
          "Root as word → skip replacement to avoid trivial matches",
          "No match → replace with the shortest root in dictionary",
        ],
        correct: 0,
      },
    ],
    dryrun: {
      input: 'dictionary=["bat","ba"], sentence="battery"',
      steps: [
        { label: "Build Trie from roots", state: "root → b → a(isEnd=true for 'ba') → t(isEnd=true for 'bat')" },
        { label: "Process word 'battery': traverse b", state: "Node b exists, not isEnd" },
        { label: "Traverse a", state: "Node a exists, isEnd=true ('ba' is a root)" },
        { label: "isEnd hit at depth 2 — stop immediately", state: "Shortest root = 'ba'" },
      ],
      question: "What does 'battery' get replaced with?",
      options: [
        '"ba" — first isEnd encountered during traversal',
        '"bat" — longer but more specific root',
        '"battery" — no replacement because full word not in dictionary',
        '"b" — shortest possible prefix',
      ],
      correct: 0,
      explanation: "Traversal hits isEnd at node 'a' (depth 2, word 'ba') before reaching 'bat'. Return 'ba' immediately — shortest root wins.",
    },
    pushback: {
      question: "What if the dictionary has thousands of roots — does your approach still scale?",
      answer: "Yes. Trie build is O(R×L) once. Each sentence word query is O(L) regardless of R. Total O((R+W)×L) — linear in total characters. Brute force is O(W×R×L) which degrades badly with large R.",
    },
    followup: {
      question: "How would you modify this to replace with the LONGEST matching root instead of shortest?",
      answer: "Instead of stopping at first isEnd, continue traversal to the end of the word, remembering the last isEnd node seen. O(L) still — just don't stop early. Return the deepest isEnd reached.",
    },
    solution: {
      approach: "Trie of roots + linear scan",
      steps: [
        "Insert every root into Trie, mark isEnd",
        "Split sentence into words",
        "For each word: traverse Trie character by character; return word-so-far at first isEnd",
        "If traversal fails (no node) or reaches end without isEnd, keep original word",
        "Join results with spaces",
      ],
      time: "O((R+W)×L)",
      space: "O(R×L) for Trie",
    },
    keywords: {
      understand: ["root","successor","shortest","prefix match","replace","stop early","isEnd"],
      brute: ["O(W×R×L)","scan","every root","sort by length","compare","prefix"],
      pattern: ["Trie","isEnd","first match","shortest root","traverse","stop","O(L)"],
      algorithm: ["traverse","isEnd","stop immediately","depth","replace","join","split"],
      tradeoff: ["O(L)","O(R×L)","Trie vs sorted scan","large R","independent of R","build once"],
      complexity: ["O((R+W)×L)","build","query","W words","R roots","L length","linear"],
      edge: ["no match","keep original","root as word","self-match","empty dictionary"],
    },
  },
  {
    id: "tri5", title: "Design Add and Search Words", difficulty: "Medium", pattern: "Trie",
    companies: ["Meta","Google"], minLevel: 3,
    description: "Design a data structure that supports addWord(word) and search(word). search(word) can search a literal word or a regex pattern containing only lowercase letters and '.', where '.' matches any single character.",
    example: { input: 'addWord("bad"), addWord("dad"), addWord("mad"). search("pad")→false, search(".ad")→true, search("b..")→true', output: "Trie with DFS backtracking at '.' nodes" },
    hints: ["Build a standard Trie for addWord.", "For search, use DFS — at a '.' node, branch into all 26 children.", "Return true as soon as any DFS branch reaches isEnd after consuming the full pattern."],
    clarify: {
      question: "What would you clarify about the search pattern?",
      why: "Knowing '.' only matches exactly one character (not zero or more) simplifies the DFS.",
      answer: "Only '.' and lowercase letters. '.' matches exactly one character. No '*' or multi-char wildcards. Patterns always match words of the same length.",
    },
    steps: [
      {
        id: "understand", label: "Understand", emoji: "🎯",
        prompt: "How does '.' differ from a regex '*'? What does search('b..') match?",
        why: "Precisely scoping '.' prevents over-engineering — no need for full regex.",
        options: [
          "'.' matches exactly one character. 'b..' matches any 3-letter word starting with b: 'bad','bat','big',…",
          "'.' matches zero or more characters like regex '*'",
          "'.' matches only vowels",
          "'.' matches the end of the word",
        ],
        correct: 0,
      },
      {
        id: "brute", label: "Brute Force", emoji: "💪",
        prompt: "Store all words in a list. For search, iterate and match character-by-character. Cost?",
        why: "O(n×L) per search motivates the Trie with early termination.",
        options: [
          "O(n×L) per search: check all n words, compare each char (handle '.' as wildcard). No structure advantage.",
          "O(1) with hashing — hash the pattern",
          "O(log n) with binary search on sorted list",
          "O(L) always — same as Trie",
        ],
        correct: 0,
      },
      {
        id: "pattern", label: "Pattern", emoji: "🔍",
        prompt: "How does a Trie help with literal characters? What changes for '.'?",
        why: "Trie gives O(L) for literals; '.' requires branching but prunes the Trie early when other branches fail.",
        options: [
          "Literal: follow single child in O(1). '.': try all 26 children via DFS — prune branches that fail early.",
          "Use a separate HashMap for wildcard lookups",
          "Convert '.' to every letter and do 26 separate exact searches",
          "Trie cannot handle '.' — need a different structure",
        ],
        correct: 0,
      },
      {
        id: "algorithm", label: "Algorithm", emoji: "⚙️",
        prompt: "Write the DFS search logic. When do you return true? When do you backtrack?",
        why: "Returning true on first success, backtracking on dead ends, is the core of the algorithm.",
        options: [
          "DFS(node, index): if index==len return node.isEnd. If pattern[index]=='.': try all 26 children, return true if any DFS returns true. Else: follow matching child or return false.",
          "BFS with a queue of (node, index) pairs",
          "Convert pattern to regex and use string matching",
          "Memoize (node, index) pairs to avoid recomputation",
        ],
        correct: 0,
      },
      {
        id: "tradeoff", label: "Trade-off", emoji: "⚖️",
        prompt: "Worst case for '....' (all dots) — how many nodes are visited?",
        why: "All-dot patterns expose the exponential branching factor, but bounded by actual Trie nodes.",
        options: [
          "All Trie nodes at depth L are visited: O(26^L) theoretical, but bounded by n×L (only nodes that exist). Sparse Trie makes this manageable.",
          "O(L) always — '.' just picks one child",
          "O(1) — cached from previous searches",
          "O(n²) regardless of L",
        ],
        correct: 0,
      },
      {
        id: "complexity", label: "Complexity", emoji: "📊",
        prompt: "Complexity of addWord and search?",
        why: "n = number of words added, L = word/pattern length.",
        options: [
          "addWord O(L). search: O(L) best case (all literals, early fail); O(n×L) worst case (pattern all '.', many words)",
          "addWord O(1), search O(L) always",
          "addWord O(n×L), search O(1)",
          "Both O(L²)",
        ],
        correct: 0,
      },
      {
        id: "edge", label: "Edge Cases", emoji: "🎯",
        prompt: "What if the pattern length differs from all added words? What about search('.')?",
        why: "Length mismatch should return false; single '.' matches any single-char word.",
        options: [
          "Pattern longer/shorter than word → DFS reaches isEnd before consuming pattern or runs out of nodes → false. search('.') → true iff any single-char word was added.",
          "Length mismatch causes infinite loop",
          "search('.') always returns true",
          "Pattern and word lengths must always match — guaranteed by problem",
        ],
        correct: 0,
      },
    ],
    dryrun: {
      input: 'addWord("bad"), addWord("dad"). search(".ad")',
      steps: [
        { label: "Build Trie", state: "root→b→a→d(isEnd), root→d→a→d(isEnd)" },
        { label: "search('.ad'): index=0, char='.' → try all 26 children of root", state: "Children exist: b, d" },
        { label: "DFS branch 1: node=b, index=1, char='a' → follow b→a", state: "Node a exists" },
        { label: "index=2, char='d' → follow a→d", state: "Node d exists, isEnd=true, index=3=len → return true" },
      ],
      question: "DFS branch 1 found 'bad' matching '.ad'. What does search return?",
      options: [
        "true — first successful branch short-circuits; no need to try 'd' branch",
        "false — must check all 26 children before returning",
        "true only after checking all branches",
        "false — '.' cannot match 'b'",
      ],
      correct: 0,
      explanation: "DFS returns true as soon as any branch succeeds. Branch b→a→d matches '.ad' → return true immediately. Remaining children (d,…) are skipped.",
    },
    pushback: {
      question: "Could you memoize the DFS to speed up repeated searches with the same pattern?",
      answer: "Yes if patterns repeat: cache (node_id, pattern_index) → bool. But node_id is non-trivial to track. In practice, the Trie is sparse enough that memoization adds overhead for most inputs. Useful only when the same '.' pattern is queried many times.",
    },
    followup: {
      question: "How would you extend this to support '*' meaning zero or more of any character?",
      answer: "Track a set of active nodes (like NFA simulation). For '*': keep current node in the set (zero chars) and expand to all children repeatedly. For each literal: advance each active node's matching child. O(n×L) per search where n = Trie nodes. Classic NFA-to-DFA approach.",
    },
    solution: {
      approach: "Trie + DFS with wildcard branching",
      steps: [
        "addWord: standard Trie insert, mark isEnd",
        "search(pattern): DFS(node, index)",
        "Base: index == pattern.length → return node.isEnd",
        "If pattern[index] == '.': recurse into all non-null children; return true if any returns true",
        "Else: follow child matching pattern[index]; return false if missing",
      ],
      time: "addWord O(L), search O(L) best / O(n×L) worst",
      space: "O(n×L) for Trie",
    },
    keywords: {
      understand: ["wildcard","dot","exactly one","not star","3-letter","match","pattern"],
      brute: ["O(n×L)","list","iterate","compare","character","wildcard","scan"],
      pattern: ["Trie","DFS","26 children","branch","literal","early return","backtrack"],
      algorithm: ["DFS","index","isEnd","dot","try all","return true","recurse"],
      tradeoff: ["O(26^L)","sparse","n×L","bounded","memoize","branching","worst case"],
      complexity: ["O(L)","addWord","search","worst case","O(n×L)","all dots","sparse"],
      edge: ["length mismatch","single dot","false","empty","no words added","all wildcards"],
    },
  },
];
