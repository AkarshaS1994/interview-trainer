export const UNION_FIND_PROBLEMS = [
  {
    id: "uf1", title: "Number of Provinces", difficulty: "Medium", pattern: "Union-Find",
    companies: ["Amazon","Google","Meta"], minLevel: 3,
    description: "Given n cities and an n×n adjacency matrix isConnected, return the number of provinces (groups of directly or indirectly connected cities).",
    example: { input: "isConnected=[[1,1,0],[1,1,0],[0,0,1]]", output: "2 (cities 0,1 form one province; city 2 alone)" },
    hints: ["This is a connected components problem — use Union-Find or DFS.", "Union-Find: for each edge (i,j), union their sets.", "Count distinct roots at the end."],
    clarify: { question: "What would you clarify?", options: ["Is the graph undirected (matrix symmetric)?","Self-loops — isConnected[i][i]=1 always?","Can n be very large (up to 200)?","Can there be isolated cities (no connections)?","Return count of provinces or list them?"], mustAsk: [0,3,4], explanation: "Undirected: isConnected[i][j]==isConnected[j][i]. Diagonal always 1 (city connected to itself). Return count. Isolated city is its own province." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What is a 'province' mathematically? This maps to what graph theory concept?", why: { question: "What graph concept does 'province' map to?", options: ["Shortest path","Connected component — a maximal set of nodes where every pair has a path between them","Minimum spanning tree"], correct: 1, explanation: "Province = connected component. Cities connected directly or transitively form one component. Problem: count connected components in an undirected graph." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would DFS count connected components? Walk through the approach.", why: { question: "What is the time complexity of DFS on an adjacency matrix?", options: ["O(n)","O(n²) — DFS visits each node once, but adjacency matrix lookup per node scans all n columns","O(n log n)"], correct: 1, explanation: "DFS with adjacency matrix: for each unvisited node, DFS marks its entire component visited. Matrix scanning: O(n) per node × n nodes = O(n²). Same as Union-Find here since matrix is n×n." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "What is Union-Find and what two operations define it?", why: { question: "What are the two core Union-Find operations?", options: ["Push and Pop","Find: get root/representative of a set. Union: merge two sets. Together: dynamically track connected components.","Insert and Delete"], correct: 1, explanation: "Union-Find (Disjoint Set Union): Find(x) returns root of x's component. Union(x,y) merges x's and y's components. With path compression + union by rank: nearly O(1) amortized per operation." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through Union-Find with path compression and union by rank.", why: { question: "What does path compression do?", options: ["Balances the tree","Makes every node point directly to root on Find. Amortizes future Finds to O(1). Tree flattens over time.","Sorts nodes by rank"], correct: 1, explanation: "Path compression: during Find, make every node on path point directly to root. Future Finds on same path are O(1). Union by rank: attach smaller tree under larger. Together: nearly O(1) per op (inverse Ackermann)." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "DFS O(n²) vs Union-Find O(n²×α(n)) — when does Union-Find win?", why: { question: "When does Union-Find significantly outperform DFS?", options: ["Never — same complexity","Dynamic graphs: edges added over time. DFS must re-run. Union-Find: O(α(n)) per new edge — near O(1). DFS restart = O(n+m) each time.","Always prefer Union-Find"], correct: 1, explanation: "Static graph: both O(n²) for matrix. Dynamic (streaming edges): Union-Find O(α(n)) per union is near O(1), while DFS must restart. Union-Find is the tool for incremental connectivity." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space for Union-Find solution?", why: { question: "Why O(n²) despite near-O(1) Union-Find operations?", options: ["Union-Find is slow","Matrix has n² entries — must read each to find edges. O(n²) matrix traversal dominates the near-O(1) union operations.","Recursion overhead"], correct: 1, explanation: "Bottleneck: reading the n×n adjacency matrix = O(n²). Union-Find ops: O(n²×α(n)) ≈ O(n²). Space: O(n) for parent and rank arrays." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "n=1 single city — how many provinces?", options: ["0","1 — one city forms one province. isConnected=[[1]]. No edges to process. One component.","Error"], correct: 1, explanation: "n=1: parent=[0]. No pairs (i,j) with i<j. Count distinct roots: just root 0. Answer=1. Single city is its own province." } },
    ],
    dryrun: { question: "isConnected=[[1,1,0],[1,1,0],[0,0,1]], n=3. parent=[0,1,2]. Process (0,1): union(0,1)→parent[1]=0. Process (0,2): 0≠2, no edge. Process (1,2): 1≠2, no edge. Count distinct roots: find(0)=0, find(1)=0, find(2)=2. How many provinces?", options: ["2 (roots: 0 and 2)","3","1"], correct: 0, explanation: "Distinct roots: {0, 2} = 2. Cities 0 and 1 share root 0. City 2 has root 2 (isolated). 2 provinces. Correct." },
    pushback: { challenge: "What if instead of adjacency matrix you get a list of edges?", options: ["Same Union-Find: for each edge (u,v), call union(u,v). Count distinct roots at end. O(m×α(n)) where m=edges.","Need to rebuild adjacency matrix first","DFS only approach for edge list"], correct: 0, explanation: "Edge list is MORE efficient for Union-Find: O(m×α(n)) instead of O(n²) matrix scan. Union-Find excels with edge lists. Process each edge, merge components, count distinct roots." },
    followup: { question: "How do you detect a cycle in an undirected graph using Union-Find?", options: ["Before union(u,v): if find(u)==find(v), adding this edge creates a cycle (u and v already connected). O(m×α(n)).","DFS only can detect cycles","Union-Find can't detect cycles"], correct: 0, explanation: "Cycle detection: for each edge (u,v): if find(u)==find(v): they're in same component → adding edge creates cycle. Else: union(u,v). If any edge triggers same-component: cycle exists. Used in Kruskal's MST to skip cycle-forming edges." },
    solution: { approach: "Union-Find with Path Compression", steps: ["parent[i]=i, rank[i]=0 for all i","For each (i,j) where i<j and isConnected[i][j]==1: union(i,j)","Count nodes where find(i)==i (distinct roots)","Return count"], time: "O(n²×α(n))", space: "O(n)" },
    keywords: {
      understand: ["province","connected component","maximal","directly","transitively","undirected"],
      brute: ["DFS","O(n²)","matrix","scan","component","visited","adjacency"],
      pattern: ["Union-Find","Find","Union","root","representative","disjoint","DSU"],
      algorithm: ["path compression","union by rank","flatten","root","near O(1)","Ackermann"],
      tradeoff: ["dynamic","streaming","O(α)","incremental","DFS restart","Union-Find wins"],
      complexity: ["O(n²)","bottleneck","matrix traversal","O(n)","space","parent","rank"],
      edge: ["n=1","isolated","single city","one province","diagonal"],
    },
  },
  {
    id: "uf2", title: "Redundant Connection", difficulty: "Medium", pattern: "Union-Find",
    companies: ["Amazon","Google"], minLevel: 3,
    description: "Given a tree with n nodes (1..n) and one extra edge added to form a cycle, return the redundant edge. If multiple answers, return the one appearing last in input.",
    example: { input: "edges=[[1,2],[1,3],[2,3]]", output: "[2,3] (removing [2,3] leaves a valid tree)" },
    hints: ["Process edges one by one using Union-Find.", "If an edge connects two already-connected nodes: it's the redundant edge.", "Return the first such edge found (which, given 'last appearing' constraint, means process in order)."],
    clarify: { question: "What would you clarify?", options: ["Only one redundant edge exists?","Should I return the last redundant edge if multiple exist?","Are nodes numbered 1 to n?","Is the graph undirected?","Guarantee the input forms a valid tree plus exactly one extra edge?"], mustAsk: [0,1,3], explanation: "Exactly one redundant edge (exactly one cycle). If multiple could be removed, return the one appearing last in input. Nodes 1..n, undirected. Input guarantees tree + one edge." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "A tree with n nodes has exactly n-1 edges. Adding one edge creates exactly one cycle. What does 'redundant edge' mean here?", why: { question: "What makes an edge 'redundant' in this context?", options: ["Heaviest edge","Removing it leaves a valid tree (n nodes, n-1 edges, fully connected, no cycles). Exactly one such edge exists.","First edge added"], correct: 1, explanation: "Tree: n nodes, n-1 edges, no cycles, fully connected. Extra edge creates one cycle. Removing any edge IN that cycle restores tree. Return the one that appears last in input." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you try removing each edge and checking if remaining edges form a tree?", why: { question: "Why is brute force O(n²) or worse?", options: ["O(n log n)","For each of n edges: remove it, run DFS/BFS to check connectivity and acyclicity on n-1 edges. O(n) per check × n edges = O(n²).","O(n) is achievable trivially"], correct: 1, explanation: "n edges × O(n) tree-check each = O(n²). Union-Find processes edges in order, detects the cycle-creating edge in O(n×α(n)) ≈ O(n)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does Union-Find detect the exact edge that creates the cycle?", why: { question: "When does Union-Find identify a redundant edge?", options: ["After processing all edges","When processing edge (u,v): if find(u)==find(v), u and v are ALREADY connected. Adding this edge creates a cycle — it's redundant. Return immediately.","When a node has degree > 2"], correct: 1, explanation: "Union-Find processes edges in order. When find(u)==find(v) before union: they're already in the same component. This edge creates a cycle. It's the redundant one." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: for each edge, check if nodes already connected. If yes — redundant. If no — union them.", why: { question: "Why process edges in INPUT ORDER (not sort by weight or nodes)?", options: ["Order doesn't matter","'Last appearing' constraint: process in input order, return the FIRST edge detected as redundant. Input order ensures correctness for 'last in list' requirement.","Sort makes it faster"], correct: 1, explanation: "Problem guarantees exactly one cycle. Processing in input order: first redundant edge found is the one to return. 'Last appearing' means: among all edges in the cycle, return the last one in the input — which is exactly the one that first triggers find(u)==find(v)." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "DFS cycle detection vs Union-Find — which is simpler for this problem?", why: { question: "What's the advantage of Union-Find over DFS here?", options: ["DFS is always simpler","Union-Find processes edges incrementally — detects cycle edge exactly when it's added. DFS must re-traverse entire graph each time to find the cycle edge. Union-Find is both simpler and faster here.","Same complexity"], correct: 1, explanation: "Union-Find: O(n×α(n)), finds cycle edge in one pass. DFS to find cycle: O(n+m), plus need to identify which specific edge in cycle to remove. Union-Find naturally identifies the cycle-creating edge." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why nearly O(n)?", options: ["O(n²) adjacency matrix","n edges processed, each O(α(n))≈O(1) union/find. Total: O(n×α(n))≈O(n). Space: O(n) parent/rank arrays.","O(n log n)"], correct: 1, explanation: "n edges (exactly n edges for n-node tree plus one). Each edge: O(α(n)) ≈ O(1). Total: O(n). Space: O(n) for DSU arrays." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What if the redundant edge is the first edge in the list?", why: { question: "edges=[[1,2],[2,1]] — is [2,1] redundant?", options: ["[1,2] is redundant","[2,1] is the last appearing duplicate. find(2)==find(1) when processing [2,1] (after [1,2] already unioned them). Return [2,1].","Both are redundant"], correct: 1, explanation: "Process [1,2]: union(1,2). Process [2,1]: find(2)==find(1)→same component→return [2,1]. The second (last appearing) duplicate edge is redundant." } },
    ],
    dryrun: { question: "edges=[[1,2],[1,3],[2,3]]. parent=[0,1,2,3]. Process [1,2]: find(1)=1, find(2)=2, different → union. parent[2]=1. Process [1,3]: find(1)=1, find(3)=3, different → union. parent[3]=1. Process [2,3]: find(2)=?, find(3)=?", options: ["find(2)=1, find(3)=1 → same! Return [2,3]","find(2)=2, find(3)=3 → different, union","Error"], correct: 0, explanation: "find(2): parent[2]=1, parent[1]=1 → root=1. find(3): parent[3]=1 → root=1. Same root! Edge [2,3] is redundant — return [2,3]." },
    pushback: { challenge: "What if the graph could have multiple connected components (not guaranteed to be a tree + 1 edge)?", options: ["Still use Union-Find: detect any edge where find(u)==find(v). Return last such edge. Works for any graph.","Need different algorithm","Union-Find only works for trees"], correct: 0, explanation: "Union-Find handles any graph. For cycle detection in general graphs: same approach — process edges, return last edge where both endpoints already connected. Works whether 1 or multiple cycles." },
    followup: { question: "Redundant Connection II — what if the graph is DIRECTED (each node has at most one parent in a rooted tree)?", options: ["Two cases: (1) node with two parents — one parent edge is redundant; (2) cycle exists. Handle both with Union-Find + in-degree tracking. More complex.","Same algorithm as undirected","Always return last edge"], correct: 0, explanation: "Directed version: check in-degrees. If node has 2 parents: one of its incoming edges is redundant. If cycle exists (no double-parent): find cycle edge with Union-Find. Combine both checks." },
    solution: { approach: "Union-Find — Detect First Cycle Edge", steps: ["parent[i]=i, rank[i]=0 for i in 1..n","For each edge (u,v): if find(u)==find(v): return [u,v] (redundant)","Else: union(u,v)","Never reached by problem guarantee"], time: "O(n×α(n))", space: "O(n)" },
    keywords: {
      understand: ["redundant","tree","n-1 edges","cycle","removing","valid","last appearing"],
      brute: ["O(n²)","remove","check","DFS","tree-check","each edge","connectivity"],
      pattern: ["find(u)==find(v)","already connected","cycle","redundant","Union-Find"],
      algorithm: ["input order","last appearing","first detected","union","find","cycle"],
      tradeoff: ["incremental","DFS","Union-Find","one pass","naturally identifies"],
      complexity: ["O(n×α(n))","O(n)","n edges","nearly O(1)","space","O(n)"],
      edge: ["duplicate","same edge","last","first","cycle","two parents"],
    },
  },
  {
    id: "uf3", title: "Graph Valid Tree", difficulty: "Medium", pattern: "Union-Find",
    companies: ["Google","Amazon"], minLevel: 3,
    description: "Given n nodes labeled 0 to n-1 and a list of undirected edges, return true if these edges form a valid tree (connected, no cycles, n-1 edges).",
    example: { input: "n=5, edges=[[0,1],[0,2],[0,3],[1,4]]", output: "true" },
    hints: ["Valid tree: exactly n-1 edges, all nodes connected, no cycles.", "Union-Find: process each edge; if find(u)==find(v) cycle exists.", "Check edge count first: != n-1 → immediately false."],
    clarify: { question: "What would you clarify?", options: ["Undirected graph?","Can edges be repeated (duplicate edges)?","Does n-1 edges guarantee a tree?","Can n=0?","Return bool or error?"], mustAsk: [0,1,3], explanation: "Undirected. No duplicate edges (simple graph). n-1 edges is necessary but not sufficient (could be disconnected or have cycle). n=0: 0 nodes, 0 edges — return true (empty tree). Return bool." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What are the two conditions a graph must satisfy to be a valid tree?", why: { question: "A valid tree requires what two properties?", options: ["Sorted nodes and min weight","Connected (all nodes reachable from any node) AND acyclic (no cycles). Equivalently: n nodes, n-1 edges, connected.","Binary and balanced"], correct: 1, explanation: "Tree = connected acyclic undirected graph. Equivalently: n nodes, exactly n-1 edges, all connected. Any two of these three properties imply the third. Check edge count + connectivity." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would DFS verify both tree conditions?", why: { question: "Two-step DFS: what do you check?", options: ["Only check edge count","(1) DFS from node 0: check all n nodes reachable (connected). (2) During DFS: check no back edges (cycle). O(n+m).","Only check cycle"], correct: 1, explanation: "DFS: start from node 0. Track visited. Back edge (visiting already-visited non-parent node) = cycle. If all n nodes visited + no cycle: valid tree. O(n+m)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does a quick edge-count check filter out most invalid inputs before Union-Find?", why: { question: "Why check edges.length != n-1 first?", options: ["Optimization only","If edge count is wrong: cannot be a tree (over-constrained or under-constrained). Eliminates O(n) invalid cases in O(1) before running Union-Find.","Not a valid shortcut"], correct: 1, explanation: "Tree must have exactly n-1 edges. If not: fail immediately. O(1) check. Avoids running Union-Find on obviously invalid inputs. Remaining check: are those n-1 edges acyclic and connected?" } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through Union-Find approach: check edge count, then union each edge detecting cycles, then verify single component.", why: { question: "Why is 'find(u)==find(v)' sufficient to detect a cycle once we've verified n-1 edges?", options: ["It's not sufficient","With n-1 edges: if any edge creates a cycle, the graph is disconnected somewhere. Find(u)==find(v) = already connected = adding this edge creates cycle.","Need back-edge DFS too"], correct: 1, explanation: "n-1 edges + no cycle = connected tree. If find(u)==find(v) for any edge: there's a cycle. Combined with n-1 edges: rest of graph is disconnected. Either condition alone with n-1 edges proves invalid." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "DFS O(n+m) vs Union-Find O(n+m×α(n)) — practically the same. When is DFS preferred?", why: { question: "When might DFS be preferred over Union-Find?", options: ["Always prefer Union-Find","Interview familiarity: DFS + visited is universally known. Union-Find path compression may not be. For static graphs, DFS is simpler to write correctly.","Never prefer DFS"], correct: 1, explanation: "Both correct. DFS: simpler code, universally understood. Union-Find: more elegant for incremental checks. Interview: DFS often quicker to write. Production system adding edges: Union-Find wins." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(n+m) effectively?", options: ["O(n²)","Edge count check O(1). Union-Find: m edges × O(α(n)) each ≈ O(m). Build parent array: O(n). Total: O(n+m).","O(m log n)"], correct: 1, explanation: "O(n) to initialize DSU + O(m×α(n)) ≈ O(m) to process edges. Total O(n+m). Space O(n) for parent/rank arrays." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "n=1, edges=[] — is it a valid tree?", options: ["False — no edges","True — single node is a valid tree. 0 edges = n-1=0 edges. No cycle. Connected trivially.","Error"], correct: 1, explanation: "n=1, edges=[]: 0 edges = n-1=0. Single node, no edges. Valid tree by definition. Return true." } },
    ],
    dryrun: { question: "n=5, edges=[[0,1],[0,2],[0,3],[1,4]]. Edge count=4=n-1=4. Process all: no find(u)==find(v) found. Count distinct roots?", options: ["1 root (all connected) → return true","5 roots → disconnected","2 roots"], correct: 0, explanation: "Process [0,1]: union(0,1). [0,2]: union(0,2). [0,3]: union(0,3). [1,4]: find(1)=0, find(4)=4, different → union. Final root: all point to 0. One component. Return true." },
    pushback: { challenge: "What if you need to check validity dynamically as edges are added one by one?", options: ["Union-Find with edge count tracking: after each union, check if edge count == n-1 AND no cycle detected. O(α(n)) per edge addition.","Re-run full DFS each time","Count components separately"], correct: 0, explanation: "Dynamic validity: maintain Union-Find + edge count. On addEdge(u,v): if find(u)==find(v): cycle exists, not a tree. Else: union, increment edge count. When count==n-1 and 1 component: valid tree. O(α(n)) per addition." },
    followup: { question: "How does this differ from checking if a directed graph is a valid rooted tree?", options: ["Directed tree: each node except root has exactly 1 incoming edge (in-degree=1). Root has in-degree 0. Check: exactly one root, no cycles. Use topological sort.","Same algorithm","Directed trees don't exist"], correct: 0, explanation: "Directed rooted tree: exactly one node with in-degree 0 (root). All others in-degree 1. Topological sort: if all n nodes processed, no cycle. Both conditions needed. Different from undirected tree check." },
    solution: { approach: "Edge Count + Union-Find", steps: ["If edges.length != n-1: return false","parent[i]=i, rank[i]=0","For each edge (u,v): if find(u)==find(v): return false (cycle)","Else: union(u,v)","Return true (n-1 edges, no cycle = connected tree)"], time: "O(n+m×α(n))", space: "O(n)" },
    keywords: {
      understand: ["connected","acyclic","n-1 edges","tree","valid","undirected","conditions"],
      brute: ["DFS","back edge","visited","cycle","all nodes","connected","O(n+m)"],
      pattern: ["edge count","n-1","check first","O(1)","filter","Union-Find","quick"],
      algorithm: ["find(u)==find(v)","cycle","union","n-1 edges","single component"],
      tradeoff: ["DFS","Union-Find","static","dynamic","interview","simplicity"],
      complexity: ["O(n+m)","α(n)","n edges","m edges","O(n)","space","initialize"],
      edge: ["n=1","empty edges","single node","true","0 edges"],
    },
  },
  {
    id: "uf4", title: "Accounts Merge", difficulty: "Medium", pattern: "Union-Find",
    companies: ["Meta","Amazon"], minLevel: 3,
    description: "Given a list of accounts where each account is [name, email1, email2, ...], merge accounts sharing at least one common email. Return merged accounts with emails sorted and name prepended.",
    example: { input: 'accounts=[["Alice","a@m","b@m"],["Bob","c@m"],["Alice","b@m","d@m"]]', output: '[["Alice","a@m","b@m","d@m"],["Bob","c@m"]]' },
    hints: [
      "Union-Find on emails: for each account, union all its emails together.",
      "Map each email to its root, then group emails by root.",
      "Sort each group and prepend the account owner's name.",
    ],
    clarify: { question: "What would you clarify?", options: ["Same name but different emails — always same person?","Two accounts with same email but different names — which name to use?","Return accounts in any order?","Emails case-sensitive?"], mustAsk: [0,1,2], explanation: "Same name ≠ same person. Same email = same person regardless of name. Use the name from any of the merged accounts (they should match by problem guarantee). Any order fine. Case-sensitive." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🎯", prompt: "Why can't you merge by account name? What uniquely identifies a person?", why: "Names aren't unique; shared email addresses are the only reliable link between accounts.", options: ["Name identifies a person","Email uniquely links accounts: two accounts sharing even one email belong to the same person. Name could be shared by unrelated people.","Index in list identifies person","Sort by name then merge"], correct: 1 },
      { id: "brute", label: "Brute Force", emoji: "💪", prompt: "How would comparing every pair of accounts for shared emails work? Complexity?", why: "Pairwise comparison is correct but quadratic in accounts × emails.", options: ["For each pair of accounts: check if any email in common. O(A² × E) where A=accounts, E=avg emails. Union-Find: O(A×E×α).","O(n log n) always","Hash-set intersection for each pair","Sort accounts by name first"], correct: 0 },
      { id: "pattern", label: "Pattern", emoji: "🔍", prompt: "How does Union-Find on emails (not accounts) capture transitive merges?", why: "Union-Find's transitivity handles chains like: account1∩account2≠∅, account2∩account3≠∅ → all three merge.", options: ["Union accounts directly","Union emails: for each account, union all its emails to the first email. Transitive unions chain correctly: if a@m and b@m unioned, and b@m and c@m unioned, a@m, b@m, c@m all in one component.","No transitive merges needed","Sort emails then merge"], correct: 1 },
      { id: "algorithm", label: "Algorithm", emoji: "⚙️", prompt: "Walk through: build DSU on emails, map email→account name, group by root, sort and format.", why: "Root-grouping collects all merged emails; sorting produces deterministic output.", options: ["Merge by name then sort","(1) DSU: union account's emails to first email. (2) email→owner map. (3) Group emails by find(email). (4) Sort each group. (5) Prepend name.","Sort all emails globally first","Hash map of name→emails only"], correct: 1 },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Union-Find vs DFS on email-graph. Which is more natural here?", why: "Both work; DFS on an email→accounts graph is also intuitive.", options: ["Only Union-Find works","DFS: build graph email→accounts. DFS from each unvisited email collecting component. Union-Find: more concise, handles transitivity naturally. Same complexity.","Only DFS works","BFS required"], correct: 1 },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity where A=accounts, E=avg emails per account?", why: "Each email processed once for union and once for grouping.", options: ["O(A²×E)","O(A×E×α(A×E)) ≈ O(A×E) — each email unioned once. Sorting: O(A×E×log(A×E)). Space: O(A×E) for DSU and maps.","O(E log A)","O(A²) time, O(E) space"], correct: 1 },
      { id: "edge", label: "Edge Cases", emoji: "🎯", prompt: "What if all accounts have unique emails (no merges)? What if one account has many emails?", why: "No merges → output same as input (just sorted emails). Large account → still O(E log E) per group.", options: ["Error — merge requires overlap","No merges: each email is its own root, no unions happen. Output = input with emails sorted. Large account: one large component, O(E log E) sort.","Must merge by name then","All accounts always merge"], correct: 1 },
    ],
    dryrun: {
      input: 'accounts=[["Alice","a@m","b@m"],["Bob","c@m"],["Alice","b@m","d@m"]]',
      steps: [
        { label: "Account 0 (Alice): union(a@m, b@m)", state: "parent[b@m]=a@m" },
        { label: "Account 1 (Bob): only c@m, no union", state: "" },
        { label: "Account 2 (Alice): union(b@m,d@m) → find(b@m)=a@m, union(a@m,d@m)", state: "parent[d@m]=a@m" },
        { label: "Group by root: a@m→{a@m,b@m,d@m}, c@m→{c@m}", state: "" },
        { label: "Sort & prepend: Alice+[a@m,b@m,d@m], Bob+[c@m]", state: "" },
      ],
      question: "How many merged account groups result?",
      options: ["2 (Alice group + Bob group)","3 (no merges)","1 (all merged)","4 (all emails separate)"],
      correct: 0,
      explanation: "Accounts 0 and 2 share 'b@m', so they merge into one Alice group with emails [a@m,b@m,d@m]. Bob has unique email. 2 groups total.",
    },
    pushback: { question: "What if the same email appears under two different names — which name do you use?", answer: "The problem guarantees this won't happen (consistent data). If it could: use the name from the earliest-indexed account or flag as a data error. In practice, store accountIndex→name mapping and use lowest index." },
    followup: { question: "How would you detect potential duplicate or fraudulent accounts (same email, different owner)?", answer: "During the email→account-name mapping step, if two accounts try to map the same email to different names, flag that email. Union-Find still handles merging; just add a validation pass over the email→name map before grouping." },
    solution: { approach: "Union-Find on Emails", steps: ["Init DSU with each email as its own root","For each account: union all emails to first email; map email→owner name","Group all emails by their DSU root","Sort each group; prepend owner name","Return all groups"], time: "O(A×E log(A×E))", space: "O(A×E)" },
    keywords: {
      understand: ["email","unique","link","transitive","name","shared","identify"],
      brute: ["pairwise","O(A²×E)","intersection","compare","quadratic"],
      pattern: ["DSU on emails","union","first email","transitive","component","chain"],
      algorithm: ["email→owner","group by root","sort","prepend name","find","union"],
      tradeoff: ["DFS","graph","email→accounts","component","same complexity"],
      complexity: ["O(A×E)","O(A×E log(A×E))","sort dominates","space DSU","maps"],
      edge: ["unique emails","no merge","sorted output","large account","one component"],
    },
  },
  {
    id: "uf5", title: "Smallest String With Swaps", difficulty: "Medium", pattern: "Union-Find",
    companies: ["Amazon","Google"], minLevel: 3,
    description: "Given a string s and a list of pairs of indices, you can swap characters at any pair of indices any number of times. Return the lexicographically smallest string possible.",
    example: { input: 's="dcab", pairs=[[0,3],[1,2],[0,2]]', output: '"abcd"' },
    hints: [
      "Characters reachable from each other via any chain of swaps can be freely permuted.",
      "Use Union-Find to group indices that are transitively connected via pairs.",
      "Sort characters within each connected component and place them in sorted index order.",
    ],
    clarify: { question: "What would you clarify?", options: ["Can pairs be used multiple times?","Are pairs bidirectional?","Is the string lowercase only?","Can pairs contain the same index twice?"], mustAsk: [0,1], explanation: "Pairs can be used any number of times — transitively, any index reachable from another can be freely swapped. Bidirectional. Lowercase. Self-pairs: no-op, fine." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🎯", prompt: "Why does 'any number of swaps' mean all chars in a connected component can be arranged freely?", why: "Repeated swaps compose to any permutation within a connected set of indices.", options: ["Only direct pairs can swap","Transitive swaps: if 0↔3 and 0↔2, then 2↔3 is reachable. Any permutation of indices in one component is achievable. Thus: sort chars in each component for lexicographic minimum.","Only adjacent swaps count","Just apply pairs in order"], correct: 1 },
      { id: "brute", label: "Brute Force", emoji: "💪", prompt: "How would BFS/DFS to find connected components work here?", why: "Build adjacency list from pairs, DFS to find components — same result as Union-Find.", options: ["Try all possible swap sequences","Build graph from pairs. DFS/BFS from each unvisited index to collect its component. Sort chars in component, assign to sorted indices. O(n+m) where m=pairs.","Sort pairs then apply","Greedy swap order"], correct: 1 },
      { id: "pattern", label: "Pattern", emoji: "🔍", prompt: "How does Union-Find group indices transitively and why is it more efficient than BFS here?", why: "Union-Find handles incremental edge additions cleanly and is concise for this pattern.", options: ["BFS and Union-Find identical","Union-Find: process each pair with union(i,j). Handles transitivity automatically via path compression. Concise for edge-list input. O(m×α(n)) vs BFS O(n+m) — practically same.","Union-Find can't handle this","Sort indices first"], correct: 1 },
      { id: "algorithm", label: "Algorithm", emoji: "⚙️", prompt: "Walk through: union all pairs, group indices by root, sort chars and indices, reassemble.", why: "Sorting both chars and indices ensures the smallest chars go to the smallest positions.", options: ["Union pairs, then sort entire string","(1) Union all pairs. (2) Group indices by find(i). (3) For each group: sort indices, sort chars at those indices. (4) Assign sorted chars to sorted indices in result.","Sort by char frequency","Only sort pairs indices"], correct: 1 },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Union-Find vs DFS for this problem. Any meaningful difference?", why: "Functionally identical; Union-Find is slightly more idiomatic for edge-list problems.", options: ["Union-Find always faster","Both O(n+m) effectively. Union-Find: iterative, handles online edges. DFS: recursive, cleaner graph traversal. For interview: either is fine. Union-Find preferred for large edge lists.","DFS always cleaner","BFS is fastest"], correct: 1 },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: "Sorting within each component dominates; total chars sorted across all components is n.", options: ["O(n²) time","O(n log n) time (sorting n chars total across all components). O(m×α(n)) for DSU. Space: O(n) for DSU + grouping.","O(m log m) time","O(n+m) time, O(1) space"], correct: 0 },
      { id: "edge", label: "Edge Cases", emoji: "🎯", prompt: "What if there are no pairs? What if all indices are connected?", why: "No pairs → no swaps possible, return original string. All connected → sort entire string.", options: ["No pairs: error","No pairs: each index is its own component → return s unchanged. All connected: one component → sort s entirely.","No pairs: sort anyway","Pairs required"], correct: 1 },
    ],
    dryrun: {
      input: 's="dcab", pairs=[[0,3],[1,2],[0,2]]',
      steps: [
        { label: "Union(0,3): {0,3}", state: "parent[3]=0" },
        { label: "Union(1,2): {1,2}", state: "parent[2]=1" },
        { label: "Union(0,2): find(0)=0, find(2)=1 → union(0,1)", state: "All in one component: {0,1,2,3}" },
        { label: "Chars at [0,1,2,3]: d,c,a,b → sorted: a,b,c,d", state: "Assign to sorted indices [0,1,2,3]" },
        { label: "Result: 'abcd'", state: "" },
      ],
      question: "How many connected components exist after all unions?",
      options: ["1 (all indices connected)","2","3","4 (no connections)"],
      correct: 0,
      explanation: "Union(0,3)→{0,3}. Union(1,2)→{1,2}. Union(0,2): find(0)=0, find(2)=1, union(0,1)→{0,1,2,3}. One component. Sort d,c,a,b→a,b,c,d. Result='abcd'.",
    },
    pushback: { question: "What if instead of swaps you can only rotate pairs (circular shift within pairs)?", answer: "Different problem — rotation within a pair has limited moves. Union-Find component approach still applies for transitivity, but within each component only rotations are possible, not arbitrary permutations. Requires case analysis per component." },
    followup: { question: "What if you want the lexicographically largest string instead?", answer: "Same algorithm but sort characters in descending order within each component before assigning to sorted indices. O(n log n) same complexity." },
    solution: { approach: "Union-Find + Component Sort", steps: ["Union all pairs using DSU","Group indices by find(i)","For each group: sort indices; collect and sort chars at those indices","Assign sorted chars to sorted indices in result array","Return result string"], time: "O(n log n)", space: "O(n)" },
    keywords: {
      understand: ["transitive","connected","component","free permutation","reachable","any number"],
      brute: ["DFS","adjacency list","BFS","O(n+m)","component","collect"],
      pattern: ["Union-Find","union pairs","transitivity","path compression","online edges"],
      algorithm: ["group by root","sort indices","sort chars","assign","sorted positions"],
      tradeoff: ["DFS equivalent","O(n+m)","idiomatic","edge-list","interview"],
      complexity: ["O(n log n)","sort dominates","O(n) space","DSU","grouping"],
      edge: ["no pairs","unchanged","all connected","sort entire","one component"],
    },
  },
  {
    id: "uf6", title: "Satisfiability of Equality Equations", difficulty: "Medium", pattern: "Union-Find",
    companies: ["Amazon","Google"], minLevel: 3,
    description: "Given equations like 'a==b' and 'b!=c', determine if all equations can be satisfied simultaneously. Return true if satisfiable, false otherwise.",
    example: { input: 'equations=["a==b","b!=a"]', output: "false (a==b forces them equal, but b!=a denies it)" },
    hints: [
      "Two-pass: first process all '==' equations (union), then check all '!=' equations.",
      "If a '!=' equation has both variables in the same component, contradiction found.",
      "Variables are single lowercase letters (at most 26 unique).",
    ],
    clarify: { question: "What would you clarify?", options: ["Variables are always single lowercase letters?","Can a variable equal itself (a==a)?","Are equations always in format 'x==y' or 'x!=y'?","Can same pair appear in both == and != ?"], mustAsk: [0,2,3], explanation: "Single lowercase letters. a==a: always true (trivially union, find(a)==find(a)). Only == and != operators. Same pair in both: contradiction, return false." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🎯", prompt: "Why must you process all '==' equations BEFORE checking '!=' equations?", why: "Equality unions must be complete before testing any inequality — otherwise transitive equalities are missed.", options: ["Process in input order","All '==' first: union transitive equalities completely. Then '!=' checks: if find(x)==find(y), contradiction. If '!=' checked mid-union, transitive links may not exist yet.","Process '!=' first","Interleave == and !="], correct: 1 },
      { id: "brute", label: "Brute Force", emoji: "💪", prompt: "How would trying all variable assignments work? Complexity?", why: "26 variables with 26 possible values → exponential; Union-Find is linear.", options: ["26^26 assignments × n checks = infeasible","Try all variable assignments (0..25 for each of 26 letters). Verify each equation. O(26^26) — completely infeasible. Union-Find: O(n×α(26)) ≈ O(n).","O(26²) comparisons","Sort equations then check"], correct: 0 },
      { id: "pattern", label: "Pattern", emoji: "🔍", prompt: "How does Union-Find on single-character variables scale? How large can the DSU be?", why: "At most 26 nodes — DSU is essentially constant size.", options: ["DSU of n nodes","DSU of at most 26 nodes (a-z). All operations are O(α(26)) ≈ O(1). Total O(n) for n equations.","DSU of n² nodes","26! nodes needed"], correct: 1 },
      { id: "algorithm", label: "Algorithm", emoji: "⚙️", prompt: "Walk through: pass 1 union all ==, pass 2 check all != for same-component violations.", why: "Clean two-pass separation ensures all equality relationships are captured before any inequality is validated.", options: ["One pass: union == then immediately check !=","Pass 1: for 'x==y' → union(x,y). Pass 2: for 'x!=y' → if find(x)==find(y): return false. Return true after all checks.","Sort equations by operator first","Check all at once with backtracking"], correct: 1 },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Union-Find vs graph coloring or constraint propagation. When does each apply?", why: "Graph coloring handles more complex constraints; Union-Find is optimal for equality/inequality partitioning.", options: ["Always use constraint propagation","Union-Find: optimal for equality/disequality over a flat set. Graph coloring: useful for k-colorable constraints (k>2). Constraint propagation: general but overkill here.","Backtracking always needed","Only sorting works"], correct: 1 },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: "DSU bounded at 26 nodes; n equations each do O(1) union/find.", options: ["O(n²)","O(n×α(26)) ≈ O(n) time. Space: O(26) = O(1) for DSU (constant regardless of input size).","O(26 log 26)","O(n log n)"], correct: 1 },
      { id: "edge", label: "Edge Cases", emoji: "🎯", prompt: "What about 'a==a'? What about 'a!=a'?", why: "Self-equality is trivially true; self-inequality is always a contradiction.", options: ["a==a: error; a!=a: error","a==a: union(a,a)=no-op, find(a)==find(a)=true, fine. a!=a: find(a)==find(a)→contradiction→return false immediately.","Both ignored","a!=a: always true"], correct: 1 },
    ],
    dryrun: {
      input: 'equations=["a==b","b!=a"]',
      steps: [
        { label: "Pass 1 (==): union(a,b)", state: "parent[b]=a (or merged root)" },
        { label: "Pass 2 (!=): check 'b!=a'", state: "find(b)=a, find(a)=a — same component!" },
        { label: "Contradiction: b and a must be unequal but are in same == component", state: "Return false" },
      ],
      question: "Why does 'b!=a' cause a contradiction?",
      options: ["a and b are in different components","a==b was unioned first, so find(a)==find(b). The != equation requires find(b)≠find(a) — contradiction → false.","b!=a is always false","No contradiction exists"],
      correct: 1,
      explanation: "Pass 1: union(a,b) → same component. Pass 2: 'b!=a' → find(b)==find(a) → contradiction. Return false.",
    },
    pushback: { question: "What if equations involved multi-character variable names?", answer: "Replace Union-Find index (charCode-based) with a HashMap from string→id. Assign each new variable a unique integer id. Same two-pass algorithm, just different variable mapping. O(n) overall." },
    followup: { question: "Can you extend this to handle '<' and '>' (ordering) constraints?", answer: "Ordering constraints can't be handled by Union-Find alone — they require topological sort or constraint propagation. Union-Find handles only equivalence classes (==) and disequalities (!=). For </>: build a DAG and check for cycles." },
    solution: { approach: "Two-Pass Union-Find", steps: ["Init DSU for 26 lowercase letters","Pass 1: for each 'x==y' → union(x,y)","Pass 2: for each 'x!=y' → if find(x)==find(y): return false","Return true"], time: "O(n)", space: "O(1) (26-node DSU)" },
    keywords: {
      understand: ["== first","!=","transitive","two-pass","order matters","complete union"],
      brute: ["26^26","infeasible","all assignments","verify","O(n)","exponential"],
      pattern: ["26 nodes","O(1) DSU","constant size","α(26)","equality","disequality"],
      algorithm: ["pass 1 union","pass 2 check","find(x)==find(y)","contradiction","return false"],
      tradeoff: ["coloring","constraint propagation","k>2","flat set","optimal","simple"],
      complexity: ["O(n)","O(1) space","26 nodes","α(26)","constant DSU"],
      edge: ["a==a","no-op","a!=a","contradiction","self","always false"],
    },
  },
];
