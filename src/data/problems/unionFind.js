export const UNION_FIND_PROBLEMS = [
  {
    id: "uf1", title: "Number of Provinces", difficulty: "Medium", pattern: "Union-Find",
    companies: ["Amazon","Google","Meta"], minLevel: 3,
    description: "Given n cities and an n×n adjacency matrix isConnected, return the number of provinces (groups of directly or indirectly connected cities).",
    example: { input: "isConnected=[[1,1,0],[1,1,0],[0,0,1]]", output: "2 (cities 0,1 form one province; city 2 alone)" },
    hints: ["This is a connected components problem — use Union-Find or DFS.", "Union-Find: for each edge (i,j), union their sets.", "Count distinct roots at the end."],
    clarify: { question: "What would you clarify?", options: ["Is the graph undirected (matrix symmetric)?","Self-loops — isConnected[i][i]=1 always?","Can n be very large (up to 200)?","Can there be isolated cities (no connections)?","Return count of provinces or list them?"], mustAsk: [0,3,4], explanation: "Undirected: isConnected[i][j]==isConnected[j][i]. Diagonal always 1 (city connected to itself). Return count. Isolated city is its own province." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What is a 'province' mathematically? This maps to what graph theory concept?", why: { question: "What graph concept does 'province' map to?", options: ["Shortest path","Connected component — a maximal set of nodes where every pair has a path between them","Minimum spanning tree"], correct: 1, explanation: "Province = connected component. Cities connected directly or transitively form one component. Problem: count connected components in an undirected graph." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would DFS count connected components? Walk through the approach.", why: { question: "What is the time complexity of DFS on an adjacency matrix?", options: ["O(n)","O(n²) — DFS visits each node once, but adjacency matrix lookup per node scans all n columns","O(n log n)"], correct: 1, explanation: "DFS with adjacency matrix: for each unvisited node, DFS marks its entire component visited. Matrix scanning: O(n) per node × n nodes = O(n²). Same as Union-Find here since matrix is n×n." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "What is Union-Find and what two operations define it?", why: { question: "What are the two core Union-Find operations?", options: ["Push and Pop","Find: get root/representative of a set. Union: merge two sets. Together: dynamically track connected components.","Insert and Delete"], correct: 1, explanation: "Union-Find (Disjoint Set Union): Find(x) returns root of x's component. Union(x,y) merges x's and y's components. With path compression + union by rank: nearly O(1) amortized per operation." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through Union-Find with path compression and union by rank.", why: { question: "What does path compression do?", options: ["Balances the tree","Makes every node point directly to root on Find. Amortizes future Finds to O(1). Tree flattens over time.","Sorts nodes by rank"], correct: 1, explanation: "Path compression: during Find, make every node on path point directly to root. Future Finds on same path are O(1). Union by rank: attach smaller tree under larger. Together: nearly O(1) per op (inverse Ackermann)." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "DFS O(n²) vs Union-Find O(n²×α(n)) — when does Union-Find win?", why: { question: "When does Union-Find significantly outperform DFS?", options: ["Never — same complexity","Dynamic graphs: edges added over time. DFS must re-run. Union-Find: O(α(n)) per new edge — near O(1). DFS restart = O(n+m) each time.","Always prefer Union-Find"], correct: 1, explanation: "Static graph: both O(n²) for matrix. Dynamic (streaming edges): Union-Find O(α(n)) per union is near O(1), while DFS must restart. Union-Find is the tool for incremental connectivity." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space for Union-Find solution?", why: { question: "Why O(n²) despite near-O(1) Union-Find operations?", options: ["Union-Find is slow","Matrix has n² entries — must read each to find edges. O(n²) matrix traversal dominates the near-O(1) union operations.","Recursion overhead"], correct: 1, explanation: "Bottleneck: reading the n×n adjacency matrix = O(n²). Union-Find ops: O(n²×α(n)) ≈ O(n²). Space: O(n) for parent and rank arrays." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "n=1 single city — how many provinces?", options: ["0","1 — one city forms one province. isConnected=[[1]]. No edges to process. One component.","Error"], correct: 1, explanation: "n=1: parent=[0]. No pairs (i,j) with i<j. Count distinct roots: just root 0. Answer=1. Single city is its own province." } },
    ],
    dryrun: { question: "isConnected=[[1,1,0],[1,1,0],[0,0,1]], n=3. parent=[0,1,2]. Process (0,1): union(0,1)→parent[1]=0. Process (0,2): 0≠2, no edge. Process (1,2): 1≠2, no edge. Count distinct roots: find(0)=0, find(1)=0, find(2)=2. How many provinces?", options: ["2 (roots: 0 and 2)","3","1"], correct: 0, explanation: "Distinct roots: {0, 2} = 2. Cities 0 and 1 share root 0. City 2 has root 2 (isolated). 2 provinces. Correct." },
    pushback: { challenge: "What if instead of adjacency matrix you get a list of edges?", options: ["Same Union-Find: for each edge (u,v), call union(u,v). Count distinct roots at end. O(m×α(n)) where m=edges.","Need to rebuild adjacency matrix first","DFS only approach for edge list"], correct: 0, explanation: "Edge list is MORE efficient for Union-Find: O(m×α(n)) instead of O(n²) matrix scan. Union-Find excels with edge lists. Process each edge, merge components, count distinct roots." },
    followup: { question: "How do you detect a cycle in an undirected graph using Union-Find?", options: ["Before union(u,v): if find(u)==find(v), adding this edge creates a cycle (u and v already connected). O(m×α(n)).","DFS only can detect cycles","Union-Find can't detect cycles"], correct: 0, explanation: "Cycle detection: for each edge (u,v): if find(u)==find(v): they're in same component → adding edge creates cycle. Else: union(u,v). If any edge triggers same-component: cycle exists. Used in Kruskal's MST to skip cycle-forming edges." },
    solution: { approach: "Union-Find with Path Compression", steps: ["parent[i]=i, rank[i]=0 for all i","For each (i,j) where i<j and isConnected[i][j]==1: union(i,j)","Count nodes where find(i)==i (distinct roots)","Return count"], time: "O(n²×α(n))", space: "O(n)" },
  },
  {
    id: "uf2", title: "Redundant Connection", difficulty: "Medium", pattern: "Union-Find",
    companies: ["Amazon","Google"], minLevel: 3,
    description: "Given a tree with n nodes (1..n) and one extra edge added to form a cycle, return the redundant edge. If multiple answers, return the one appearing last in input.",
    example: { input: "edges=[[1,2],[1,3],[2,3]]", output: "[2,3] (removing [2,3] leaves a valid tree)" },
    hints: ["Process edges one by one using Union-Find.", "If an edge connects two already-connected nodes: it's the redundant edge.", "Return the first such edge found (which, given 'last appearing' constraint, means process in order)."],
    clarify: { question: "What would you clarify?", options: ["Only one redundant edge exists?","Should I return the last redundant edge if multiple exist?","Are nodes numbered 1 to n?","Is the graph undirected?","Guarantee the input forms a valid tree plus exactly one extra edge?"], mustAsk: [0,1,3], explanation: "Exactly one redundant edge (exactly one cycle). If multiple could be removed, return the one appearing last in input. Nodes 1..n, undirected. Input guarantees tree + one edge." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "A tree with n nodes has exactly n-1 edges. Adding one edge creates exactly one cycle. What does 'redundant edge' mean here?", why: { question: "What makes an edge 'redundant' in this context?", options: ["Heaviest edge","Removing it leaves a valid tree (n nodes, n-1 edges, fully connected, no cycles). Exactly one such edge exists.","First edge added"], correct: 1, explanation: "Tree: n nodes, n-1 edges, no cycles, fully connected. Extra edge creates one cycle. Removing any edge IN that cycle restores tree. Return the one that appears last in input." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would you try removing each edge and checking if remaining edges form a tree?", why: { question: "Why is brute force O(n²) or worse?", options: ["O(n log n)","For each of n edges: remove it, run DFS/BFS to check connectivity and acyclicity on n-1 edges. O(n) per check × n edges = O(n²).","O(n) is achievable trivially"], correct: 1, explanation: "n edges × O(n) tree-check each = O(n²). Union-Find processes edges in order, detects the cycle-creating edge in O(n×α(n)) ≈ O(n)." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "How does Union-Find detect the exact edge that creates the cycle?", why: { question: "When does Union-Find identify a redundant edge?", options: ["After processing all edges","When processing edge (u,v): if find(u)==find(v), u and v are ALREADY connected. Adding this edge creates a cycle — it's redundant. Return immediately.","When a node has degree > 2"], correct: 1, explanation: "Union-Find processes edges in order. When find(u)==find(v) before union: they're already in the same component. This edge creates a cycle. It's the redundant one." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through: for each edge, check if nodes already connected. If yes — redundant. If no — union them.", why: { question: "Why process edges in INPUT ORDER (not sort by weight or nodes)?", options: ["Order doesn't matter","'Last appearing' constraint: process in input order, return the FIRST edge detected as redundant. Input order ensures correctness for 'last in list' requirement.","Sort makes it faster"], correct: 1, explanation: "Problem guarantees exactly one cycle. Processing in input order: first redundant edge found is the one to return. 'Last appearing' means: among all edges in the cycle, return the last one in the input — which is exactly the one that first triggers find(u)==find(v)." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "DFS cycle detection vs Union-Find — which is simpler for this problem?", why: { question: "What's the advantage of Union-Find over DFS here?", options: ["DFS is always simpler","Union-Find processes edges incrementally — detects cycle edge exactly when it's added. DFS must re-traverse entire graph each time to find the cycle edge. Union-Find is both simpler and faster here.","Same complexity"], correct: 1, explanation: "Union-Find: O(n×α(n)), finds cycle edge in one pass. DFS to find cycle: O(n+m), plus need to identify which specific edge in cycle to remove. Union-Find naturally identifies the cycle-creating edge." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why nearly O(n)?", options: ["O(n²) adjacency matrix","n edges processed, each O(α(n))≈O(1) union/find. Total: O(n×α(n))≈O(n). Space: O(n) parent/rank arrays.","O(n log n)"], correct: 1, explanation: "n edges (exactly n edges for n-node tree plus one). Each edge: O(α(n)) ≈ O(1). Total: O(n). Space: O(n) for DSU arrays." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What if the redundant edge is the first edge in the list?", why: { question: "edges=[[1,2],[2,1]] — is [2,1] redundant?", options: ["[1,2] is redundant","[2,1] is the last appearing duplicate. find(2)==find(1) when processing [2,1] (after [1,2] already unioned them). Return [2,1].","Both are redundant"], correct: 1, explanation: "Process [1,2]: union(1,2). Process [2,1]: find(2)==find(1)→same component→return [2,1]. The second (last appearing) duplicate edge is redundant." } },
    ],
    dryrun: { question: "edges=[[1,2],[1,3],[2,3]]. parent=[0,1,2,3]. Process [1,2]: find(1)=1, find(2)=2, different → union. parent[2]=1. Process [1,3]: find(1)=1, find(3)=3, different → union. parent[3]=1. Process [2,3]: find(2)=?, find(3)=?", options: ["find(2)=1, find(3)=1 → same! Return [2,3]","find(2)=2, find(3)=3 → different, union","Error"], correct: 0, explanation: "find(2): parent[2]=1, parent[1]=1 → root=1. find(3): parent[3]=1 → root=1. Same root! Edge [2,3] is redundant — return [2,3]." },
    pushback: { challenge: "What if the graph could have multiple connected components (not guaranteed to be a tree + 1 edge)?", options: ["Still use Union-Find: detect any edge where find(u)==find(v). Return last such edge. Works for any graph.","Need different algorithm","Union-Find only works for trees"], correct: 0, explanation: "Union-Find handles any graph. For cycle detection in general graphs: same approach — process edges, return last edge where both endpoints already connected. Works whether 1 or multiple cycles." },
    followup: { question: "Redundant Connection II — what if the graph is DIRECTED (each node has at most one parent in a rooted tree)?", options: ["Two cases: (1) node with two parents — one parent edge is redundant; (2) cycle exists. Handle both with Union-Find + in-degree tracking. More complex.","Same algorithm as undirected","Always return last edge"], correct: 0, explanation: "Directed version: check in-degrees. If node has 2 parents: one of its incoming edges is redundant. If cycle exists (no double-parent): find cycle edge with Union-Find. Combine both checks." },
    solution: { approach: "Union-Find — Detect First Cycle Edge", steps: ["parent[i]=i, rank[i]=0 for i in 1..n","For each edge (u,v): if find(u)==find(v): return [u,v] (redundant)","Else: union(u,v)","Never reached by problem guarantee"], time: "O(n×α(n))", space: "O(n)" },
  },
];
