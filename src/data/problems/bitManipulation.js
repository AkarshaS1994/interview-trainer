export const BIT_MANIPULATION_PROBLEMS = [
  {
    id: "bm1", title: "Single Number", difficulty: "Easy", pattern: "Bit Manipulation",
    companies: ["Meta","Amazon","Google"], minLevel: 1,
    description: "Given non-empty integer array where every element appears twice except for one, find that single one. Must run in O(n) time and O(1) extra space.",
    example: { input: "nums=[4,1,2,1,2]", output: "4" },
    hints: ["XOR: a ^ a = 0, a ^ 0 = a.", "XOR all elements — duplicates cancel out.", "One pass, one variable."],
    clarify: { question: "What would you clarify?", options: ["Exactly one element appears once?","All others appear exactly twice?","Can the single number be 0?","Negative numbers allowed?","Array is non-empty?"], mustAsk: [0,1,3], explanation: "Exactly one element appears once. All others appear exactly twice (not more). Negative numbers: XOR works on bit representation. Non-empty guaranteed." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What makes XOR perfect for this problem? What are XOR's key properties?", why: { question: "Which XOR properties solve this problem?", options: ["XOR is commutative only","a^a=0 (self-cancellation) + a^0=a (identity) + commutativity + associativity. These together mean all pairs cancel, leaving the single.","XOR is just addition mod 2"], correct: 1, explanation: "a^a=0: pair cancels. a^0=a: single survives. Commutativity and associativity: order doesn't matter. XOR all nums: pairs cancel to 0, single XOR 0 = single." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would a HashMap (count frequencies) solve this? Why violates the O(1) space constraint?", why: { question: "HashMap approach: O(n) time but what space?", options: ["O(1)","O(n) — store count for each unique number. Violates the O(1) constraint.","O(log n)"], correct: 1, explanation: "HashMap: count[num]++ for each num. Find key with count==1. O(n) time, O(n) space. XOR: O(n) time, O(1) space — no storage needed." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "Trace through XOR of [4,1,2,1,2] step by step.", why: { question: "After XORing all elements of [4,1,2,1,2], what's the result?", options: ["4 — because 1^1=0, 2^2=0, leaving 4^0=4","0","7"], correct: 0, explanation: "4^1=5, 5^2=7, 7^1=6, 6^2=4. Or think: 1^1=0, 2^2=0, 4^0=4. XOR is commutative so grouping pairs first is fine." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Write the algorithm. It's literally 2 lines — what are they?", why: { question: "Why is no visited tracking, no sorting, no HashMap needed?", options: ["We need HashMap for correctness","XOR accumulator automatically handles pairing without memory. Mathematically guaranteed.","We need sorting"], correct: 1, explanation: "result=0. For num in nums: result ^= num. Return result. Two lines. The math (XOR properties) does all the work — no auxiliary data structure needed." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "XOR O(n) O(1) vs sort O(n log n) O(1) vs HashMap O(n) O(n). Which is best and why?", why: { question: "Why is XOR strictly better than sorting here?", options: ["Sorting is equally good","XOR: O(n) time O(1) space. Sort: O(n log n) time O(1) space. XOR is strictly better — faster with same space.","HashMap is best"], correct: 1, explanation: "XOR wins: O(n) time is strictly better than O(n log n) (sort) and equals HashMap but with O(1) space. XOR is optimal for this specific problem." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(1) space — don't XOR operations use memory?", options: ["They do use memory","Only one integer variable (accumulator) regardless of n. O(1) extra space.","XOR uses O(n) bits"], correct: 1, explanation: "One variable: result. Updated n times but still just one integer. O(1) extra space. XOR is a bitwise operation on the variable, not a data structure." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "Single element array [5]: return?", options: ["0","5 — only element, no pairs. 5^nothing = 5.","Error"], correct: 1, explanation: "Single element: result=0, then result^=5 → result=5. Return 5. XOR with 0 (identity) naturally handles this." } },
    ],
    dryrun: { question: "nums=[2,3,2,4,4]. Step through: result starts at 0. XOR each: 0^2=2, 2^3=1, 1^2=3, 3^4=7, 7^4=?", options: ["3","7","0"], correct: 0, explanation: "7^4: 7=0111, 4=0100. XOR: 0011=3. Result=3. Verify: 2^2=0, 4^4=0, leaving 3. Correct." },
    pushback: { challenge: "What if three copies of each element appear except one that appears once?", options: ["Bit counting: for each bit position, count 1s across all numbers mod 3. Non-zero bit = single number's bit. O(32n)=O(n).","XOR still works","Must sort"], correct: 0, explanation: "Three copies: XOR fails (a^a^a=a, not 0). Bit counting mod 3: for each of 32 bit positions, sum all values' bits, take mod 3. Remaining 1 = the single number's bit. O(32n)=O(n)." },
    followup: { question: "What if two elements appear once and all others appear twice?", options: ["XOR all: get x^y. Find any set bit in x^y (bit where x and y differ). Partition array by that bit, XOR each partition separately. O(n) O(1).","Can't solve with XOR","Need HashMap for two singles"], correct: 0, explanation: "Two singles x,y: XOR all = x^y. Find rightmost set bit (diff bit). Partition: nums where that bit=1 vs bit=0. XOR each group separately → gives x and y. O(n) O(1)." },
    solution: { approach: "XOR Accumulator", steps: ["result = 0","For each num in nums: result ^= num","Return result"], time: "O(n)", space: "O(1)" },
    keywords: {
      understand: ["XOR","a^a=0","a^0=a","cancellation","commutativity","associativity"],
      brute: ["HashMap","count","O(n) space","O(1)","violates","constraint"],
      pattern: ["trace","pairs cancel","single survives","accumulator","XOR all"],
      algorithm: ["result=0","result^=num","two lines","no auxiliary","guaranteed"],
      tradeoff: ["O(n)","O(n log n)","sort","O(1) space","strictly better","optimal"],
      complexity: ["O(n)","O(1)","one variable","integer","bitwise","accumulator"],
      edge: ["single element","[5]","identity","result=5","one element"],
    },
  },
  {
    id: "bm2", title: "Number of 1 Bits", difficulty: "Easy", pattern: "Bit Manipulation",
    companies: ["Meta","Amazon","Google"], minLevel: 1,
    description: "Write a function that takes an unsigned integer and returns the number of '1' bits it has (Hamming weight).",
    example: { input: "n=11 (binary: 00000000000000000000000000001011)", output: "3" },
    hints: ["n & (n-1) removes the lowest set bit.", "Count how many times you can do this before n becomes 0.", "Or: n & 1 checks LSB, then right-shift."],
    clarify: { question: "What would you clarify?", options: ["Signed or unsigned integer?","32-bit or 64-bit?","Return count of 1s or positions?","Can n be 0?","Is the input always non-negative?"], mustAsk: [0,1,3], explanation: "Unsigned 32-bit integer (standard for this problem). Return count of 1-bits. n=0: return 0 (no 1-bits). Non-negative always." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "Convert n=11 to binary. How many 1 bits does it have?", why: { question: "11 in binary is?", options: ["1011 (four digits, three 1s)","1010","11 = 3 ones"], correct: 0, explanation: "11 = 8+2+1 = 1011 in binary. Three 1-bits. Leading zeros don't count. 00000000000000000000000000001011 — same number, 3 ones." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would the naive approach (check each bit) work? What's the complexity?", why: { question: "Why is the naive bit-check O(32) = O(1) rather than O(n)?", options: ["It's O(log n)","Integer is fixed-width (32 bits). Always exactly 32 iterations regardless of value. O(32) = O(1).","O(n) for n bits"], correct: 1, explanation: "Fixed 32-bit integer: always check all 32 bit positions regardless of value. O(32) is constant. Not O(n) in terms of input value — n here is a fixed-size type." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "The Brian Kernighan trick: n & (n-1) removes the lowest set bit. Why?", why: { question: "Why does n & (n-1) always remove exactly the lowest set bit?", options: ["Coincidence","n-1 flips the lowest set bit from 1 to 0 and all lower bits from 0 to 1. ANDing with n clears those positions.","n-1 removes all bits"], correct: 1, explanation: "Example: n=12=1100. n-1=1011. n&(n-1)=1100&1011=1000. Removed the lowest set bit (the 1 at position 2). Repeat until 0: count = number of 1-bits." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through Kernighan's algorithm. How many iterations does it take?", why: { question: "How many iterations does n & (n-1) approach take?", options: ["Always 32","Exactly popcount(n) — one iteration per 1-bit. For n with k ones: k iterations.","Depends on position of bits"], correct: 1, explanation: "n & (n-1) removes one 1-bit per iteration. Stops when n=0. Total iterations = number of 1-bits. Much fewer than 32 if n has few 1-bits." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "Kernighan (k iterations) vs bit-shift (32 iterations) vs built-in popcount. When does each matter?", why: { question: "When does Kernighan's approach significantly outperform bit-shift?", options: ["Never — always 32 bits","When n has very few 1-bits (sparse): Kernighan does k<<32 iterations. For n=1 (one bit): 1 iteration vs 32.","Kernighan is always slower"], correct: 1, explanation: "Sparse integers: Kernighan dramatically faster. IP addresses, sparse flags: usually few 1-bits. Dense (like 0xFFFFFFFF=32 ones): similar to bit-shift. Built-in popcount: O(1) hardware instruction." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time complexity of Kernighan's approach?", why: { question: "Is the time complexity O(1) or O(log n) or O(k)?", options: ["O(log n)","O(k) where k = number of 1-bits. Since k ≤ 32 for 32-bit integers: O(1) for fixed-width integers.","O(n)"], correct: 1, explanation: "k ≤ 32 always. O(k) = O(1) for fixed 32-bit int. Technically O(k) relative to popcount. For interview purposes: O(1) since 32-bit is fixed width." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "n=0: how many 1-bits?", options: ["1","0 — no set bits in 0. Loop doesn't execute.","Error"], correct: 1, explanation: "n=0: Kernighan loop condition n!=0 is false immediately. Count=0 returned. Bit-shift: 32 iterations, 0&1=0 each time, count=0. Correct." } },
    ],
    dryrun: { question: "n=6 (binary: 110). Apply n&(n-1): 6&5=4, count=1. Then 4&3=0, count=2. n=0: stop. Return?", options: ["2","3","6"], correct: 0, explanation: "6=110. 6&5=110&101=100=4, count=1. 4=100. 4&3=100&011=000=0, count=2. n=0: stop. Return 2. Correct: 110 has two 1-bits." },
    pushback: { challenge: "What if you need to count 1-bits for all numbers from 0 to n (Counting Bits problem)?", options: ["DP: for each i from 1 to n: bits[i] = bits[i>>1] + (i&1). O(n) time O(n) space.","Count bits individually O(n×32)","XOR approach"], correct: 0, explanation: "Counting Bits: bits[i] = bits[i/2] + (i%2). Right shift one position gives a smaller number whose bit count we've already computed, plus 1 if LSB is 1. O(n) total." },
    followup: { question: "What is the Hamming distance between two numbers (number of positions where bits differ)?", options: ["XOR the two numbers (flips bits where they differ), then count 1-bits with popcount. Hamming = popcount(x XOR y). O(1).","Compare bit by bit","Subtract and count"], correct: 0, explanation: "Hamming distance: (x^y) has 1s where x and y differ. popcount(x^y) = Hamming distance. One XOR + one popcount = O(1). Elegant composition of two bit tricks." },
    solution: { approach: "Brian Kernighan's Algorithm", steps: ["count = 0","While n != 0: n = n & (n-1); count++","Return count"], time: "O(k) where k = number of 1-bits", space: "O(1)" },
    keywords: {
      understand: ["binary","1-bits","Hamming weight","11=1011","three 1s","unsigned"],
      brute: ["32 iterations","check each bit","fixed-width","O(32)","O(1)","constant"],
      pattern: ["n & (n-1)","lowest set bit","removes","n-1 flips","AND clears"],
      algorithm: ["Kernighan","k iterations","popcount","one per 1-bit","until 0"],
      tradeoff: ["sparse","few 1-bits","k<<32","hardware popcount","O(1)","dense"],
      complexity: ["O(k)","k ≤ 32","O(1)","fixed width","32-bit","constant"],
      edge: ["n=0","count=0","no set bits","loop doesn't execute","immediate"],
    },
  },
  {
    id: "bm3", title: "Counting Bits", difficulty: "Medium", pattern: "Bit Manipulation",
    companies: ["Amazon","Google","Meta"], minLevel: 3,
    description: "Given integer n, return array ans of length n+1 where ans[i] is the number of 1s in the binary representation of i.",
    example: { input: "n=5", output: "[0,1,1,2,1,2]" },
    hints: ["ans[i] = ans[i>>1] + (i&1).", "i>>1 is i/2 (same bits shifted right).", "Build from i=1 upward — each answer uses a previously computed result."],
    clarify: { question: "What would you clarify?", options: ["Include ans[0] (which is 0)?","Return for 0..n inclusive?","Any time complexity requirement?","Return array or print?","n can be 0?"], mustAsk: [0,1,2], explanation: "Include 0 — array of length n+1. 0..n inclusive. Ideally O(n) (can't do better). Return array. n=0: return [0]." },
    steps: [
      { id: "understand", label: "Problem Understanding", emoji: "🔍", prompt: "What's the naive O(n × 32) approach and why can we do O(n)?", why: { question: "Why can we compute bits[i] in O(1) using bits[i>>1]?", options: ["We can't — each number is independent","i>>1 = i with LSB removed. bits[i>>1] is already computed. bits[i] = bits[i>>1] + LSB of i. One lookup + one bitwise op.","Coincidence"], correct: 1, explanation: "Right-shift removes LSB. The remaining bits' count is bits[i>>1] (already computed). Add 1 if LSB was 1 (i&1). This DP builds on previously computed answers." } },
      { id: "brute", label: "Brute Force", emoji: "🔨", prompt: "How would counting bits for each number individually work?", why: { question: "Why is O(n × 32) = O(n) asymptotically but worse in practice?", options: ["Same thing","n numbers × 32 iterations each = 32n operations. O(n) but 32× constant factor vs O(n) with factor 1-2. For n=10^6: 32M vs 2M ops.","It's O(32n) which is worse"], correct: 1, explanation: "Brute force: 32 operations per number × n numbers = 32n. DP: 2 operations per number × n = 2n. Same big-O but 16× fewer operations. For large n, the constant matters." } },
      { id: "pattern", label: "Pattern Recognition", emoji: "🧩", prompt: "State the DP recurrence. What property of bit shifting makes this work?", why: { question: "Why does bits[i] = bits[i>>1] + (i&1) work for all i?", options: ["Only works for even i","Universal: right-shifting removes LSB (binary division by 2). Remaining value i>>1 has same higher bits. Lower bits count = bits[i>>1]. Add 1 if original LSB was 1.","Only works for powers of 2"], correct: 1, explanation: "Works for ALL i ≥ 1: i's binary = (i>>1)'s binary with LSB appended. So count(i) = count(i>>1) + (i&1). i>>1 is always < i, so its answer is computed first in bottom-up DP." } },
      { id: "algorithm", label: "Algorithm Design", emoji: "⚙️", prompt: "Walk through computing bits[0..5] using the recurrence.", why: { question: "What is bits[5] using the recurrence?", options: ["bits[5] = bits[2] + (5&1) = 1 + 1 = 2","bits[5] = bits[4] + 1 = 1 + 1 = 2","bits[5] = 5 bits"], correct: 0, explanation: "5>>1=2, 5&1=1. bits[5]=bits[2]+(5&1)=1+1=2. Verify: 5=101 binary, two 1-bits. Correct." } },
      { id: "tradeoff", label: "Trade-off", emoji: "⚖️", prompt: "i>>1 recurrence vs i&(i-1) recurrence. What's the alternative?", why: { question: "Alternative: bits[i] = bits[i & (i-1)] + 1. What does this mean?", options: ["Wrong formula","i&(i-1) removes lowest set bit. bits[i&(i-1)] counts remaining 1s. Add 1 for the removed bit. Same O(n) but slightly different structure.","Same as i>>1"], correct: 1, explanation: "bits[i&(i-1)] + 1: i&(i-1) removes lowest 1-bit. We've already computed that count. Add 1 for the removed bit. Both recurrences are O(n) — just different ways to express the same DP." } },
      { id: "complexity", label: "Complexity", emoji: "📊", prompt: "Time and space complexity?", why: { question: "Why O(n) time is optimal for this problem?", options: ["O(n log n) is optimal","Must output n+1 values — O(n) output implies O(n) time lower bound. Can't do better than what you need to output.","O(1) possible"], correct: 1, explanation: "Output array of n+1 elements. Must write n+1 values = O(n) minimum. Our DP achieves this minimum — O(n) time, O(n) space (the output array)." } },
      { id: "edge", label: "Edge Cases", emoji: "🔐", prompt: "What edge cases exist?", why: { question: "n=0: return?", options: ["[]","[0] — bits[0]=0, zero has no 1-bits","[1]"], correct: 1, explanation: "n=0: array of length 1, just [0]. bits[0]=0 by definition. Loop from 1 to n doesn't execute. Return [0]." } },
    ],
    dryrun: { question: "n=4. Fill bits[0..4]: bits[0]=0, bits[1]=bits[0]+(1&1)=0+1=1, bits[2]=bits[1]+(2&1)=1+0=1, bits[3]=bits[1]+(3&1)=1+1=2, bits[4]=bits[2]+(4&1)=1+0=?", options: ["1","2","0"], correct: 0, explanation: "bits[4]=bits[2]+(4&1)=1+0=1. Final array: [0,1,1,2,1]. Verify: 0=0,1=1,2=10,3=11,4=100. Counts: 0,1,1,2,1. Correct." },
    pushback: { challenge: "What if n can be up to 10^9 — too large for an array?", options: ["For individual queries: use O(32) popcount per number. For range sums: use closed-form digit DP. Array approach infeasible for n=10^9.","Same array approach","O(n log n) alternative"], correct: 0, explanation: "Large n: can't allocate 10^9 array. For single count: O(32) Kernighan or hardware popcount. For 'sum of 1-bits from 0 to n': digit DP on binary representation. O(log n) per query." },
    followup: { question: "Given this bits array, find the maximum XOR of two numbers in 0..n?", options: ["Different problem — use Trie (bit trie) for O(n×32) maximum XOR pair finding. Counting bits array doesn't directly help.","Use the bits array","XOR all pairs"], correct: 0, explanation: "Maximum XOR pair: unrelated to counting bits. Build prefix trie of binary representations. For each number, greedily pick opposite bit at each level. O(n×32)." },
    solution: { approach: "DP with Right-Shift", steps: ["bits = [0] * (n+1)","For i in 1..n: bits[i] = bits[i>>1] + (i & 1)","Return bits"], time: "O(n)", space: "O(n)" },
    keywords: {
      understand: ["O(n)","O(n×32)","DP","i>>1","LSB","previously computed"],
      brute: ["32 iterations","each number","32n","constant","practice","factor"],
      pattern: ["recurrence","right-shift","i>>1","i&1","all i","bottom-up"],
      algorithm: ["bits[5]=2","5>>1=2","5&1=1","1+1","101","two 1-bits"],
      tradeoff: ["i&(i-1)","remove lowest","add 1","alternative","same O(n)","different"],
      complexity: ["O(n)","optimal","output","lower bound","n+1 values","minimum"],
      edge: ["n=0","[0]","bits[0]=0","length 1","loop doesn't execute"],
    },
  },
];
